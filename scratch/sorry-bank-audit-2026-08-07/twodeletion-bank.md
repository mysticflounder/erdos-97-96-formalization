# TwoDeletionCollision theorem-bank audit (2026-08-07)

Read-only audit.  Searches used the scoped corpus
`erdos-97-96-formalization-lean` and the four canonical P97 theorem-bank
registries (`docs/general-n-certificate-bank-mining-2026-07-09.md` and the
three `certificates/*general*n*mining.{md,json}` files).  No exact current
producer for any of the three leaves was found; the registry hits were older,
unrelated U1/C5D3B or generic geometry entries.

| Leaf (source lines) | Exact live input and first missing antecedent | Candidate/provenance and import reachability | Verdict and circularity |
|---|---|---|---|
| `b1_globalGapOrClosedTerminal_of_counterexample` (`TwoDeletionCollision.lean:142-149`) | Input is exactly a `B1GlobalTransportContext` (the full two-deletion collision package).  The required output is the disjunction in `B1Live.lean:723-754`: a third bisector carrier, a bisector fiber of cardinality at least 3, or a universal escaped-shell overlap lower bound.  Existing local B1 results produce an escape and an overlap *upper* bound, not any of these three antecedents. | `B1GlobalTransportContext` and `B1GlobalGapOrClosedTerminal` are source definitions at `B1Live.lean:695-754`; `false_of_b1_global_gap_or_closed_terminal` (`:756-774`) is a checked consumer only.  `b1_live_bisectorSet_eq_pair` and the three `b1_live_false_*` results are checked consumers/normalizers, not a global producer.  `TwoDeletionCollision` imports `B1Live` directly. | **NO HIT** (producer gap).  The checked collision adapter immediately calls this sorry at `:207-208`, so all downstream B1 closure is conditional on this leaf; no upstream checked theorem closes it. |
| `false_of_exactFourMutualOmission_fourCenterCommonDeletion_blockerCoincidence` (`:630-699`) | Inputs include the exact-four mutual-omission pair, all blocker exclusions, a `CommonDeletionTwoCenterPacket` at `first.deleted.1`, and the finite split `first.deleted.1 = center(u) ∨ center(v) ∨ secondBlocker`.  The first missing antecedent is a contradiction from that carrier equality.  The checked `false_of_exactFour_twoDeletion_blockerTwoCycle` instead requires the two positive cross-row memberships (`:231-238`); neither the packet nor `_hcollision` supplies them. | `CommonDeletionTwoCenterPacket` (`CommonDeletionTwoCenter.lean:29-46`) only packages two q-deleted `HasN 4` rows, blocker avoidance, exact row cardinalities, and overlap ≤ 2; its constructor (`:50-60`) adds no cross-deletion incidence.  RetainedMatching normalization/cycle theorems produce packets/walk endpoints only.  Kalmanson schemas require explicit CCW/equality premises absent here. | **NO HIT** (not close/help).  The checked combined theorem (`TwoDeletionCollision.lean:803-925`) calls this leaf for the three coincidence branches; the later one-way-cross consumer (`:930-1014`) calls the combined theorem, so this sorry is downstream of the checked split and upstream of the final five-center consumer (`:1024-1096`). |
| `false_of_exactFourMutualOmission_fourCenterCommonDeletion_survivalSquare` (`:704-802`) | Input adds one of four bidirectional survival-square disjunctions (`:764-802`), plus the same packet and exclusions.  The first missing antecedent is a theorem ruling out *one* such square under these exact hypotheses (e.g. a new geometric/order obstruction or positive row-incidence bridge). | Scoped hits include only generic survival-to-support-omission equivalences and collision endpoint producers.  `CommonDeletionTwoCenterPacket` still supplies local q-deleted rows, not a square contradiction; RetainedMatching cycle results are producer walks, not a universal square obstruction.  No survival-square/collision theorem with this signature exists in the canonical banks. | **NO HIT** (not close/help).  The checked split (`:395-460`) is only a finite case split yielding the four squares or three coincidence equalities.  The combined theorem routes the four square branches to this sorry (`:894-925`), then one-way-cross and five-center consumers depend on it; no circular upstream proof is available. |

The only checked nearby contradiction is the two-cycle theorem: it uses three distinct
bisector-fiber points (physical apex and both actual blockers) after obtaining the two
cross-row memberships.  That exact bridge is the missing content in the coincidence
leaf, and no existing bank entry supplies it.  No Lean build or source edit was run.
