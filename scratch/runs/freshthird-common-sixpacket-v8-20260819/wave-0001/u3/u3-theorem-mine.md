# Post-v10 FreshThird theorem mine — 2026-08-19

The new solver data consists of one corrected SAT readback for the U3
exact-radius ingress.  Its only generalizable source facts are the already
encoded frame obligations: a three-point dangerous-radius class, six bounded
audit centers, and an escaping q-deleted four-row.  The readback supplies no
canonical-row membership, blocker-fiber identity, return/collision, or strict
descent fact.

A bounded indexed-Lean search for a theorem consuming this exact pattern found
only existing APIs:

- `exists_escaping_shift_or_mixedConfinedAuditPacket`;
- `exists_escaping_shift_or_confinedCriticalFourShell`;
- the mixed-frame and escape-cycle reductions; and
- the existing `SixSurvivorU3AuditObstruction` consumer shell.

No new source-level producer was found.  The v10 SAT model is therefore a
negative theorem-mining result: adding the same U3 fields to another finite
query is not justified.  The next candidate must bridge the arbitrary
q-deleted escape row to the canonical `indexedRow`/blocker map, or prove a
well-founded descent/return theorem for repeated escapes.
