# Math Skeptic Audit: ATAIL-SUB2 (SUB2-H UNSAT, commit 46d4d056)

Transcribed verbatim by the orchestrator from the skeptic agent's final
message. All other artifacts in this directory are the agent's own
(audit_ident.py, audit_frame.py, fresh smoke_results.json,
witness_scan_results.json). The orchestrator independently re-ran
audit_ident.py: AUDIT IDENT RESULT: PASS.

**Target**: scratch/atail-sub2/ (STATEMENT.md, proof.md, verify_identity.py,
encoding.py, smoke.py, witness_scan.py, REPORT.md)

## Verdict per dimension

**1. Statement fidelity — CONFIRMED.** I hand-expanded every asserted atom of
both `census/candidate_d_probe/runs/SUB2_{left,right}.smt2` (all 20 atoms per
side, including the ten I-ORD triples per chain and the three duplicated
v2/v3-collinear atoms) and each matches STATEMENT.md's reconstruction exactly
at oy = 0. `build_sub2` (encoder.py:489-518) semantics match: closed disk
(`ge` on R² − d², expanding to p₀ − p₀² − p₁² + 2ω·p₁), strict below-axis,
strict ρ² > 0, strict all-triples order. The 7-variable/8-variable resolution
(oy → 0 per kill_notes K-D and STATE.md:102), the sA2(v2,p,v3) = −p₁
duplication note, and the closed-vs-open disk resolution (closed proved,
which subsumes) are all correct.

**2. Proof correctness — CONFIRMED.**
- Frame setup: legitimate. T234 > 0 forces w_a ≠ w_b and x off the line
  (h > 0); τ > 0 from sA2(w_a,w_b,x) = 2τh — I re-derived this. Degenerate
  cases are excluded by hypothesis, not assumption.
- All 11 conversion lemmas: I verified each by hand (bilinear cross-product
  expansion) and symbolically by machine (audit_frame.py: identities in
  ℚ(t)[M₀,M₁,h,τ] via tan-half-angle, plus the excluded direction (−1,0)
  separately — PASS). Directions and strictness all correct, including the
  right-chain sign flips.
- Convexity step: correct; the quantitative form
  Σ(w−v2)·(w−v3) = 2P·Q + 2τ² ≤ 0 checks.
- The identity (IDENT = λ·E): **independently re-verified** (audit_ident.py,
  written from scratch): exact division with remainder 0 AND re-expansion
  q·E − IDENT ≡ 0; a division-free check (IDENT vanishes identically as a
  polynomial in ℚ[a,b,s] under the general parameterization of E = 0); and
  2000 exact-rational configurations. λ is the explicit division quotient;
  E = (‖x−w_a‖² − ‖x−w_b‖²)/2 verified symbolically.
- Positivity bookkeeping: correct. Term 1 strictly positive (all three
  factors strict); terms 2–4 nonnegative (c2 ≥ 0 as a sum of squares,
  S2·S3 > 0 in both chains, g_a + g_b ≥ 0 from individual disk memberships);
  one strict term suffices; RHS = 0 under E = 0. Contradiction correctly
  assembled.
- Side claims: exactly 7 of 10 triples consumed per chain (verified
  per-triple against each chain's index set); the ω-carrying artifacts are
  killed as-they-stand — g_a ≥ 2·oy·(−a1) ≥ 0 checked against the actual
  smt2 atoms (`ge` g + 2·oy·p₁, `gt` −p₁, `ge` oy).

**3. Verification artifact honesty — CONFIRMED, two notes.**
- verify_identity.py does what proof.md claims; the from-scratch Fraction
  division's loop invariant makes empty remainder a sound positive
  certificate; re-run: ALL CHECKS PASS. No float in any decision path
  (floats appear only in the S4 diagnostic and the disclosed margin-banded
  two-ring scan, zero in-band).
- Note A (minor mislabel of evidence): proof.md Step 2 cites
  "machine-checked on 400 random exact-rational frames" — that is sampling,
  not symbolic verification. Non-blocking: the conversions are elementary,
  I verified them symbolically, and the certificate route is symbolic and
  does not depend on them.
- Note B (stale record): scratch/atail-sub2/smoke_results.json on disk said
  `S5 pass: false, ALL_PASS: false`. The cause (dyadic-float literal
  formatting in the sympy set comparison) was fixed losslessly in the
  checked-in smoke.py and disclosed in s5_atom_fix.json and REPORT.md, but
  the primary results file was never regenerated. My full re-run of the
  checked-in suite: SMOKE ALL PASS (fresh record at
  scratch/atail-sub2-audit/smoke_results.json). [Orchestrator: the stale
  file was regenerated by a fresh smoke.py run on 2026-07-12; on-disk
  ALL_PASS is now true.]

**4. Smoke gate — CONFIRMED.** m ≤ 2 and vertex-pair ⇒ s = 0 reproduce unsat
(mixed-pair monoliths unknown; accepted via the kill_notes-verbatim lemma
split, all four legs unsat — documented, sound). All 13 banked witnesses PASS
the independent exact full-window evaluator, which I checked atom-by-atom
against build_window; straddle witnesses 0202/0211 are k = 1 per
witnesses.json and PASS. Witness scan: zero hits across 13 probe witnesses,
W15/W16 lattice families (exact), two-ring families (reproduced
independently, ALL CLEAN). The third-cut handling is **not circular**:
encoding validation rests on S4/S5/S6 (atom-identical transcription against
both artifacts, single-violation near-miss reproduction matching
float_refine_sub2.py:48 and the probe's −4e−6 margin, and an exact
all-but-T123 control), none of which consume SUB2. The doc discrepancy is
real and pre-existing: census/candidate_d_probe/report.md:102 and
docs/dead-ends.md (~line 802-805) list "s = 2 straddle-only" among PROVEN
constraints while STATE.md:58-59 and kill_notes K-D mark the kill direction
as riding SUB2 — an overclaim in those two docs at probe time (the proven
content was k = 1 realizability). Mooted by this decision, but the mislabel
should be annotated. [Orchestrator: both sites annotated 2026-07-12.]

**5. Downstream claims — CONFIRMED.** The H-reduction I verified directly
(2ω·p₁ ≤ 0, one line) — and it is moot anyway since the certificate kills
the ω-carrying artifacts directly. The constraint-subset map I verified
against build_window myself: subsequence triples of I-ORD, identical
E-PKT/I-POS/I-DISK atoms, oy ≥ 0 from the MEC identity + I-OBT_v1 + b > 0
(itself the I-ORD triple (v2,v3,v1)), below-axis from strict I-CAP chord 1 +
v1 above axis. Pigeonhole covers every k-case of (0,3,0,1), (0,4,0,0),
(1,3,0,0) and k ∈ {0,2} of (0,2,0,2), (0,2,1,1), (1,2,0,1); k = 1 correctly
stays OPEN. K_plus scope exact: q2_token.py:131-132 defines
K_plus = K_proven ∪ {(0,3,0,1),(0,4,0,0),(1,3,0,0)} — precisely the new
whole-class kills; the sub-case kills remove no token class (each survives
via k = 1), so "no Q2 rerun needed, K_plus tier becomes PROVEN" is exactly
scoped. The exclusion-soundness premise (formulation.md §3,
orchestrator-verified 2026-07-07) is accurately stated as the remaining
conditionality.

## Headline: AUDIT PASS — safe to record SUB2-H UNSAT as PROVEN (both
chains, closed-disk form, and the ω-carrying SUB2 artifacts), with the
downstream kills as scoped.

Non-blocking items to fix when recording (all executed 2026-07-12):
1. Regenerate stale scratch/atail-sub2/smoke_results.json — DONE.
2. Annotate the pre-existing "s = 2 straddle-only = PROVEN" overclaim in
   census/candidate_d_probe/report.md:102 and docs/dead-ends.md — DONE.
3. Record the PROVEN flavor precisely: pen-and-paper + exact machine-checked
   certificate. The closure-matrix ATAIL-SUB2 row's own definition-of-done
   ("A Lean theorem") is NOT yet met — the row is marked MATH-PROVEN with
   the Lean residue OPEN, not DONE.
