# FreshThird configured-branch call-6 theorem-bank audit (2026-08-08)

## Verdict

No banked theorem closes or authenticates call 6.  The fixed SMT2 input is Z3
`unsat`, but the independent cvc5 replays timed out.  It remains an empirical,
unverified fixed-input result and must not be promoted.

The required `nthdegree docs search --lean --agentic` attempt exhausted the
configured GPT-5.3-Codex-Spark quota, then its automatic fallback returned the
generic cardinality-independent consumer
`CapCrossingKalmansonBridge.false_of_five_ccw_three_shell_equalities_of_index_size`.
A scoped search of the current Lean corpus also found these nearby consumers:

- `CapCrossingKalmansonBridge.false_of_five_ccw_three_shell_equalities_of_index_size`;
- `CapCrossingKalmansonBridge.false_of_six_ccw_two_k2_three_selected_rows`;
- `FivePointEuclideanObstruction.false_of_five_ccw_second_two_selected_rows`;
- `false_of_freshThird_orderSelectedEndpointDeletionObstructions`.

The generic five-point consumer needs three named shell equalities, an
injective boundary/image presentation, convex independence, CCW, and the
accepted strict index order.  The selected-row consumers obtain their
equalities from actual `SelectedFourClass` rows and positive support
memberships.  The FreshThird-specific consumer instead requires its deletion
obstruction packet and source equalities.  Call 6 supplies none of those
universal ingress adapters merely from its fixed rank assignment.

The weakest currently visible missing bridge is a source theorem deriving the
generic consumer's three named shell equalities from the active one-way cross
survivor `1 -> 0` and the independent mutual survivors `0 -> 1` and `1 -> 0`,
followed by the injective boundary/CCW/strict-order lift.  Packaging those
survivors as actual selected rows with positive support incidences would be a
stronger sufficient producer.  The general theorem registries contain no
matching FreshThird cross/mutual producer.

Classification: consumers found; producer and lift missing; closure none.
