# Source map

Source gate: PASS. `SOURCE-VALIDATION.json` checks 9 Lean files, 32 declaration tokens, and full-file SHA-256 pins. `V2-PIN-VERIFICATION.json` checks all 9 source hashes, 6 predecessor hashes, v2 schema/source-validation hashes, and all 3 v2 base hashes before launch.

The encoding uses four explicit distinct support slots per materialized role, source membership in its own actual row, center exclusion, functional source aliases, exact same-blocker support equality, distinct-center overlap ≤2, named actual-blocker fiber cardinality ≤4, exact Q support cardinality 4, Q blocker equality, C source inequality, source-legal FirstNonHit/Interaction constructors, deletion-fan/repeated-pair constraints, and the kernel-clean thirteen-point/seven-circle obstruction as the authenticated lazy cut mechanism.

The quotient has no ambient carrier enumeration, MAXN bound, pinned endpoints, endpoint distances, or universal coverage lift.
