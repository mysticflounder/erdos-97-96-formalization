# Restricted FreshThird cross-row Kalmanson result (preserved)

This records the pre-correction query that only admitted `(b_z, pinnedCenter,
b_w)` triples and coupled the hfan pair to the active cross pair.  Branch 1
(`outside_fresh_between_source_canonical`, fresh cap 1, row cap 1, pair
`(0,1)`) was SAT under the exact structural complement.  Exact metric replay
was z3-UNSAT but cvc5 timed out at 120 seconds, so it was not authenticated
and no cut was permitted.  This result is restricted-scope evidence only.
