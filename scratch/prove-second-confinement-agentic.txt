The corpus supports the claim via a composed argument, not a single named lemma with exactly that wording.

Use the tri-apex retained setup first: `exists_three_hit_of_two_collisionRows_capCross_normalForm` gives the two-cycle/tri-apex normal-form context after checked exact-two normalization, so you are in the robust minimality regime where deletions and omissions must be tracked globally.  
From `exact_disjoint_cap_pairs_force_cross_omission_and_two_outside`, two disjoint exact two-point cap pairs force the complete cross-omission rectangle and each row has exactly two outside points.  
Then `exists_secondRowOutsidePoint_ne_firstOutsidePair` says the second collision row contributes two outside points of the first cap, and by minimality/normalization at least one can be treated as genuinely new unless it already lies in the first row’s outside pair.  
That candidate new point is ruled out as first-blocker (`secondRowOutsidePoint_blocker_ne_first`) and if it aligns with second-blocker behavior you get an escalated fiber packet (`freshOutsideSecondBlockerFiber_of_secondRowOutsidePoint`), which then feeds a forbidden directed one-way mutual-omission/joint-deletion pattern (`false_of_twoDistinctExactFourMutualOmissionJointDeletions_oneWayCrossOmission`).  
Together with support-overlap and blocker-separation facts (`selected_support_inter_card_le_two_of_not_mem_other_selected_support`, `blocker_centers_ne_of_not_mem_other_selected_support`) and critical-row center uniqueness (`actualRow_center_eq_commonBlocker_of_sameCap_outside_sources`), this blocks alternative outside-center accommodation.  
Under `uniqueFourCover_of_triApexRobust`, such a configuration cannot sustain those extra deletion-residue centers, so the second-row outside points must collapse into the original two outside points of the first collision row. That is, the second collision row’s outside portion is contained in the first row’s outside pair.
  handles: T9VHD1 KS0MF6 7MRYTD GF6R0X TJEGTR DKF9BG NK5YT6 0K3HZQ N51PQR X7H5RV  (read with `nthdegree docs show <handle> --lean`)
searched: nthdegree docs show KS0MF6 --lean, nthdegree docs show T9VHD1 --lean, nthdegree docs show GF6R0X --lean, nthdegree docs search --lean "blocker centers" "mutual omission" --kind theorem, nthdegree docs search --lean "second collision row outside" --kind theorem, nthdegree docs search --lean "tri-apex deletion" --kind theorem, nthdegree docs show TJEGTR --lean, nthdegree docs show 7MRYTD --lean, nthdegree docs show X7H5RV --lean, nthdegree docs show N51PQR --lean, nthdegree docs search --lean "second outside points first pair" --kind theorem, nthdegree docs search --lean "outside points of the first" --kind theorem, nthdegree docs search --lean "cross-omission rectangle" --kind theorem, nthdegree docs show NK5YT6 --lean, nthdegree docs show DKF9BG --lean, nthdegree docs show 0K3HZQ --lean, nthdegree docs search --lean "two points outside the first cap", nthdegree docs search --lean "secondRowOutsidePoint" --kind theorem
reminder: agent-written answer — apply rigor standards to all claims.  Verify against the cited chunks (`nthdegree docs show <handle> [--lean|--corpus <slug>]`).  treat uncited claims as unverified.
