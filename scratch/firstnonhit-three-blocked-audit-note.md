Correction: the distinct-blocker arm does NOT provide the missing three blocked deletions. In `TwoSourceFreshThirdResidual.lean:1323`, `freshThird_qRow_distinctBlocker_has_two_omissionSuccessors Q source hcenter` gives `diff := KQ.support \\ Ksource.support`, `2 ≤ diff.card`, and every `w ∈ diff` has the POSITIVE statement `HasNEquidistantPointsAt 4 (D.A.erase w) (H.centerAt source.1 source.2)`. The landing theorem `selectedRow_inter_card_ge_three_of_blocked_deletions` instead requires the NEGATION `¬ HasNEquidistantPointsAt ...` for every member. `H.no_qfree_at source.1 source.2` supplies that negation only for `source.1` itself. Although `source.1 ∈ KQ.support` and `source.1 ∉ diff`, the natural candidate `blocked := insert source.1 diff` therefore contains two survivors, not two blocked deletions, and cannot satisfy `hblocked`. This is a polarity obstruction, not a Lean proof gap. The source gives at most one blocked Q-row point in this arm; the other two are explicitly deletion survivors. Remaining separate issue is exact blocker-fiber saturation branch.
