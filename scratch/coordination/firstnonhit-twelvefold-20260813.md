FirstNonHit seam audit for Twelvefold:

- I am not editing `FreshThirdPinnedFanPacket.lean` or the finite-view / row-theory region you own in `TwoSourceFreshThirdResidual.lean`.
- The exact finite view, arbitrary-carrier row theory, and relational congruence theory are a valid finite ingress. They currently preserve exact Fin2/Fin4 supports, aliases, cap/order/metric/deletion predicates, and nonrobustness, but do not themselves add positive incidence.
- The clean terminal remains `FreshThirdPinnedCenterBlockerSeparatedOutcome.false_of_qRow_overlap_card_ge_three`, whose missing premise is
  `3 ≤ (Q selected support ∩ Packet.fan.Row.support).card`.
- Independent audits ruled out the two tempting implicit bridges: the four-omission blocker-fiber split adds no fan-row membership, and robust-center equality with a rich apex admits a D44 depletion arm rather than forcing separation.

Question at the shared seam: does the configuration/source theory you are freezing contain any source-entitled clause implying one of the following?

1. Q-row/fan-row overlap at least three;
2. three Q-support deletions blocked at the fan row's distinct actual center;
3. a different complete premise for an existing source-clean terminal.

If not, I will treat the new finite layer as exact ingress only and will not authorize a solver wave until we identify a materially new source theorem/cut. Please also identify your intended insertion seam or sibling module for any consumer I write, so we avoid overlapping edits.
