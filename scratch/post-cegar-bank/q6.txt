`FreshThird`-related results in the merged Lean corpus say that the canonical blocker is controlled by the first cap: for an escaping FreshThird point, its canonical blocker lies in the strict interior of an indexed cap, and that same construction preserves the equal-vs-distinct canonical-blocker fork used by later cap-order consumers. For same-cap cross-deletion behavior, the key fact is this: if two distinct outside sources have distinct canonical blockers inside the same cap, then at least one cross-deletion preserves a 4-point row; forcing both deletions to fail is inconsistent (it would make both blockers bisect the same outside pair). In the cap-eight branch, there is also a guaranteed “third” cap-source whose blocker is different from the two collision blockers and both apices, and that source survives one deletion from each collision pair.

A companion FreshThird lemma builds a cyclic shared-pair packet from the canonical source row and fresh-blocker row (sharing the two fresh endpoints), giving canonical/different-cap boundary data for downstream consumers.
  handles: CC0HGR SKVQVC 3Y755Y Z9FAHM  (read with `nthdegree docs show <handle> --lean`)
searched: cd /Users/adam/projects/math-projects/erdos-97-96-formalization && nthdegree docs show J08R5A --lean && nthdegree docs s…, cd /Users/adam/projects/math-projects/erdos-97-96-formalization && nthdegree docs search --lean --limit 8 "FreshThird cr…, cd /Users/adam/projects/math-projects/erdos-97-96-formalization && nthdegree docs show Z9FAHM --lean && nthdegree docs s…, cd /Users/adam/projects/math-projects/erdos-97-96-formalization && nthdegree docs search --lean --kind theorem --limit 8…, cd /Users/adam/projects/math-projects/erdos-97-96-formalization && nthdegree docs show 3Y755Y --lean && nthdegree docs s…
reminder: agent-written answer — apply rigor standards to all claims.  Verify against the cited chunks (`nthdegree docs show <handle> [--lean|--corpus <slug>]`).  treat uncited claims as unverified.
