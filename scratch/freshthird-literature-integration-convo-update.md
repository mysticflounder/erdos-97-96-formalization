FreshThird literature-integration checkpoint:

- The full pinned-endpoint packet now has a source-clean dichotomy: either `DRow` contributes two actual first-cap witnesses, yielding a genuine `Dumitrescu.IsCapWitnessPair`, or `DRow.support ∩ firstCap` has cardinality at most one.
- A new generic `BoundaryIndexing.selectedFourSupportOrder` canonically sorts every `SelectedFourClass` support by actual boundary index, with exact support-coverage lemmas. This removes row-support cyclic order from the producer gap, but does not manufacture a cap pair, named crossing pairing, or Ptolemy crossing point.
- Generic cospherical/Ptolemy adapters remain conditional consumers. The focused target builds are green, and the new declarations have no `sorryAx` in the axiom audit.
- The earlier 2+2-cap Python fixture remains rejected. The corrected packet/replay scaffold passes 5 focused tests and Ruff, but is still diagnostic; an independent source-entitlement audit is running before any CEGAR wave.
- No closure claim: `false_of_freshThird_pinnedEndpoint_outsideSeedResidual` is currently off the published kernel spine. The next producer target is the ≤1 first-cap residual plus source-proved cap-side/pairing data.
