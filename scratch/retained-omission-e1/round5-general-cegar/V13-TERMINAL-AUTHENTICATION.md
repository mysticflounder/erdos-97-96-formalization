# v13 terminal authentication: exact-n15 retained-omission 16-case replay

## Verdict

**PASS, as a terminal incomplete replay.** The artifact tree
`artifacts-v13-canary/20260802T110226.865850Z-v11-16-replay-pid84479`
is internally authenticated and agrees with its frozen source-selection chain.
All 16 intended cases terminated `unknown`; none is `sat`, `unsat`, or `error`.
Consequently this run is fail-closed discovery evidence only: it proves no
finite-surface exclusion and supplies no Lean/kernel closure.

This was a read-only authentication. No solver, generator, or Lean command was
run.

## Hash and provenance checks

- Selection manifest byte SHA-256:
  `600d9d93662e8fe27a4074e75656f5ddaa3f759e06c05584291f661d3befe30b`.
  This equals `source_summary_sha256` in both replay invocation and summary.
- The selection's source invocation byte hash is
  `2a7af78eac6f2949b2ce9dc4d5808fffd6b87d3af1cb3ab9118cfb28ed76d4b5`;
  the live source invocation matches it. All 16 source `result.json` byte
  hashes match their manifest entries (16/16, zero mismatches).
- Frozen executable inputs match the recorded hashes: wrapper
  `62d98dea810928a38418544874e798bf1996c8d3afe5360be9e92f8fde9d5bea`,
  v13 `45470dd8282be615c90a3a324306e35d73452dbd281473d377b3a26415345849`,
  v12 `8da7127a36dafdbce667edcd44ebef0db62eb2245ad832f373817c4bc28df1c9`,
  v8 `e0c98be80b2729cf50ee1d9da6c081fc4db2095e66abfb3c85abfbf5799b7162`,
  v9 miner `f0bcb74f5117cd0fc47e524fc222e7c1a435bc8a85785f7e4dbbc504f35cc25e`,
  schema-v13 `763d55a7bd5ec75e6a67505804a8243589efbaedede48b491692e6abc4a18f45`,
  and the lane-local `uv.lock`
  `a942a01001ddeeb51124fc84740bc6ae5b6221fdda605a83509a3cff1bef399c`.
- All six Lean source-contract files still match the frozen hashes:
  CirclePowerMatrix `95c472f2...b81497`, U1CarrierInjection
  `deb69ae2...29393d`, Foundation `a391c93c...9de9bd3`,
  FrontierLiveClosure `ca0a4d33...e53ee`, PhysicalSecondApexCommonDeletion
  `98da3a4b...02dc3`, and SmallSReductions `af3345fb...dcad`.
- Every replay result repeats the invocation's target, schema/version, scripts,
  source contract, lock, and resolved budgets exactly (16/16). Invocation and
  summary agree after removing summary-only fields.
- Each per-case result is exactly equal as JSON data to its embedded summary
  result (16/16), and the summary's `full_encoding_sha256_by_case` is exactly
  regenerated by its results. Replay invocation and summary byte hashes are
  respectively `029e637b13f7f80420f9c2704d07a52d2bfc030c73d185d6530f27c2def046fd`
  and `d03cadd2b1601136ccfbab206864aa75a99df70caae3ceefe8856b947aba30b8`.
- Per case, the recorded hashes of `encoding.smt2.gz`,
  `boolean_encoding.smt2.gz`, `boolean_prepass.json`, and
  `power_cuts_v12.json` match the files; the decompressed full and Boolean SMT2
  hashes match their recorded formula hashes; and base/first-check/final/full
  full-encoding hashes agree because no cuts were learned. Total: 112/112 hash
  relations checked, zero mismatches.

For compact reproducibility, SHA-256 over sorted `case_id<TAB>sha256` inventories
is: source results `dadaaf8649c2f2be82e6e924e72e40b89bc677f61c5f4414aaa4e0124b1df437`,
replay result files `c303d8d831f36b06132df6cf28c7aa0f857ff759e4b99df54e0cef0b9d183242`,
decompressed full encodings `4086abab61ccbfd2e3a1f4e176e249722de7e7df089b4a560daef19a3cf81662`,
and decompressed Boolean encodings
`1e11dc0cf9a7afa2ddb0170d189303b5a4456c49c9e77577da63b408fe233bef`.

## Case-set authentication

The driver's literal `CASE_IDS`, the selection manifest, the 16 source-result
directories, the 16 replay directories, and the 16 summary results are the same
set. There are 16 unique IDs, 16 unique case records, and seeds 1 through 16
exactly once. Each ID is the canonical rendering of its case record. The set is
12 `fresh_DDD` cases plus 4 `fresh_SDD` cases; there are no missing, duplicate,
extra, or mislabelled cases.

## Terminal semantics and exit code

For every case:

- Boolean prepass: `status = "sat_no_cut"`, one solver check/model examined,
  zero learned cuts, `sound_full_case_unsat = false`, and
  `terminal_claim = "none"`. This is an abstraction model with no admissible
  mined cut, not a SAT verdict for the full formula.
- Full solver: one check, `status = "unknown"`, `reason_unknown = "canceled"`,
  `error = null`, `complete = false`, and zero cuts. Each check consumed
  600.000--600.059 seconds against `timeout_ms = 600000`, authenticating
  `canceled` here as the Z3 timeout outcome rather than an exception.

The summary therefore correctly says `counts = {"unknown": 16}` and
`complete = false`. `matrix_is_complete` accepts only an exact case set whose
statuses are all `sat`/`unsat`; `run_matrix` returns `0` when that predicate is
true and **`2` otherwise**. Thus this artifact deterministically implies driver
exit code 2: the documented incomplete/UNKNOWN channel, not the per-case error
channel (`error` is null in all 16 cases). The artifact tree does not separately
capture its parent shell's numeric exit status, so this is source-and-artifact
authentication of the exit semantics, not an independent shell-transcript
observation.

## Failures

None found.
