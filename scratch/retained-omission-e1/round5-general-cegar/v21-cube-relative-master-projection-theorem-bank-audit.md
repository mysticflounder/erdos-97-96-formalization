# V21 cube-relative master-projection theorem-bank audit

Date: 2026-08-02

## Verdict

**NO HIT.** The completed v21 pilot suggests no sound universal theorem and
provides no new live-spine-applicable bank consumer.

The exact finalized v19 sample has 802 of 825 semantic Boolean fixes invariant
across 190 rejected full assignments and 23 fixes varying. V21 tested all 16
current v20 cubes against the exact finalized Boolean master
(`fad5a5b2831f03efe387a796891462e3bf6a690ae3f3b73b609d148e68a20f6b`)
and exact cumulative-blocker identity
(`0c781b1b179eca9a149129a15a92b3ebfd3045122a6727319a841c4ae516b2aa`).
Every query `M and C and not I` was SAT. The recorded countermodels violate
between 156 and 193 proposed invariant fixes per cube. Thus the observed 802-fix
regularity is not a master entailment, and none of the 190 complete blockers can
soundly be replaced by the proposed cube-relative blocker. The exact result is
0 shortened cubes and 0/190 shortened current assignments.

This SAT result is about the authenticated finite Boolean master. It is not a
geometric counterexample to the live Lean theorem. Conversely, it supplies no
bridge from the finite exact-`n = 15`, DDD slice to the general carrier.

## Live-spine fit

The current source and generated blueprint place
`Problem97.ATailFrontierLiveClosure.false_of_retainedOmission_triApexAllLarge_core`
directly on the open `Problem97.erdos97_rhs` spine. Its inputs are
`FrontierCommonDeletionParentResidual`,
`RetainedOmissionAllLargeNormalForm`, and `TriApexAllLargeContext`; its body is
still `sorry` in `FrontierLiveClosure.lean`.

The one scoped indexed Lean-corpus search returned the existing source-clean
chain:

- `exists_criticalShell_center_with_otherRichCapSlice_card_le_one` locates a
  canonical critical-shell center in one strict cap and bounds every distinct
  rich-cap radius-slice intersection by one;
- `criticalShell_inter_otherRichCapSlice_card_le_one` proves that bound from
  `false_of_criticalShell_center_in_cap_of_two_hits_in_distinct_cap`;
- the terminal consumer requires an actual shell with at least two hits in a
  radius slice of a cap distinct from the shell center's cap.

This chain is directly import-reachable because it occurs earlier in the same
`FrontierLiveClosure.lean` module. It does not close the target: neither arm of
`RetainedOmissionAllLargeNormalForm` currently produces the required distinct-
cap two-hit packet. V21 does not improve that producer boundary. The useful
complementary theorem shape remains “`R`, `Q`, and `G` produce a canonical
critical shell and a distinct rich-cap radius slice with at least two hits,”
but that was already the v19 audit's identified missing theorem, not a theorem
suggested or supported by v21.

Exactly one `nthdegree docs search --lean --agentic` call was made for this
concrete candidate. No repeat or exploratory agentic Lean search was made.

## Registry dispositions

| bank family | reachability and exact producer requirement | v21 disposition |
|---|---|---|
| Current `Census554.EqualityCore` | In-tree consumers require an injective geometric `Realizes`; negative Boolean semantics additionally require `ExactAt` and a concrete excluded-label closure witness. | No producer. SAT countermodels rule out treating observed invariant fixes as one. |
| RVOL U5 incidence | The registry has 96 class-level arbitrary-`CounterexampleData` incompatibility families, supported by metric/scalar kernels, and the relevant bank is RVOL-reachable. Consumers require a `U5DangerousTriple`, exact Q-critical/deleted-K4 classes, and explicit cross-memberships. The registry has no arbitrary live-packet classifier into those families. | No live Q/G incidence packet or classifier. |
| RVOL U1 row-slot | Eighteen useful contradiction consumers are unimported. They require `U3FixedTriplePacket`, `RowSlotLabelPacket`, explicit row-distance equalities, and membership in one of eight certified ordinals. | No placement/equality or ordinal-coverage producer. |
| `u1TwoLargeCapObstruction` | Unimported local-pattern consumer requiring five named points, distinctness, seven exact distance equalities, and one inequality; it has no completeness theorem. | No matching metric packet. |
| RVOL/legacy declarations named “projection” | RVOL C5D3B projection emptiness needs exact real-coordinate circle equations and strict sign inequalities; renaming projections are non-contradiction helpers. Legacy projection-gap results are fixed-coordinate identities or fixed-cell inequalities. | Lexical false friends; Boolean blocker projection supplies none of their hypotheses. |
| Legacy Radon/Msolve/dimension-zero banks | Require an actual four-point convex/Radon configuration or fixed trigonometric/cell polynomial systems. Legacy aggregates are not current import-clean consumers. | No matching producer or live consumer. |
| Older general-theorem C-F/scalar reserve | The C-F sign route remains an unproved proposal; the Cramer/S0236 facts are scalar helpers with no current Q/G consumer. | No direct bridge. |

The bank census therefore leaves the same structural boundary as before:
proved finite/metric consumers exist, but the live branch lacks the geometric
incidence/placement producer needed to instantiate them.

## Evidence and verification limit

Audited inputs:

- `v21-saved-model-pilot.json` and the finalized v19 artifact identities;
- `docs/live-blueprint.md` and the current target source;
- `docs/general-n-certificate-bank-mining-2026-07-09.md`;
- `certificates/p97_rvol_general_n_mining.{md,json}`;
- `certificates/erdos97_legacy_general_n_mining.{md,json}`;
- `certificates/erdos_general_theorem_p97_mining.{md,json}`;
- the current indexed `erdos-97-96-formalization-lean` corpus.

This was a read-only source/registry/index audit. No Lean or Lake command,
kernel/axiom gate, SAT/SMT solver, production wave, or source edit was run.
The generated blueprint was not refreshed during this audit, so live-spine
claims are limited to its checked-in snapshot plus direct current-source
inspection. External Z3 SAT evidence remains outside the Lean kernel trust
boundary.
