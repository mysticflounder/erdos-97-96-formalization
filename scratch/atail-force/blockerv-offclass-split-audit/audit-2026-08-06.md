# `vRowBlockerOffClass` split audit (2026-08-06)

## Verdict

There is **no qualifying source-proved exhaustive split with a terminal
consumer/direct proof on every child** in the current theorem bank.

The smallest useful source-proved split is the two-hit/low-hit split for the
critical row sourced at the off-class blocker itself.  The two-hit child has a
direct cap-count contradiction.  The complementary child source-proves at
least three distinct same-center common deletions, but no existing theorem
consumes that packet to `False`.  Promoting the low-hit child now would
therefore replace the live `sorry` with another load-bearing leaf, not close it.

## Live leaf and unconditional consequences

In
`Problem97.ATailFrontierLiveClosure.false_of_exactFourRigid221_sourceHeavy_secondOppositeLarge_pentagonBlockerV_vRowBlockerOffClass`
(`Rigid221SourceHeavy.lean:3827`), set

```text
Hlate := lateFirstApexSystem R
C     := SelectedClass D.A S.oppApex2 P.rho
I     := S.capInteriorByIndex S.oppIndex2
w     := Hlate.centerAt P.v.1 P.v.2
Kw    := (Hlate.selectedAt w hwA).toCriticalFourShell
T     := C ∩ I
```

The parent already proves `w ∈ D.A` and assumes `w ∉ C`.  The exact `v`-row
trace gives `v,xv ∈ row(v)`.  `v ∈ I` follows by rewriting `Q.hblockerInterior`
with `Q.hblockerEqV`; `xv ∈ I` is `Q.hxvInterior`.  Therefore
`ATailTwoCenterCapLocalization.commonPhysicalPair_center_mem_secondCapInterior`
(`TwoCenterCapLocalization.lean:191`) gives

```text
w ∈ I \ C.
```

Moreover `u,xu ∈ I` comes from `packet.source_pair_interior`, and `v,xv ∈ I`
as above.  Their distinctness follows from the exact five-element equality
`packet.physical_class` together with `P.hclassFive = 5`.  Hence

```text
4 ≤ T.card
5 ≤ I.card                 -- add w, which is off C
7 ≤ S.oppCap2.card         -- capInteriorByIndex_card_add_two
```

This strengthens `Q.hsecond : 6 ≤ S.oppCap2.card`, but the current tree has no
upper-bound or cap-growth consumer for `7 ≤ S.oppCap2.card`.  The cap-cardinality
lead is therefore a producer only, not closure.

## Smallest exhaustive discriminator

Split on the decidable proposition

```text
2 ≤ (T ∩ Kw.support).card.
```

This is exhaustive, with complementary child
`(T ∩ Kw.support).card ≤ 1`.

### Child A: two physical-interior hits

Statement:

```text
2 ≤ (T ∩ Kw.support).card.
```

Choose distinct `x,y` in the intersection.  The row source `w` is in the
second cap.  Two-center localization puts its actual blocker
`Hlate.centerAt w hwA` in the same strict cap interior, because `x,y` are
distinct points of the physical radius class and of the row.  Thus
`{w,x,y}` is contained in `Kw.support ∩ capByIndex oppIndex2`, contradicting

```text
CapSelectedRowCounting.selectedFourClass_inter_capByIndex_card_le_two.
```

This is exactly the source-proved proof pattern of
`ATailLargeCapUniqueFiveCrossIncidence.false_of_largeCapUniqueFiveTwoHitCriticalRow`
(`LargeCapUniqueFiveCrossIncidence.lean:59`).  That theorem cannot be applied
literally because its packet is indexed by
`LargeCapUniqueFiveSecondApexRadius`, whose `unique_K4_radius` field is absent
here.  The direct proof itself does not need uniqueness in this leaf: the
exact five-point `rho` class and `w ∉ C` give survival at the physical apex,
while `Hlate.no_qfree_at` separates the actual blocker from that apex.

**Closure mapping:** source-proved direct contradiction using
`commonPhysicalPair_center_mem_secondCapInterior` and
`selectedFourClass_inter_capByIndex_card_le_two`.

### Child B: at most one physical-interior hit

Statement:

```text
(T ∩ Kw.support).card ≤ 1.
```

Since `4 ≤ T.card`, finite-set subtraction gives

```text
3 ≤ (T \ Kw.support).card.
```

Consequently there are three distinct `q1,q2,q3 ∈ C ∩ I`, each outside
`Kw.support`.  For every such `q`:

1. `cross_deletion_survives_iff_not_mem_selected_support` proves K4 at
   `Hlate.centerAt w hwA` survives deletion of `q`;
2. the exact five-point physical class proves K4 at `S.oppApex2` survives
   deletion of `q`; and
3. `ATailCommonDeletionTwoCenter.nonempty_commonDeletionTwoCenterPacket`
   constructs

```text
CommonDeletionTwoCenterPacket
  D Hlate q (Hlate.centerAt w hwA) S.oppApex2.
```

So the low-hit child is strictly narrower than the current leaf: it adds the
row-intersection bound, a three-point outside-row lower bound, and three
origin-tagged common-deletion packets at one fixed pair of centers.

**No-consumer arm:** no registered theorem turns these three generic packets
into `False`.

- `physicalSecondApex_commonDeletion_robust_or_critical`
  (`PhysicalSecondApexCommonDeletion.lean:146`) returns full robustness or a
  critical shell.  The live context already has full second-apex deletion
  robustness, so this does not eliminate the branch.
- `LargeCapUniqueFiveTwoCommonDeletionSources` and its normalization theorems
  require `LargeCapUniqueFiveSecondApexRadius`, including uniqueness of the
  positive K4 radius.  Exact cardinality five at `rho` does not prove that
  uniqueness.
- `PhysicalOmissionCycleParentResidual.false_of_largeSecondApexRadius_of_residualConsumers`
  takes the missing contradiction consumers as arguments; it is a conditional
  assembler, not a terminal.
- The U5 contradiction bank requires a classified incidence matrix
  (dangerous triple/tetrahedral packet, positive cross-incidences, or a
  row-slot classifier).  The three common-deletion packets do not provide it.

The first missing bridge is therefore a source-proved consumer for **three
distinct physical-interior deletion sources preserving K4 at the same
off-class-blocker/physical-apex pair**, under the retained rigid-pentagon
data, without assuming uniqueness of the apex K4 radius.

## Other checked splits

- Re-splitting `centerAt v` by the five class labels is already the parent
  split; the present theorem is its unique off-class child.
- Splitting by apex K4-radius uniqueness gives a useful unique-radius arm, but
  the non-unique arm has no terminal consumer; distinct radii only force a
  compatible cap-interior lower bound.
- Splitting the existing `v` row by further class incidences yields at most the
  already-known two-point row/class bound and no terminal.
- The singleton-minimal-core outside-class API requires an
  `ExactFourMutualOmissionSourceContext` for source `v` (not supplied here) and
  is nonterminal even if constructed.
- The 96 general-n U5 class consumers and 18 unimported U1 source-unit
  consumers are real source-proved theorems, but the live leaf has neither the
  requisite incidence classifier nor an ordinal `RowSlotLabelPacket` /
  `AllEightSlotMetricCore` producer.

## Recommendation

Do not add a new Lean child obligation merely for Child B unless the missing
three-common-deletion consumer is proved in the same change.  If a narrowing
split is desired despite non-closure, the two-hit/low-hit discriminator above
is the smallest honest one: Child A disappears and Child B records a concrete
cardinality decrease plus three deletion packets.  It must still be reported
as one unresolved on-spine leaf.

