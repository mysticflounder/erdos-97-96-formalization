# Generated-successor full-parent surface

Date: 2026-07-16

Status: **KERNEL-CHECKED SCRATCH INTERFACE; DIRECT CONTRADICTION STILL OPEN.**

Owned files:

- `GeneratedSuccessorParent.lean`
- this report

No production Lean, closure docs, blueprint state, protected file, or unique-row
artifact was changed.

## Result

`GeneratedSuccessorParent.lean` pins the remaining theorem at the first surface
which retains all of the data excluded by the known countermodels:

```lean
def FalseOfOriginalQOutsideMiddleSuccessor
    (P : OriginalQOutsideMiddleParentContext D p q t1 t2 t3 u) : Prop :=
  forall _surface : OriginalQOutsideMiddleSuccessorParentSurface P, False
```

The intended mathematical theorem is therefore exactly the emptiness of
`OriginalQOutsideMiddleSuccessorParentSurface P`. It is not the false reduced
claim that an arbitrary `CommonDeletionTwoCenterPacket`, or an arbitrary
`OriginalQOutsideMiddleSuccessor`, is contradictory.

The file also provides the requested assumption-taking consumer:

```lean
structure OriginalQOutsideMiddleSuccessorEliminator ... : Prop where
  close : FalseOfOriginalQOutsideMiddleSuccessor P

theorem false_of_originalQOutsideMiddleSuccessor
    (E : OriginalQOutsideMiddleSuccessorEliminator P)
    (K : OriginalQOutsideMiddleSuccessorParentSurface P) : False :=
  E.close K
```

`close` is the eliminator's only field. The adapter therefore does not split the
missing mathematics into hidden assumption-bearing wrappers. Constructing that
one field is precisely the remaining proof.

## Exact retained surface

`OriginalQOutsideMiddleParentContext` copies the current parent inputs before
frontier/F2 projection:

- the noncollinearity witness and nonobtuse circumscribed MEC triangle;
- the circle case and structural Moser triangle;
- the concrete `CapTriple`, surplus-cap index, live-source cap membership, and
  non-exact opposite-cap branch;
- `hNoM44`, `9 < D.A.card`, and global no-removability;
- the original common-critical-system existence input;
- the localized no-q-free and fixed-triple packets;
- the pairwise-distinct base, live U1 packet, and retained source-row center and
  support equations.

Its `leafPacket` is definitionally the existing CP-built
`leafSurplusPacket`; it is not a separately selected surplus packet.

`OriginalQOutsideMiddleSuccessorParentSurface` then retains:

1. `j != i` and `5 <= (CP.capAt j).card`, the actual second-large-cap witness;
2. one `LiveDangerousRetainingSystem W`, so the generated row and frontier use
   the same total critical map `W.H`;
3. the exact first-apex radius, positivity, cardinality-four lower bound, and
   `CriticalPairFrontier`;
4. one `FixedSingleFrontierRadiusTerminal` on that same frontier;
5. the history pair with `historyPair.x = frontier.pair.q`;
6. the coherent packet, anchored transition, and row-external fresh source;
7. the fresh source's off-surplus placement and the checked first-center
   equality with `p`;
8. the full `AnchoredProvenanceF2NormalForm`; and
9. one exact live-heavy-at-x, live-heavy-at-y, or mixed-at-x generated-source
   successor.

The final alternatives are represented by
`AnchoredF2GeneratedSuccessorCase`. Each constructor stores the actual
`OriginalQOffLiveGeneratedProfile` and the corresponding
`OriginalQOutsideMiddleSuccessor`; the generated center cannot drift to an
anonymous dangerous label.

## Exact anchored-origin direction

The original anchored origin has first center `coherent.firstCenter` and packet
`fresh.toSharedRowCommonDeletionPacket`. The F2 code then retypes that packet at
`p` using `firstCenter_eq_p`.

Those two packets are not claimed to be definitionally identical. The checked
adapter

```lean
OriginalQOutsideMiddleSuccessorParentSurface.toOriginTagged
```

reconstructs the original unaligned origin specifically with

```lean
RCommonDeletionOrigin.anchored
  history_source_eq_frontier_q transition fresh fresh_off_surplus
```

while the generated-successor case retains the aligned packet separately. This
is the exact implication direction of the current source and avoids an
unjustified dependent cast.

## Theorem-bank and nthdegree preflight

The required registries were searched before defining the interface:

- `docs/general-n-certificate-bank-mining-2026-07-09.md`;
- `certificates/p97_rvol_general_n_mining.{md,json}`;
- `certificates/erdos97_legacy_general_n_mining.{md,json}`; and
- `certificates/erdos_general_theorem_p97_mining.{md,json}`.

The current bank conclusion remains unchanged. The portable U1 consumers,
including `u1TwoLargeCapObstruction`, require explicit metric/incidence
producers. The bank itself does not derive those fields from the generated
successor. The registry explicitly records that the finite-consumer side is
available while the geometric producer remains missing.

`nthdegree docs list` was run, followed by Lean-corpus searches for:

- common-deletion packets coupled to a critical-pair frontier and second large
  cap;
- original-q outside-middle generated profiles;
- MEC/cap outside-pair contradictions; and
- second-apex deletion survival versus actual blockers.

The nearest hits were the already-audited
`outsidePair_unique_capCenter`, the blocker-separation theorems in
`CriticalPairFrontier`, and the old double-apex/U1 obstruction consumers. None
accepts this surface without a new parent-level producer.

## Why the existing sinks still do not apply

- `OffLiveCommonDeletionSuccessor` requires the new source to belong to the old
  middle row. Here `source_not_mem_middle` has the opposite polarity.
- `secondApexCritical_reorients_to_firstApexUniqueFour` requires deletion
  failure at the physical second apex. The successor was constructed from
  deletion survival there.
- `outsidePair_unique_capCenter` requires two named outside points shared by
  both distinct center circles. The successor supplies one generated point;
  omission from a chosen support does not produce the missing equality.
- `alignedRCommonDeletionBranch` merely reclassifies the successor and can
  revisit live-heavy or mixed F2.
- adding another origin constructor would retain provenance but supply no
  descending measure or immediate consumer.

Accordingly, this lane does not infer a distance inequality from support
omission and does not mine an anonymous support pattern.

## Countermodel boundary

The pinned statement is not refuted by either current negative audit:

- the exact local parabola models omit the global K4 system, convex/MEC/cap
  order, frontier origin, and second-large-cap witness; and
- the 24-vertex successor-cycle regression is a finite non-Euclidean common-
  deletion abstraction without the retained parent geometry or anchored
  original-q provenance.

Those models rule out a reduced geometry-free sink. They do not instantiate
`OriginalQOutsideMiddleSuccessorParentSurface`.

Conversely, the present scratch file does not prove the full surface empty.
The status is therefore:

- **PROVEN:** the full data can be represented without projection, the exact
  anchored origin can be recovered, and the one-field consumer adapter is
  kernel clean;
- **OPEN:** construct the `close` field, most plausibly using a cap/MEC/order or
  global critical-map fact unavailable on the reduced successor;
- **NOT CLAIMED:** closure of a production `sorry`.

## Validation

The Lean LSP correctly rejected this file as outside the Lake source root, so
the documented single-file validation exception was used. From `lean/`, the
file was checked against the existing scratch oleans with:

```bash
env LEAN_PATH="/private/tmp/p97-r-origin-tagged-coordinator-oleans:/private/tmp/p97-r-f2-original-q-outside-middle-oleans:/private/tmp/p97-r-f2-original-q-offlive-oleans:/private/tmp/p97-r-f2-original-q-source-extraction-oleans:/private/tmp/p97-r-f2-generated-escape-placement-oleans:/private/tmp/p97-r-f2-three-row-bank-deficits-oleans:/private/tmp/p97-r-f2-supportheavy-k4-split-oleans:/private/tmp/p97-r-f2-live-mixed-handler-root-oleans:/private/tmp/p97-r-f2-anchored-provenance-lift-oleans:/private/tmp/p97-r-f2-strict-cell-normal-form-oleans:/private/tmp/p97-r-f2-three-row-chain-oleans:/private/tmp/p97-r-f2-supportheavy-reduction-oleans:/private/tmp/p97-r-f2-liveheavy-placement-oleans:/private/tmp/p97-root-failure-parent-lift:/private/tmp/p97-r-failure-parent-lift-oleans:/private/tmp/p97-r-two-off-live-oleans:/private/tmp/p97-r-live-common-deletion-current-oleans:/private/tmp/p97-r-live-common-deletion-oleans:/private/tmp/p97-r-reciprocal-swap-reduction-oleans:/private/tmp/p97-r-collapse-lean427-current-pb2jam3_:/private/tmp/p97-r-orbit-entry-oleans:/private/tmp/p97-joint-transition-oleans:./.lake/build/lib/lean" \
  lake env lean -R .. -M 16384 \
  -o /private/tmp/p97-r-f2-generated-successor-parent-oleans/GeneratedSuccessorParent.olean \
  ../scratch/atail-force/r-f2-directed-original-q-row/generated-successor-parent/GeneratedSuccessorParent.lean
```

Exit status: `0`, with no Lean warnings. Both printed declarations close under:

```text
propext
Classical.choice
Quot.sound
```

The Lean source contains no `sorry`, `admit`, declared `axiom`,
`native_decide`, or `unsafe` declaration. No full build, production wiring,
blueprint refresh, or publication gate was run.
