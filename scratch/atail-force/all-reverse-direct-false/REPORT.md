# Exact-five all-reverse direct-`False` audit

Date: 2026-07-17

Status: **DIRECT FIXED-ANCHOR ORDER ROUTE REFUTED BY AN EXISTING
KERNEL-CHECKED EUCLIDEAN REGRESSION.  THE SMALLEST CURRENT DISTINCT
BANK-FACING TERMINAL CONTRACT IS AN ADJACENT U5 ALIGNMENT PACKET; IT IS NOT
NOMINATED AS A PRODUCER.  SOURCE DRAFT ONLY;
NO LEAN/LAKE BUILD WAS RUN WHILE THE USER-OWNED `ShellCurvature` LANE IS
ACTIVE.**

This lane owns only
`scratch/atail-force/all-reverse-direct-false/`.  It does not edit production,
plan documents, generated blueprint state, `ShellCurvature`, `surplusM44`,
the card-five cap/mutual-consumer file, or the protected 555/654 lanes.

## Theorem-bank preflight

Before deriving a new local contradiction, the lane checked the registries
required by `AGENTS.md`:

- `docs/general-n-certificate-bank-mining-2026-07-09.md`;
- `certificates/p97_rvol_general_n_mining.{md,json}`;
- `certificates/erdos97_legacy_general_n_mining.{md,json}`; and
- `certificates/erdos_general_theorem_p97_mining.{md,json}`.

Indexed Lean searches covered all-reverse exact-five cycles, a fixed external
equal-radius anchor with cap-chord bisectors, same-cap blocker order, repeated
outside pairs, and q-critical adjacent-center incompatibilities.  The nearest
current consumers remain:

- `CapSelectedRowCounting.outsidePair_unique_capCenter`;
- `U5QCriticalTripleClass.two_triple_centers_adjacent_incompatibility`;
- `U5QCriticalTripleClass.two_triple_centers_third_common_incompatibility`;
- the sibling-only
  `U5QCriticalTripleClass.mutual_no_p_exact_center_incompatibility`; and
- the already-audited physical omission-cycle and reverse-row reductions.

The bank contains consumers after positive cross-row alignment is named.  It
does not produce that alignment from the current all-reverse packet.

## Exact retained target

The requested theorem surface is

```lean
R : FrontierCoupledExactFivePhysicalCycleParentResidual H F
hreverse : forall i,
  source i in selectedSupport (source (successorIndex i))
```

`R` retains the robust large-radius parent (minimality, no `IsM44`, the large
physical cap, and the physical radius), the full first-apex frontier, the
origin-tagged two-source continuation, the anchored omission cycle, and the
same concrete `CriticalShellSystem` throughout.

The previous checked scratch DAG already proves on this arm:

1. the period is `3..5`;
2. every successor actual blocker is a distinct strict-physical-cap vertex;
3. its exact critical shell meets the physical cap in precisely the adjacent
   source pair;
4. the two remaining shell points lie outside that cap;
5. the outside pairs are injective, and equality of two such pairs is already
   an immediate contradiction; and
6. the adjacent cap sources strictly straddle their actual blocker.

Thus a repeated outside pair is not a weaker missing producer and is not
proposed here.

## Fixed-anchor chord/bisector route: negative

For each cycle edge, both the actual blocker and `S.oppApex2` lie on the
perpendicular bisector of the adjacent source chord.  The actual blocker lies
strictly between the two sources in cap order, while all sources lie on the
same exact physical-apex circle.

This does **not** contradict strict convexity, MEC geometry, or cap order.
The existing kernel-checked exact rational regression

```text
scratch/atail-force/strict-oppcap-cross-classifier/
  directed-cycle-geometry/DirectedCycleCountermodel.lean
```

realizes exactly the period-three pattern.  Its boundary order is

```text
o, left, q0, c0, c2, q1, c1, q2, right.
```

It proves:

- `q0,q1,q2` and both cap endpoints have one common radius from the fixed
  apex `o`;
- `c0` bisects `q0,q1`, `c1` bisects `q1,q2`, and `c2` bisects `q2,q0`;
- the three blocker centers are distinct strict-cap vertices and each lies
  between its chord endpoints in the relevant cap interval;
- the nine-point carrier is convex independent;
- the displayed Moser triangle is non-obtuse; and
- the displayed circumdisk contains the carrier.

Every printed theorem in that regression has kernel axiom closure exactly
`propext`, `Classical.choice`, and `Quot.sound`.

This is not a global `CounterexampleData`: it omits all-center K4,
deletion-criticality, and the total critical map.  It is nevertheless a
decisive regression against any proof using only the fixed physical anchor,
the co-radial source cycle, cap order, and the three in-cap bisectors.

`PG136` / Lemma 184 does not eliminate the model.  Its two nesting laws assume
restricted coarse orders in which the two moving centers lie before, or on
opposite sides of, the complete source triple.  The exact model interleaves
the centers and sources, so neither restricted order is forced.  Lemma 26's
A/B classification also needs a selected two-cross quartet and does not add
the missing global row incidence.

## Full-row extension search: unresolved, not refuted

A bounded source/ledger search found **no checked extension** of that exact
period-three Euclidean core in which every blocker circle also carries its
two required outside-cap targets.  In particular, no located artifact puts
all three full exact four-target blocker rows, the fixed-apex source cycle,
and the retained parent data on one Euclidean convex/MEC carrier.  Therefore
the fixed-anchor regression does **not** refute a theorem that uses the full
critical rows.

The closest checked regressions keep the two required layers separate:

1. `DirectedCycleCountermodel.lean` has the exact Euclidean convex/MEC
   fixed-apex period-three bisector cycle, but not the two outside targets in
   each blocker row.  It also lacks global K4, the total source-indexed
   critical map, deletion-criticality, `F.minimal`, `F.noM44`, the retained
   first-apex frontier, and the origin-tagged continuation.
2. `paired-common-deletion-nonreturn-audit/exact_five_paired_cycle_model.py`
   has an exact 24-vertex finite metric with global K4, a total
   deletion-critical blocker map, exact full rows, and a genuine paired
   return cycle.  It is non-Euclidean, uses a two-cycle rather than this
   period-three cap cycle, and does not instantiate `CounterexampleData`,
   `ConvexIndep`, MEC/cap geometry, or the full parent.
3. Proposition 198 in `/opt/nfs/verified-results.md` has an exact rational
   convex realization of eight full four-target rows, including its endpoint
   groups, fixed outsider, and pair-uniqueness conditions.  It does not have
   the fixed physical apex/exact-five source class, this adjacent-source
   cycle, the MEC/cap packet, a total common critical map, deletion
   minimality/no-M44, or the first-apex frontier.
4. The strengthened `critical-map-incidence` card-12 shadow has a total
   critical map, exact full rows, deletion criticality, cap profiles, and
   cyclic-order constraints.  It is only a finite incidence shadow: it lacks
   shared Euclidean coordinates, actual `ConvexIndep`, MEC/Moser geometry,
   the full cap predicates, and the complete parent fields.

These artifacts justify only the following scoped verdict:

```text
fixed-apex Euclidean cap/bisector data alone       permits the cycle;
full-row/global-critical finite abstractions alone permit return;
their conjunction on the retained physical parent remains untested.
```

The precise aggregate target is therefore still direct `False` from the full
parent and all-reverse hypothesis.  The following conditional contract records
one exact way an aggregate proof could meet a current sink:

```lean
theorem exists_reverseEdgeU5AdjacentTerminal_of_allReverse
    (R : FrontierCoupledExactFivePhysicalCycleParentResidual H F)
    (hreverse : forall i,
      source i in selectedSupport (source (successorIndex i))) :
    Nonempty (ReverseEdgeU5AdjacentTerminal R)
```

It must not be read as evidence that this positive packet is easier to force
than the contradiction itself.  A proof must couple
the actual outside targets in the source-indexed critical rows to a dangerous
triple/adjacency (or another existing terminal sink).  That is the smallest
currently precise aggregate cap/critical-map/U5 lemma; no checked combined
countermodel was found, so this lane does not label it false or blocked.

## Shell-curvature reconciliation

`exact-five-reverse-outside-pair-producer` already constructs the exact
straddle packet needed to put the two outside residuals in the middle roles of
one cyclic shell window.  `reverse-shell-curvature-adapter` then constructs
those roles, but its current audit detects that the production
`CounterclockwiseAngleChart` demands strict triple orientation even across an
identified period endpoint.  The present production source still has that
unrestricted field, so no nonvacuous chart application is available yet.

Even after the separate chart repair, a single-row curvature budget is not a
consumer.  The fixed-anchor Euclidean regression above already shows that
the cap chord/bisector part of three rows can coexist.  A multirow curvature
argument would therefore need a new checked overlap-multiplicity or
interlacing theorem that visibly uses the actual critical map; summing the
existing marginal budgets is insufficient.

## Smallest current distinct bank-facing terminal contract

For one edge `i`, all reverse membership already constructs two q-critical
rows for the same deleted source `q = source i`:

```text
own row center       = H.centerAt (source i)
successor row center = H.centerAt (source (successorIndex i)).
```

The source draft `AllReverseDirectFalse.lean` defines

```lean
ReverseEdgeU5AdjacentTerminal R
```

with exactly the remaining fields required by the imported adjacent-center
U5 sink:

1. one dangerous triple for deleted source `q`;
2. both actual blocker centers are members of that triple;
3. the dangerous center lies in both q-erased actual supports; and
4. one blocker center lies in the other blocker's q-erased support.

The draft theorem

```lean
false_of_allReverse_of_reverseEdgeU5Adjacent
```

then obtains the two canonical q-critical classes from
`nonempty_reverseMembershipQCriticalPair` and calls
`U5QCriticalTripleClass.two_triple_centers_adjacent_incompatibility`
directly.  It retains the full parent `R`; it does not replace the actual rows
or invent a repeated outside pair.

This is the smallest current **distinct** named-bank completion found.  It is
not nominated as a producer theorem: the global implication

```text
full parent R + all reverse membership
  -> Nonempty (ReverseEdgeU5AdjacentTerminal R)
```

is open and may be as strong as the direct branch contradiction.  The
cap-local regression proves that any proof of it must visibly
use all-center K4/deletion-criticality or another global critical-map return,
not only the co-radial chord cycle, cap order, or shell-curvature marginals.

## Validation and closure status

- Required theorem-bank/index preflight: **done**.
- Existing exact Euclidean fixed-anchor regression: **re-audited; decisive
  for the local route**.
- `AllReverseDirectFalse.lean`: **source draft, no `sorry`/`admit`; not
  elaborated because the active build gate forbids Lean/Lake builds**.
- Direct `False` from `R + hreverse`: **open**.
- Production `sorry` closed by this lane: **none**.
