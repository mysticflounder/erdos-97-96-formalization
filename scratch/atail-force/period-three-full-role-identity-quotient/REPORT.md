# Period-three full-role / identity quotient

Date: 2026-07-17

Status: **THE SOURCE-FAITHFUL QUOTIENT IS FAR LARGER THAN THE OLD SIX-CELL
MODEL, AND A FULLY DISJOINT LEGAL CELL IS EXACT SAT UNDER THE COMPLETE LINEAR
SHARED-ORDER/KALMANSON SYSTEM.  THE OLD 10,368-CELL UNSAT IS AN UNSUPPORTED
IDENTITY/ORDER ARTIFACT.  PERIOD-THREE `P + K + ALL-REVERSE` DOES NOT CLOSE BY
LINEAR KALMANSON FORCE ALONE.  NO PRODUCTION `sorry` IS CLOSED.**

This lane owns only
`scratch/atail-force/period-three-full-role-identity-quotient/`.  It did not
edit production Lean, shared docs, proof-blueprint state, git, protected
lanes, `ShellCurvature`, or `SurplusM44`, and it ran no Lean/Lake command.

## Required preflight

Before constructing the quotient, the audit checked every registry required
by `AGENTS.md`:

- `docs/general-n-certificate-bank-mining-2026-07-09.md`, including the
  sibling `p97-rvol` section;
- `certificates/p97_rvol_general_n_mining.{md,json}`;
- `certificates/erdos97_legacy_general_n_mining.{md,json}`; and
- `certificates/erdos_general_theorem_p97_mining.{md,json}`.

Focused `nthdegree docs search --lean` queries covered period-three reverse
rows, shared strict boundary/Kalmanson order, first-apex retained and
double-deletion rows, exact-five critical shells, and the existing two-large-
cap obstruction.  No bank contains the required aggregate consumer or a
source-to-cell identity quotient.  The indexed
`u1TwoLargeCapObstruction` remains a consumer of a much stronger named metric
packet and does not supply this quotient.

The latest prose backup at
`/opt/nfs/front-a-closure-backup-20260718T025149Z/verified-results.md` was also
checked as discovery input:

- Lemma 202 / Corollary 203 are conditional MEC-wedge sinks after a particular
  shared-target handoff has been produced;
- Lemma 223 forces owner/target alternation whenever two centers own one pair;
- Lemma 230 localizes the second owner inside a same-block target interval;
  and
- Lemma 237 assumes a family of exact transverse A rows and is not sourceable
  from selected T0/T1 rows.

Only Lemmas 223/230 constrain every physical-apex/reverse-blocker shared
cycle-source pair.  The SAT probe below includes that constraint.  None of
these prose results is treated as already imported Lean.

## 1. Executable staged schema

`enumerate_schema.py` implements the source-audit checklist as separate
layers.  It deliberately does not collapse the independent retained frontier
and physical exact-five frontier.

### First-apex rows

The exact labelled counts are:

| arm | surface | exact labelled count |
| --- | --- | ---: |
| equal radius | `q,w` plus all T1 role words | 31 |
| equal radius | T0/T1 overlap 0, including two fresh completions | 57 |
| equal radius | T0/T1 overlap 1, including one fresh completion | 172 |
| equal radius | T0/T1 overlap 2 | 186 |
| equal radius | all completion cells | **415** |
| unequal radius | every labelled T0/T1 completion-role cell | **273** |

The equal-radius cells carry only the ambient lower bound

```text
8 - |T0 completion intersection T1|.
```

They never assert that the ambient radius class is exhausted.  In particular,
overlap two still permits arbitrary additional same-radius carrier points.

For scale only, forgetting permutations of genuinely unnamed completion
slots would reduce the equal cells from 415 to 402 and the unequal cells from
273 to 32.  Those smaller numbers are **candidate** quotients, not coverage
claims; no source transport theorem currently licenses the slot permutations
after cross-front identities and boundary positions are attached.

### Physical exact-five and blockers

There are seven labelled physical profiles:

```text
3 strict: 2
4 strict: 4
5 strict: 1
```

For blocker `b_i`, current source only excludes its two incident cycle
sources.  The schema therefore allows it to equal the nonincident source
`c_(i+2)`, any strict noncycle exact-five point, or a fresh strict physical
point, subject to blocker injectivity.  Summed over the seven profiles this
gives 140 labelled blocker/exact-five matchings:

```text
physical strict count 3: 16
physical strict count 4: 80
physical strict count 5: 44
```

### Cross-front identities

After each front's internal equalities are fixed, its actual points are
distinct.  Role-preserving equalities between the two fronts are therefore
exactly partial injective matchings.  Without using any slot symmetry, the
schema counts:

```text
equal-radius cross-front skeletons:   1,146,000
unequal-radius cross-front skeletons: 3,303,304
both boundary orientations:          8,898,608
```

This is before reverse-target matching or within-cap boundary orders.  It is
already enough to show that the old `6 * 1,728 = 10,368` decision was nowhere
near source coverage.

These counts are exact inside the audited exclusive `I/S/O` role
normalization.  They are not claimed as a source-complete total because the
source-to-role extractor must still classify distinguished cap endpoints
before cross-role endpoint aliases can be quotiented.

### Reverse targets and carrier gaps

The six outside-target slots have exactly the previously audited 53 legal
equality partitions:

```text
3 actual targets:  8
4 actual targets: 32
5 actual targets: 12
6 actual targets:  1
```

Leaving a partition block unmatched represents a genuinely fresh target, so
there is no fixed six-point target domain.  Extra carrier vertices are never
named: they occupy arbitrary gaps between named vertices.  Both boundary
orientations remain explicit until a reflection-transport theorem is proved.

The full cross-target/order product was intentionally not materialized after
the SAT stop in Section 3.

## 2. Source tightening from two-owner alternation

The physical apex and reverse blocker are distinct owners of each adjacent
cycle-source pair.  Lemma 223 therefore makes their owner chord alternate
with the source chord, and Lemma 230 places the reverse blocker strictly
between the sources in the physical open block.  A predecessor order which
put the blocker on the complementary interval was UNSAT from just the
physical and one reverse row; its group core was:

```text
use__physical, use__reverse2
```

After correcting the block order to place all three blockers in their
Lemma-230 intervals, the fully disjoint cell is SAT.

The round-13 prose handoff also uses its Lemma 11 to prove that this reverse
fiber omits the opposite physical apex: a circle centered inside a cap and
through two cap points cannot also pass through the opposite MEC vertex.
Current Lean search found the ordered-cap one-sided injectivity primitives in
`CapSelectedRowCounting.lean`, but no imported theorem with this interior-
center/opposite-vertex statement.  The missing Lean bridge is therefore a
theorem of the following mathematical shape:

```text
center in strict interior of cap
two distinct same-radius support points in that cap
---------------------------------------------------
opposite MEC vertex not in the same radius support.
```

Specializing it to `successor_actualBlocker_mem_physicalCapInterior` and the
two adjacent cycle sources would remove the physical apex from every
`reverseOutsidePair`.  The SAT cell already chooses six fresh private
targets, and its exact-support constraints explicitly require the physical
apex distance to differ from every reverse radius.  Thus the ledger **does
use/satisfy the Round-13 apex exclusion**; the missing Lean bridge concerns
source-wide quotient pruning, not validity of this fixed cell.

## 3. Decisive SAT stop

`probe_fully_disjoint.py` uses 25 named points in one legal unequal-radius
cell:

- T0 has role profile `3I + S`;
- T1 has `3I + O` and is disjoint from T0;
- the independent physical exact-five class has three strict members and one
  hit in each adjacent cap;
- all three blockers are fresh and distinct from the exact-five points;
- all six reverse targets are fresh, with three disjoint outside pairs; and
- every T0/T1 point is distinct from every physical/reverse role.

The exact QF_LRA formula includes:

- positivity and every strict triangle inequality;
- both strict Kalmanson inequalities for every cyclic quadruple in the common
  boundary word;
- all T0/T1 equalities and unequal radii;
- all physical exact-five equalities and disequalities excluding every other
  named point;
- the required second-apex `q/w` distance inequality;
- all three exact reverse-row equalities and disequalities excluding every
  other named point (including the physical apex); and
- the Lemma-223/230 blocker/source alternation.

Replay:

```bash
UV_CACHE_DIR=/tmp/p97-uv-cache uv run python \
  scratch/atail-force/period-three-full-role-identity-quotient/\
  probe_fully_disjoint.py
```

Result:

```text
DECISION: EXACT_PROJECTED_SAT_STOP
T0 radius = 323
T1 radius = 314
physical radius = 205
reverse radii = 221, 199, 221
dist(second,q) = 206
dist(second,w) = 209
minimum triangle margin = 1
minimum Kalmanson margin = 1
```

The script independently substitutes the rational model into every encoded
triangle, Kalmanson, and row equality.  This is an exact verified rational
ledger for the QF_LRA formula, not sampling and not `unknown`.

It is **not** a planar Euclidean realization, an MEC/cap-segment realization,
a total CSS/minimality extension, or a live counterexample.  Its decisive
scope is narrower and sufficient for routing:

> the shared convex order, all strict linear Kalmanson inequalities, and the
> named T0/T1/physical/reverse row equalities do not imply period-three
> `False`.

## 4. Consequence for the closure route

The proposed

```text
false_of_periodThree_allReverse_firstApexRows
```

cannot be proved from its current linear shared-order abstraction alone.
Enumerating the remaining millions of identity/order cells or extracting
Farkas cores is not load-bearing after one legal projected SAT cell.

The **first missing live constraint** beyond this ledger is not another role
or order case.  It is simultaneous planar Euclidean realization: the abstract
distance variables must arise from one family of points in `EuclideanSpace
ℝ (Fin 2)`.  Equivalently, their squared-distance matrix must have Euclidean
rank at most two and the chosen cyclic word must be realized by one strictly
convex embedding.  The ledger enforces every linear consequence currently in
the probe, but not this rank/coordinate condition.

The next ingredient must reject the fully disjoint cell using information
absent from QF_LRA.  The honest escalation order is:

1. planar Euclidean realizability of this fixed 25-role cell;
2. MEC/cap-segment and nonobtuse support-triangle constraints, including the
   formal Lemma-11 opposite-apex omission;
3. extension of the three named reverse rows to the total critical-shell map
   with source-by-source deletion failure; and
4. the parent minimality / `noM44` fields.

The next bounded probe should therefore keep this **one fixed 25-role cell**
and introduce coordinate variables, rather than enumerate more quotient
breadth:

```text
normalize FIRST = (0,0), SECOND = (1,0);
one (x,y) pair for each of the other 23 named vertices;
all row equalities as squared-distance polynomial equations;
all exact-shell exclusions and the two radius inequalities as sign-split
  strict polynomial branches;
every named point strictly on the interior side of every oriented boundary
  edge (the fixed word is one strictly convex polygon);
MEC follow-up only after the coordinate-only verdict:
  one disk center/radius, the three distinguished vertices on its boundary,
  every named vertex in the disk, and the three nonobtuse dot products.
```

Run the coordinate-only QF_NRA cell with a hard 120-second budget in Z3 and
cvc5.  A SAT hit must be substitution-checked in exact/high-precision
coordinates; an UNSAT or `unknown` is not banked without an independently
checkable certificate.  If coordinate-only SAT, add the displayed MEC block
to the same cell.  This probe decides genuine progress because SAT moves the
frontier past Euclidean or MEC geometry, while certified UNSAT yields a
cell-level nonlinear obstruction.  It does not spend time on the already
refuted linear quotient breadth.

The exact symmetry theorem which would be required before any later complete
enumeration is a source-to-cell transport proving invariance under:

- permutations of unnamed T0/T1 completion slots;
- swaps inside each unordered reverse pair;
- relabeling fresh private points within each cap block;
- arbitrary unnamed carrier gaps; and
- reflection of the complete labelled formula.

Without that theorem, canonicalizing these actions would repeat the fixed-
cell coverage bug.  Because the current abstraction is already SAT, building
this transport is not the immediate proof-closing task.

## Epistemic ledger

- **EXACT COMBINATORIAL COUNTS:** all counts printed by
  `enumerate_schema.py`, within the stated role normalization.
- **EXACT PROJECTED SAT:** one fully disjoint 25-point QF_LRA cell, with an
  independently checked rational ledger.
- **PROSE-PROVEN / NOT LEAN-MAPPED:** Lemmas 202, 223, 230 and the round-13
  Lemma-11 handoff from the `/opt/nfs` backup.
- **NOT CLAIMED:** complete source quotient, Euclidean or MEC realization,
  total CSS/minimality extension, direct `False`, or production closure.
- **PRODUCTION `sorry` CLOSED:** none.
