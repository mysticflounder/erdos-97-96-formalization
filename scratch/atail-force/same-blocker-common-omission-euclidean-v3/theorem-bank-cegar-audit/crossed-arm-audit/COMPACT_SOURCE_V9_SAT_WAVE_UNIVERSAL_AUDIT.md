# Compact source-arm v9 SAT wave: universal theorem-bank audit

## Verdict

**Terminal classification: finite-Boolean SAT fixed point; no new sound universal constraint identified.**

The completed `source-at-common` compact-v9 run has a replayed SAT witness after all 1,197 connected candidates. It contains 9,488 structural constraints and 205,225 theorem constraints, including all 21,203 fresh encoded theorem keys and all 403 fresh structural keys. The final witness activates no registered theorem-family match and no structural violation. The fresh wave therefore exhausts the registered finite encodings for this run; it does not exhibit a recurring antecedent from which a further universal theorem can presently be stated.

The theorem-bank audit found genuine existing U5 and local crossed-arm consumers, but no theorem whose hypotheses are supplied by this survivor. In every promising route the first missing bridge is a positive semantic producer/classifier from the crossed-row/five-center data to a typed incidence or metric packet. Adding another conditional contradiction theorem would only duplicate the current open frontier.

This is a source-only audit. No Lean, Lake, proof-blueprint, solver, generator, or kernel/axiom gate was run.

## Exact run evidence

Primary artifacts:

- `scratch/atail-force/same-blocker-common-omission-euclidean-v3/theorem-bank-cegar-audit/crossed-arm-audit/source-at-common-compact-v9-canonical-resume-1800s-critical-k4-compact-pb/result.json`
- `scratch/atail-force/same-blocker-common-omission-euclidean-v3/theorem-bank-cegar-audit/crossed-arm-audit/source-at-common-compact-v9-canonical-resume-1800s-critical-k4-compact-pb/witness.json`
- `scratch/atail-force/same-blocker-common-omission-euclidean-v3/theorem-bank-cegar-audit/crossed-arm-audit/source-at-common-compact-v9-canonical-resume-1800s-critical-k4-compact-pb/iterations.jsonl`
- resume baseline: `scratch/atail-force/same-blocker-common-omission-euclidean-v3/theorem-bank-cegar-audit/crossed-arm-audit/source-at-common-compact-source-semantic-replay-one-candidate-300s-critical-k4-compact-pb/result.json`

The exact recorded facts are:

- schema `n17-crossed-outside-pair-full-metric-theorem-bank-cegar-v9`, tag `compact-v9-canonical-resume-1800s`, arm `source-at-common`, `n = 17`, cap profile `(6,8,6)`;
- status `SAT`, 1,197 candidates, all connected, with 1,196 SAT cut iterations followed by the SAT `surviving-candidate` record at iteration 1,196;
- `semantic_replay = PASS`;
- theorem constraints increased from 184,022 at resume to 205,225, hence 21,203 fresh encoded keys;
- structural constraints increased from 9,085 to 9,488, hence 403 fresh keys, all in `raw_btw_sep`;
- the final structural totals are 8,379 `raw_btw_sep` and 1,109 `mutual_bisector_transport` constraints;
- all registered selected-row/raw-equality theorem families and all raw cyclic between/separation cuts are marked encoded;
- the undirected-edge equality closure is recorded as complete, with 136 edges and 2,040 biconditionals, referring to `Problem97.Census554.EqualityCore.EdgeClosure.sound`;
- `forced_shared_fourth = false`; collision deletion and the crossed omissions remain active.

Fresh encoded theorem keys by family:

| Family | Fresh encoded |
|---|---:|
| `convex_five_point_core` | 2,721 |
| `first` | 4,193 |
| `five_circle_isosceles_two_selected_rows` | 882 |
| `five_direct_three_row_equalities` | 831 |
| `five_kalmanson_three_shell_equalities` | 485 |
| `five_second_three_row_equalities` | 774 |
| `four_endpoint_centers_bisect_middle_pair` | 91 |
| `four_middle_centers_bisect_endpoint_pair` | 94 |
| `kalmanson_012_124_314` | 506 |
| `kalmanson_012_325_415` | 1,687 |
| `kalmanson_013_235_415` | 1,300 |
| `kalmanson_013_412_523` | 1,103 |
| `second` | 3,714 |
| `six_k2_three_row_triangle` | 796 |
| `six_main_row_equalities` | 69 |
| `six_mirror_interleaving_equalities` | 104 |
| `six_sparse_row_equalities` | 1,002 |
| `six_two_triple_row_equalities` | 851 |

There were no fresh encoded keys for `five_kalmanson_three_selected_rows`, `global_kalmanson_14role_16eq`, `reversed_second`, or `six_sparse_two_selected_rows`. This is explained by 4,193 exact-orbit suppressions for `reversed_second` and 884 subsumption suppressions (`five_kalmanson_three_selected_rows`: 246; `six_sparse_two_selected_rows`: 638), not by a new mathematical route. There were no retirements.

The replay also records zero active matches for every registered theorem family and zero active structural violations. Its separate checks pass for the crossed arm, cap position, cap-crossing/Kalmanson conditions, shared-pair separation, and geometric-incidence projections.

## Fixed survivor

The surviving role assignment is:

- crossed pair `P = (p1,p2)`, `Pρ = (r1,r2)`;
- `Q.source = t1`, `Q.other = t3`;
- blockers `bp = r2`, `bq = p1`, `br = q2`;
- common fourth `s4`, opposite fourth `t4`;
- cyclic order `a1,t3,t2,t1,t4,a2,p2,r2,q1,p1,q2,r1,a3,s4,s1,s3,s2`.

Its principal packets include

- `p_support = {t1,t3,p1,p2}`;
- `q_common_support = {t1,q1,q2,s4}`;
- first-apex classes `{t4,p1,p2,s3}`, `{a2,q1,q2,a3}`, and `{t2,r1,r2,s1}`;
- blocker equalities `blocker(p1) = blocker(p2) = blocker(t1) = blocker(t3) = r2`.

After deleting `r2`, the five selected rows recorded by the residual are

| Center | Selected row |
|---|---|
| `a1` | `{t4,p1,p2,s3}` |
| `a2` | `{a1,t4,a3,s2}` |
| `a3` | `{a1,a2,p1,r1}` |
| `p1` | `{t1,q1,q2,s4}` |
| `r2` | `{t1,t3,p1,p2}` |

All five avoid the deleted point. These are exact facts about one finite Boolean model. They are not Euclidean coordinates, a `CounterexampleData` term, a U5 class construction, or a universal nonrealizability proof.

## Route classification

| Plausible route | Classification | Audit result |
|---|---|---|
| The 18 fresh encoded theorem families and fresh shared-pair separation cuts | **Existing theorem** | Every fresh key is another instance of an already registered local family or structural rule. The fixed point violates none of them, so this wave supplies no new family or closure. Orbit/subsumption suppressions are encoding deduplication only. |
| Local crossed-arm blocker locking and `sharedFourth_or_crossBlockers_avoid_namedRows` | **Existing theorem** | These source-level lemmas already reach the avoid-named-row residual. Their latter branch is explicitly the finite-saturation residual still lacking a positive producer; the witness remains in that frontier. |
| `Problem97.U5QCriticalTripleClass.qcritical_exact_qcritical_chain_incompatibility` | **Existing theorem** | This is the strongest bank match (`certificates/p97_rvol_general_n_mining.json`, source `lean/RVOL/P97/U5GlobalIncidenceSupport.lean:194`). It requires a `U5DangerousTriple`, three typed q-critical/deleted-K4 classes, point membership in `T`, distinctness, and a particular class-membership chain. The compact model supplies none of those typed antecedents. |
| `Problem97.u5_common_bisector_triple_incompatibility` and exact-selected U5 metric incompatibilities | **Existing theorem** | These are genuine source-proved, archive-reachable U5 consumers, but they require `CounterexampleData`, noncollinearity, explicit distance equalities/radii, and constructed `U5QDeletedK4Class` or q-critical data. The generic row keys do not imply those hypotheses. The bridge audit explicitly warns that the common-bisector theorem is specialized U5 data, not an arbitrary row-key schema. |
| C5D3B q-anchor crossing consumers | **Existing theorem** | Lexically close, but unusable: they require `C5D3BSameSideTerminalPacket`, `U5QAllowedAuditSupport`, bounded-center memberships, and exact selected-class decomposition/intersection hypotheses. They have no current local exact/name/shape match, and the registry audit marks this source surface non-portable because of duplicate declarations/stale oleans. |
| Generated U1 `extra_omit3` consumers | **Existing theorem** | Their omission is a fixed record/slot condition requiring `U3FixedTriplePacket`, `RowSlotLabelPacket`, and explicit row equalities; it is not the survivor's common blocker omission. |
| Census554 edge closure and cyclic-separation infrastructure | **Existing theorem** | These are sound local consumers/checking infrastructure, not a witness finder, finite-cover theorem, or producer of a concrete forbidden row pattern. The registry expressly records no small complete motif alphabet or coverage claim. |
| Exact role-labelled five-center survivor, or a coordinate/QF_NRA obstruction for it | **Fixed-witness-only** | It is a useful exact finite specification for a later realizability experiment, but no Euclidean coordinates or nonrealizability proof were produced here. A result for these 17 named roles would not by itself be universal. |
| A classifier from the live crossed-row/five-center residual to one named U5 incidence/metric packet | **Universalizable, but not presently established** | This is the correct kind of bridge. The run does not identify which U5 consumer must result, and zero final bank matches means no particular disjunct has experimental support from this encoding. Stating only that some contradiction packet exists would repackage the current open theorem rather than narrow it. |
| A new 14-role Kalmanson, six-point, common-bisector, or exact-selected-row cut inferred from the final survivor | **No candidate** | The corresponding registered match counts are zero at the fixed point; `global_kalmanson_14role_16eq` has no fresh key; and the compact Boolean artifact lacks the Euclidean antecedents needed for the metric variants. |
| Legacy `erdos/97` and `erdos-general-theorem/97` banks | **No candidate** | Their censuses contain no ambient-`n` candidates and no relevant crossed/selected-four/Kalmanson/bisector/q-critical/five-center declaration match. Generic `three_common_equidistant_centers_eq`, Radon, old Msolve/Cramer, and S0236 consumers all lack a current incidence, metric-shadow, coordinate-cell, or census-realization producer. |

The existing live load-bearing theorem
`false_of_capSource_firstFiber_crossedThreeRowExactSupports`
already names the universal crossed-row plus five-center residual and remains an explicit `sorry` obligation in `lean/Erdos9796Proof/P97/FrontierLiveClosure.lean`. A conditional restatement of its conclusion, or a package assuming an unspecified bank obstruction, is bookkeeping rather than progress.

## Required theorem-bank pass

All mandated registries were checked:

- `docs/general-n-certificate-bank-mining-2026-07-09.md`;
- `certificates/p97_rvol_general_n_mining.md` and `certificates/p97_rvol_general_n_mining.json`;
- `certificates/erdos97_legacy_general_n_mining.md` and `certificates/erdos97_legacy_general_n_mining.json`;
- `certificates/erdos_general_theorem_p97_mining.md` and `certificates/erdos_general_theorem_p97_mining.json`.

The sibling p97-rvol census contains the substantial reusable U5 bank (96 class-level incidence incompatibility families, supported by 88 metric-point and 112 scalar-algebra kernels), but its own conclusion is that it lacks a theorem classifying an arbitrary live packet into one of those incompatible families. The legacy censuses are bounded local geometry inventories rather than ambient-`n` producer banks. Thus none changes the producer boundary for this survivor.

Exactly one broad agentic Lean-corpus search was run:

> arbitrary finite strictly convex point configuration with five-center collision-deletion exact selected rows, crossed complementary omissions at common and opposite blocker rows, compact no-selected-K4 after source deletion, mutual-bisector transport and shared-pair separation forces a forbidden five- or six-point Kalmanson or equal-distance theorem-bank pattern, or directly yields contradiction

The search synthesis proposed U5 exact-selected, common-bisector, blocker-locking, and six-point incompatibilities. Direct registry/source comparison downgrades that suggestion: those declarations are existing consumers, but their typed U5, noncollinearity, distance-equality, or class-membership antecedents are not constructed by the compact witness. In particular, the bridge audit forbids treating `u5_common_bisector_triple_incompatibility` as a generic row-schema theorem. The search therefore found analogy, not an applicable closure theorem.

## Next universal constraint

**None.**

There is no single additional universal clause justified by this SAT wave. The most exact next proof target is not a new constraint but the missing positive producer already exposed by the live frontier: from the exact hypotheses of `false_of_capSource_firstFiber_crossedThreeRowExactSupports`, handle the avoid-named-row blocker residual by constructing the full antecedent of one explicitly named U5/local incompatibility (or prove the two fourth points coincide). V9 neither selects a particular U5 consumer nor supplies its antecedents, so naming one now would be conjectural and would not be an auditable narrowing.

## Trust boundary

- **Empirically verified by the recorded replay:** satisfiability of this finite Boolean encoding, exact key/count accounting, and the listed semantic projection checks.
- **Source-audited:** declarations and registry metadata cited above, including local/archive reachability and textual proof status.
- **Not verified in this audit:** current Lean elaboration, transitive import reachability from a publish target, kernel acceptance, transitive axioms, absence of `sorryAx`, or compatibility with current oleans.
- Registry `source-proved` means no textual `sorry`/`admit`; it is not a kernel-closure verdict. `source_reachable` is reachability within the scanned archive root, not necessarily from the current publish target. The master census freshly kernel-checked one representative U5 family member, not every exact declaration used in this comparison.
- The run establishes neither a universal-`n` theorem nor UNSAT, Euclidean realizability/nonrealizability, full `CounterexampleData`/`noM44`, MEC/nonobtuse geometry, or coverage by unregistered production metric theorems. Compact `PbLe` is an encoding of no-selected-K4 constraints, not an extra mathematical antecedent.

## Post-audit metric-core addendum

This addendum records a later result that was not available for the SAT-wave audit above. It does not retroactively turn the finite Boolean SAT run into closure. It adds a new, externally established equality obstruction extracted from that fixed witness and changes the best next theorem candidate.

### Exact algebraic core

Artifact:

- `scratch/atail-force/same-blocker-common-omission-euclidean-v3/theorem-bank-cegar-audit/crossed-arm-qf-nra-v4/metric-core-miner-v9/result.json`, key `algebraic_unit_core`;
- associated scripts recorded by the artifact: `algebraic-minimal-core.sing` and `algebraic-minimal-core.smt2` in the same directory.

The core uses the seven points

```text
a1, p2, r2, s3, t1, t2, t4
```

and exactly these eleven squared-distance equalities:

```text
d2(r2,t4) = d2(s3,t4)
d2(r2,t4) = d2(t1,t4)
d2(a1,p2) = d2(a1,s3)
d2(a1,p2) = d2(a1,t4)
d2(a1,p2) = d2(s3,t1)
d2(a1,p2) = d2(s3,t2)
d2(a1,r2) = d2(a1,t2)
d2(p2,t2) = d2(p2,t4)
d2(p2,t2) = d2(r2,t2)
d2(p2,r2) = d2(r2,t1)
d2(p2,t1) = d2(t1,t2)
```

Their undirected-edge equality classes have sizes `5,3,3,2,2,2`. The recorded gauge is `(a1,p2)`, leaving ten polynomial variables and eleven polynomials. The atom hash is `7cd4de6875b046a9860d57731f393edd1c6d90b2c4e07813deebf913f0c35061`.

The recorded exact-algebra status is:

- Singular over characteristic zero: `UNIT`, return code 0;
- msolve corroboration: `UNIT` in both forward and reverse generator orders;
- Z3: `UNKNOWN` by timeout;
- cvc5: `UNKNOWN` by timeout;
- production equality-signature matches: empty.

The Singular result says that after using `a1 != p2` to normalize those two anchors, the equality ideal has no complex point and hence no real planar realization. Z3/cvc5 `UNKNOWN` neither weakens nor independently confirms that algebraic conclusion. The numerical Jacobian rank is heuristic only.

The minimization claim is deliberately limited. Greedy deletion of each induced point returned `NONUNIT`, but the one-pass equality-deletion phase retained four equalities because their deletion checks timed out; only seven equality deletions returned `NONUNIT`. Thus the artifact establishes a seven-point/eleven-equality unit core, not an unconditional inclusion-minimal core.

Provenance remains the fixed `source-at-common` witness audited above: the artifact records upstream result hash `2258a038625ff149d498b39b9683646abb0793bcc21ce0c11531ee3a1f0a437a`, witness hash `48a35a28c42d23c976d5d5a748f7768c00660c068b145911e7174371ac645bb8`, global-closure hash `2bd17f326f94c278f0795583acea56d581702dda78500bb2fdb99b2044901cb4`, and semantic replay `PASS`.

### Theorem-bank comparison

The seven mandatory registries listed in the main audit were rechecked against this exact equality graph. None contains an exact seven-point/eleven-equality declaration or normalized signature match. The closest indexed sibling theorem remains

```text
Problem97.U1LargeCapRouteBTailMetricResidualTarget.u1TwoLargeCapObstruction
```

at `lean/RVOL/P97/U1TwoLargeCapObstruction.lean:77`, catalogued in `certificates/p97_rvol_general_n_mining.md` and `certificates/p97_rvol_general_n_mining.json`. It is a five-point, seven-distance-equality obstruction, so it cannot consume this core merely by renaming its labels.

The current local Census554 sources also contain seven-point collision theorems that are closer in arity but not in equality closure:

- `seven_point_five_circle_collision`;
- `seven_point_six_circle_collision` and `seven_point_six_circle_collision_b`;
- `seven_point_twin_four_circle_collision`;
- `seven_point_orbit_collision`;
- `seven_point_circle_network_collision`;
- `seven_point_hinge_closed_tail_collision`.

The production matcher explicitly checked its registered equality-only closure signatures and returned no match. Independent source comparison agrees: the eleven-equality local families have closure-class size multisets `7,3,3,2`, `6,4,3,2`, `6,6,2`, or `6,3,3,3`, rather than this core's `5,3,3,2,2,2`; the orbit family has thirteen hypotheses. Exactness or convex-order signatures are outside the QF_NRA artifact, so they cannot silently supply a match either.

Exactly one new agentic Lean-corpus query was run for this addendum:

> For seven distinct planar points A P R S U V W, is there an existing Lean theorem proving contradiction from exactly these eleven squared-distance equalities: d2(R,W)=d2(S,W), d2(R,W)=d2(U,W), d2(A,P)=d2(A,S), d2(A,P)=d2(A,W), d2(A,P)=d2(S,U), d2(A,P)=d2(S,V), d2(A,R)=d2(A,V), d2(P,V)=d2(P,W), d2(P,V)=d2(R,V), d2(P,R)=d2(R,U), and d2(P,U)=d2(U,V)? Search general-n Problem97 U5 incidence, metric-point, scalar-algebra, Kalmanson, six/seven-point equality obstruction, and legacy theorem banks for an exact declaration or a theorem whose hypotheses are exactly constructible from this equality graph.

The corpus answer found no exact declaration. It named `u1TwoLargeCapObstruction` and Route-B tail theorems as closest results, but direct source and registry inspection confirms that none exposes these eleven equalities as constructible hypotheses. As before, the agent-written search synthesis is discovery evidence only; the source/registry comparison controls the classification.

### Classification and exact next theorem candidate

**Classification: genuinely new universalizable equality-theorem candidate; externally algebraically verified, not Lean/kernel proved.**

The natural theorem is a collision result, not a theorem tied to these role names:

```text
For A P R S U V W : R^2,
the eleven equalities above imply A = P.
```

Equivalently, adding `A != P` yields `False`; an injective-realization adapter would obtain that inequality automatically. This is ambient-cardinality independent and uses only a seven-label equality graph, so it is a proper universal theorem shape rather than a fixed-`n = 17` statement. A proof-facing implementation would naturally have two layers:

1. a metric collision theorem for arbitrary planar points, proved from an auditable exact algebraic identity or direct Lean algebra; and
2. an `EqualityCore` structure plus `EdgeClosure.sound` adapter that recognizes the six closure classes and contradicts an injective `Realizes` map.

The occurrence discovered here is nevertheless **fixed-witness-only evidence**. The miner found this labeled subgraph inside one equality closure. Until a generic, proved matcher or a direct producer constructs the corresponding six `EdgeClosure` classes from arbitrary live crossed-row/five-center hypotheses, the new collision theorem would not close the universal route. Hard-coding these seven witness labels, or assuming the eleven equalities in a wrapper, would be bookkeeping rather than promotion.

Accordingly, the post-audit next universal constraint is now exact: formalize the seven-point collision theorem with the eleven equalities above, then prove the generic closure matcher/producer. This supersedes the earlier `None` only because the later characteristic-zero computation supplied a concrete universal equality signature; it does not supersede any theorem already present in the bank.

### Addendum trust boundary

- The `UNIT` verdict is trusted external characteristic-zero computation, corroborated by msolve, not a Lean proof or checked Nullstellensatz certificate.
- No Lean, Lake, proof-blueprint, solver, generator, or kernel/axiom command was run for this addendum; all solver facts are read from the completed artifact.
- No current elaboration, import reachability, transitive axiom status, or `sorryAx` status was established for the nearby source theorems.
- The no-match result covers the registered production equality-only signatures and the audited source/registry candidates. It is not a claim about all possible mathematical derivations from arbitrary stronger hypotheses.
- Universalizability describes the theorem's quantified shape. Current applicability remains fixed-witness-only until both the theorem and its generic matcher/producer are proved and wired to the live publish spine.
