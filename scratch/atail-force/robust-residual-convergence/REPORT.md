# Robust residual convergence audit

Date: 2026-07-17

Status: **THE CHECKED DAG HAS CONVERGED TO TWO NONRECURRENT SECOND-APEX
GEOMETRIC FRONTS, PLUS ONE INDEPENDENT RETAINED-PARENT FRONT.  GENERIC
COMMON-DELETION, SHARED-PAIR, AND FRESH-ROBUST-CENTER OUTPUTS ARE RECURRENCE
EDGES, NOT NEW RESIDUALS.  THE SMALLEST HONEST NEXT CLOSER IS A DIRECT
THEOREM ON THE FULL-PARENT EXACT-FIVE ALL-REVERSE LEAF.  COMPLETING THE
`ShellCurvature` ANGLE CHART HELPS THAT PROOF METHOD BUT DOES NOT BY ITSELF
CLOSE OR CHANGE THE TARGET.  NO PRODUCTION `sorry` IS CLOSED.**

## 2026-07-17 ownership correction

The exact-five all-reverse branch is now owned as an aggregate occurrence
proof from the complete
`L : FrontierLargeOppositeCapsBiApexRobustResidual B`, followed immediately
by an existing same-cap/Kalmanson/frontier-return consumer.  The preferred
occurrence produces one canonical `reverseOutsidePair` co-radial from
`S.oppApex1`; production `outsidePair_unique_capCenter` then closes the leaf.

The exact finite no-return models and the fully disjoint exact-QF_LRA-SAT
25-role cell remain regression gates: they refute deriving this occurrence
from bare `P/K`, fixed `T0/T1` marginals, or the linear Kalmanson quotient.
They do not refute an aggregate proof that retains both cap bounds, the
original frontier, complete critical-fiber provenance, MEC geometry,
`D.Minimal`, and `noM44`.  Consequently the occurrence is reactivated only at
the full-parent boundary; the public endpoint remains direct
`false_of_largeOppositeCapsBiApexRobust L`.  Bare-`B`, fixed-initial-`H`, and
additional conditional consumers remain retired.

This audit owns only
`scratch/atail-force/robust-residual-convergence/`.  It does not edit or
rebuild production Lean, shared documents, generated blueprint state,
`surplusM44`, protected unique/card-five files, or the user-owned
`ConvexCyclicOrder/ShellCurvature.lean` lane.

## Scope and preflight

The starting point is the already-consumed survival-cover branch:

```text
source-indexed card-14 survival cover
  -> physical-second-apex CommonDeletionTwoCenterPacket
  -> FullyDeletionRobustAt D S.oppApex2
     or protected swapped unique-four.
```

The protected unique arm is outside this audit.  On the robust arm the exact
public surface is

```lean
L : FrontierLargeOppositeCapsBiApexRobustResidual B
```

with both physical opposite caps of cardinality at least six and hence
`14 <= D.A.card`.

The audit read the current source and reports for:

- `robust-second-apex-geometry`;
- all `robust-large-radius-*` continuations;
- all `robust-two-radius-*` continuations;
- `robust-minimal-consumer-bank`;
- `first-apex-robust-global-consumer`;
- `RetainedStrictInteriorParentNormalForm` and the bi-apex terminal audit;
- the current cap-strengthened `L`/direct-geometry reports; and
- the newer production exact-five chain through
  `LargeCapUniqueFivePhysicalOmissionCycle` and
  `PhysicalOmissionCycleParentResidual`, together with both current cycle-arm
  consumer audits.

Before proposing the branch theorem below, the required theorem-bank
preflight checked:

- `docs/general-n-certificate-bank-mining-2026-07-09.md`, especially the
  sibling `p97-rvol` bank;
- `certificates/p97_rvol_general_n_mining.{md,json}`;
- `certificates/erdos97_legacy_general_n_mining.{md,json}`;
- `certificates/erdos_general_theorem_p97_mining.{md,json}`; and
- indexed Lean searches for exact-five critical shells, two disjoint robust
  radii, cap-straddling critical rows, multirow shell curvature, common
  outside pairs, strict-lap nonreturn, and the seven-equality large-cap
  obstruction.

The search recovered the current exact-five modules, the U5 named-incidence
consumers, `outsidePair_unique_capCenter`, strict-lap interfaces, and
`u1TwoLargeCapObstruction`.  It found no theorem deriving the missing
multirow collision or named cross incidence from the live residuals.

## 1. Exact current branch DAG

Legend:

- **checked** means source-clean and recorded as kernel-checked by its owning
  lane;
- **protected** means an already-owned unique branch, not a residual here;
- **loop** means projection reaches the same robust/common-deletion state
  without a decreasing invariant;
- **live residual** means a nonrecursive positive normal form with no current
  terminal.

### 1.1 Physical-second-apex classifier

The checked equivalence in `robust-second-apex-geometry` is:

```text
FullyDeletionRobustAt D S.oppApex2
  <->
  (some positive physical-apex radius class has card >= 5)
  or
  (two distinct positive physical-apex K4 radii have disjoint exact
   selected four-supports, and oppCap2.card >= 6).
```

On `L`, the cap-six conclusion is already present.  Thus the robust branch
has exactly the following two top-level mathematical arms.

### 1.2 Large physical-radius arm

For

```lean
Flarge : RobustLargeRadiusParentSurface D S
```

production `RobustLargeRadiusReduction` gives:

```text
large radius class, card >= 5
  -> CommonDeletionTwoCenterPacket (..., center, S.oppApex2)
     or
     LargeCapUniqueFiveSecondApexRadius
       + LargeCapUniqueFiveTwoCommonDeletionSources.
```

The older `SharedCriticalPairAtLargeSecondApex` constructor has been
eliminated: `5 > 4` supplies a physical-radius point outside its fresh exact
shell, and deletion of that point produces the displayed common-deletion
packet.

The two reduced constructors behave differently.

#### Generic physical common deletion

```text
CommonDeletionTwoCenterPacket (..., center, S.oppApex2)
  -> FullyDeletionRobustAt D S.oppApex2
     or protected swapped unique-four.
```

The first result is the classifier input again.  It is a **loop**.  The
second is **protected**.  Further work on this constructor is useful only if
it proves a genuine nonreturn/rank theorem while retaining its origin data.

#### Exact-five continuation

Current production is substantially stronger than the earlier
unused-critical-row or endpoint-row escape reports.  The checked chain is:

```text
LargeCapUniqueFiveSecondApexRadius
  -> unused cap source and its actual critical row
  -> that row has at most one physical strict-cap exact-five hit
  -> two distinct physical strict-cap sources omitted by that row
  -> two origin-tagged common deletions at the same actual blocker and
     physical apex
  -> canonical exact row supports and a third common-deletion source
  -> a source-exact physical actual-critical omission cycle K
     with 2 <= K.period <= 5.
```

`PhysicalOmissionCycleParentResidual` retains the large-radius parent, a
same-`H` first-apex frontier, the two-source continuation, the cycle, its
anchored ingress source, and the parent-radius equality.  This is the current
exact-five consumer surface; the older anonymous ordered-shell escape is no
longer the end of the DAG.

Production then splits every cycle exhaustively.

##### Exact-five mutual-omission arm

For one edge both directed source memberships are absent.  The checked
scratch chain is:

```text
PhysicalActualCriticalMutualOmissionEdge
  -> canonical crossed deletion packets
  -> at every actual blocker, q-deletion or w-deletion survives,
     except possibly one off-cap double-blocking center whose complete
     source fiber has card <= 2
  -> card/cap pigeonhole produces two sources of one survival color whose
     actual blockers lie in one closed cap
  -> blockers distinct:
       SameCapCommonDeletionResidual
     or blockers equal:
       EqualBlockerShellResidual.
```

This is a **live residual**.  Projecting the distinct-blocker arm to a generic
common-deletion packet re-enters the known successor machinery and loses the
same-cap/origin data.  Retained as stated, its first missing terminal input is
two named common outside targets.  The equal-blocker arm similarly needs a
second same-cap center through a named outside pair or an equivalent positive
cross incidence.

##### Exact-five all-reverse-membership arm

The checked scratch chain is:

```text
all reverse memberships
  -> period is 3, 4, or 5
  -> every source deletion is critical at two distinct actual centers
     with erased supports intersecting in at most one point
  -> every successor critical row has exact cap split 2 same-cap + 2 outside
  -> reverse-row centers are injective
  -> the two-point outside residuals are injective
  -> period <= choose(card(cap complement), 2)
  -> the two same-cap source targets straddle their blocker in strict cap
     order.
```

This is the most geometrically normalized **live residual**.  A repeated
outside pair is already immediately contradictory by
`outsidePair_unique_capCenter`; the unconditional checked conclusion is
injectivity/capacity, not repetition.  The current U5 packets still lack
their dangerous-triple alignment and named common points.

### 1.3 Two distinct physical-radius arm

The broad `robust-second-apex-minimal-deletion` trichotomy is valid but no
longer the best normal form.  The checked strict-pair reduction chooses two
strict `oppCap2` points from one physical row and gives:

```text
two disjoint physical four-rows
  -> localized equal-radius collision
     or singleton common deletion
     or unequal-radius two-source minimal core.
```

The endpoints are:

1. **Localized collision.**  The fresh center lies strictly between the two
   sources in the same ordered strict cap and is fully deletion-robust.  No
   nested interval or global rank is proved.  Iterating the fresh robust
   center is therefore a **relocation loop**; an exact three-cycle regression
   blocks a naive local-betweenness descent.
2. **Singleton common deletion.**  This is a physical-second-apex
   `CommonDeletionTwoCenterPacket`, hence the same **robust-or-protected
   loop** as above.
3. **Unequal two-source core.**  Two pairwise-disjoint exact shells occur at
   one fresh robust center.  Checked consequences include two disjoint
   off-physical equal pairs, unequal radii, fresh-center packing, and
   `12 <= iCountAt`.  This is a **live residual**.  Exact convex geometry
   realizes two disjoint four-shells, so ordinary one-shell curvature or the
   local isosceles count is not a terminal.

### 1.4 Independent retained-parent coupling

The robust-radius split does not subsume the production
`RetainedStrictInteriorTerminalSurface R`.  Independently, every common parent
also reaches one of:

1. localized collision plus mutual omission;
2. matching source return;
3. matching endpoint critical fiber plus fresh continuation; or
4. three distinct blockers plus cross-hit/cycle continuation.

Coupling this surface with `B.secondApex_robust` produces no current direct
terminal.  The strongest genuinely new branch-specific consequence occurs
on `matchingSourceReturn` / `jointDeletion`: one named source can be deleted
while K4 survives at four pairwise-distinct centers.  Banked U5/Kalmanson
consumers still require a positive prescribed cross-row membership.  This is
an independent **live parent residual**, not an alternative proof of either
radius arm.

Likewise, first-apex robustness forces the critical blocker map to omit the
first apex and hence to have a nontrivial equal-blocker fiber.  Its exact
common critical shell is checked, but anonymous blocker-fiber iteration
supplies no cap placement or second center.  It is a useful side constraint,
not a terminal branch.

## 2. Residuals which should be retired as separate work

The following objects are loops, contradictions in disguise, or superseded
normal forms.

1. `SharedCriticalPairAtLargeSecondApex`: **redundant**; it already reduces
   to physical common deletion by the `5 > 4` extra-point argument.
2. A bare physical-second-apex `CommonDeletionTwoCenterPacket`: **loop**; its
   consumer returns the same robustness or a protected unique-four arm.
3. `RobustSecondApexMinimalDeletionInstalledSingleton` and the strict-pair
   singleton: **loop** for the same reason.
4. A fresh `FullyDeletionRobustAt` center without a nested interval, rank, or
   strict-lap packet: **relocation loop**, not progress.
5. The broad collision/multi-core/installed-singleton trichotomy:
   **superseded for primary work** by the strict-pair two-radius normal form
   and the exact-five physical-cycle normal form.
6. `LargeCapUniqueFiveTwoHitCriticalRow` as a producer: **contradiction in
   disguise**; production already proves it false.  Its load-bearing output
   is the low-hit/two-common-deletion complement.
7. A repeated reverse outside pair as an intermediate producer:
   **contradiction in disguise**; the ordered-cap outside-pair terminal closes
   immediately.  Current data instead proves those pairs injective.
8. A third common point on the robust collision pair: **terminal antecedent,
   not a routine producer**; two distinct circle centers cannot have that
   third noncollinear common point.
9. Cumulative K4 or witness closure after deleting an omission cycle:
   **wrong polarity**.  `no_qfree_at` blocks the source's own actual blocker,
   and minimality returns the already-known fresh minimal core rather than a
   contradiction.
10. One anonymous exact shell plus one `ShellCurvature` inequality:
    **nonterminal**.  The local configuration is present at every global K4
    center; aggregation and source provenance are the missing force.

## 3. Smallest load-bearing next theorem

The smallest current theorem which removes a genuine leaf of the public
`L -> False` proof, retains every potentially relevant parent field, and
does not create another packet is the all-reverse exact-five branch closer.
Schematically:

```lean
theorem false_of_largeOppositeCapsBiApex_exactFive_allReverse
    {D : CounterexampleData} {S : SurplusCapPacket D.A}
    {radius : ℝ} {H : CriticalShellSystem D.A}
    {F0 : CriticalPairFrontier D S radius H}
    {R : FrontierCommonDeletionParentResidual F0}
    {B : FrontierBiApexRobustResidual R}
    (L : FrontierLargeOppositeCapsBiApexRobustResidual B)
    {Flarge : RobustLargeRadiusParentSurface D S}
    (E : ExactFivePhysicalCycleParentResidual H Flarge)
    (hreverse : ∀ i : Fin E.cycle.period,
      (E.cycle.source i).1 ∈
        (H.selectedAt
          (E.cycle.source (E.cycle.successorIndex i)).1
          (PhysicalVertex.mem_A
            (E.cycle.source (E.cycle.successorIndex i))))
          .toCriticalFourShell.support) :
    False
```

This is architectural pseudocode; the eventual Lean declaration should use
the exact namespaces and may take the already-produced
`FrontierCoupledExactFivePhysicalCycleParentResidual H Flarge` in addition to
`L` if its anchored continuation/frontier fields are needed.  It should not
define another wrapper.

Why this leaf is the smallest honest choice:

- it is reached by the production exhaustive cycle split;
- it closes one actual nonrecursive branch of the large-radius arm;
- its period is only three through five;
- every row already has an exact 2+2 cap split, distinct source/blocker
  provenance, an injective two-point outside residual, and strict straddling
  of the same-cap targets;
- a repeated outside pair or the missing named U5 incidence has an immediate
  existing consumer; and
- retaining `L` preserves the original common-deletion parent, both physical
  robustness facts, both cap-six bounds, minimality, `noM44`, and the original
  frontier rather than hoping that a reconstructed anonymous frontier is
  enough.

The required new mathematical content is an **aggregate multirow geometric
collision**: the period-three-to-five family of cap-straddling reverse rows
must repeat its outside pair, complete one named U5 incidence pattern, or
otherwise violate a common curvature/cap budget.  A theorem proving only one
more row, one more deletion survival, or one more local curvature inequality
does not reduce this leaf.

Subsequent exact audits put a sharper lower bound on that new content. An
exact rational strict-convex nonobtuse-MEC model realizes the full period-three
co-radial source/cap-straddling blocker core together with one legal outside
target. Thus neither a one-row one-target obstruction nor a full-cycle-core
one-target obstruction is true. At the next threshold, both legal orders of
two outside targets completing one `2 + 2` row admit exact rational abstract-
distance ledgers satisfying every triangle and strict Kalmanson inequality,
the shell equalities, and named-class exclusions. Those ledgers are not planar
Euclidean models, but they prove that ordinal cancellation and linear Farkas
closure cannot decide the complete-one-row surface. A separate exact rational
eleven-point construction realizes the same-side order in planar nonobtuse-MEC
geometry with exact row/class exclusions. One complete row is therefore fully
retired. The smallest unresolved local question is simultaneous completion of
all three period-three rows; global critical/deletion coupling is the next
stronger target.

The conditional fixed-order all-three-row Kalmanson census covers the overlap
schemas rather than assuming six distinct targets. Its 2,214 quotient cases
range from three to six actual outside points. Exact ordinal cancellation closes
1,224;
the remaining 990 have independently replayed rational LRA ledgers satisfying
the complete triangle/Kalmanson/equality/exclusion system. Thus the linear
route is exhausted as a universal closer. It remains useful as a checked
pruning layer before planar Euclidean/MEC or global critical/deletion analysis.

Inside the fixed anchored chiral order used by the audit, the ordinal cores give
a sharper conditional finite normal form: the union of the three outside pairs
has cardinality at least five, and at cardinality five only a row-`0`/row-`1`
shared target survives. The 1,224 rejections compress to `Disjoint P0 P2` and
`Disjoint P1 P2`, followed by generic finite-set arithmetic. Current source,
however, provides only per-row `ReverseRowCyclicRoles`; it does not derive this
shared anchored order from `K + period = 3 + all-reverse`. One shared cap block
is constructible, but the honest source order has forward and reflected
branches, with a different exceptional overlap pair. `StrictCapBlockData` also
omits the global CCW evidence required by the existing Kalmanson API. A
pointwise `StrictCapOrder` consumer for both orientations is therefore required
before the normal form becomes live pruning.

The pointwise consumer is now proved on paper for both orientations. The sole
Lean gap is a pointwise wrapper of the existing extreme-pair hull-line
intersection theorem. It yields one pair disjoint from the other two and hence
the invariant union-cardinality pruning; it does not yield `False`.

The bounded Euclidean follow-up covers all target-union graph types of
cardinality three through six, conditional on that order, with exact-equality
parameterizations. Every branch is `UNKNOWN_NO_HIT`: there is no SAT witness,
UNSAT/coverage result, or rational reconstruction. The fixed-order systems have
generic dimensions `11` and `13`, and representative Z3/cvc5 probes are also
`UNKNOWN`; equality-only Gröbner mining is not a decision route. Further local
work must first prove the common order and then use interval/CAD or another
exact semialgebraic reduction.

Correctly centering the return at `right = S.oppApex1` gives an exact negative
linear result: all 990 fixed-forward rational LRA survivors make all three
reverse pairs non-co-radial from `right`, and all eight strict direction cells
have independently replayed witnesses. Pure triangle/Kalmanson/all-three-row
reasoning cannot produce the terminal return.

The higher-parent audit identifies a narrower nonrecursive OPEN terminal: return
one reverse outside pair to the retained original frontier pair, or prove it is
co-radial from `S.oppApex1`. The reverse blocker and first apex would then be
distinct centers in `oppCap2` through the same outside pair, so the existing
`outsidePair_unique_capCenter` theorem closes this leaf immediately. No current
total-critical-map, K4, minimality, `noM44`, or period-4/5 theorem supplies the
return occurrence. Exact finite marginal and rational convex/MEC regressions
both avoid it: the first retains current CSS-support/fiber/bi-apex/frontier
marginals, and the second retains both cap bounds, one complete reverse row, and
the retained pair marginal. The finite model already includes first-apex K4/
double deletion and a support-locked total CSS abstractly. A proof must
therefore cross-couple the global critical/deletion layer to Euclidean/MEC/all-
row order or minimality; neither layer alone forces the occurrence.

The source-faithful next combined packet adds two rows centered at the retained
first apex: `T0` contains the original pair at radius `r`, and the double-
deletion row `T1` avoids that pair at radius `rho`. If `rho = r`, six distinct
points are co-radial; if `rho ≠ r`, the two four-supports are disjoint. The
three legal frontier-pair cap-role cases and all unnamed completion roles must
remain explicit. No theorem yet couples this role domain to the reverse rows.

The exact split audit now rules out three tempting substitutes for that
coupling.  First, `T0/T1` are selected four-point classes rather than exact CSS
shells.  Second, the corrected dynamic audit supplies one exact SAT
representative in each coarse radius/role cell, with total CSS, K4, deletion
failure, all three period-three reverse rows, pair injectivity/straddling,
common cap order, and live two-circle intersection bounds while every reverse
pair avoids both T0 and T1.  These six witnesses do not exhaust the 31 labelled
same-radius role assignments or both boundary orientations.  Thus even the
named occurrence is not forced by the represented finite role/radius/CSS
marginals.  Third,
the resulting cap-six count and singleton deletion robustness are already
fields of `L` and `R`.  The equal-radius arm does add arbitrary two-point
deletion robustness at the first apex, but the checked finite metric interface
still realizes the six-class, blocker-map, and deletion marginals without
`False`.  The honest remaining gap is a same-model Euclidean/MEC/order and
full-critical/minimality coupling proving the full-`L` aggregate occurrence
and then applying an existing terminal consumer.  One pinned blocker map is
already strict-Kalmanson UNSAT even without target negation, but it is not
map-independent coverage.  A bounded internal period-three lemma may be
stated as `false_of_periodThree_allReverse_firstApexRows` from `P`, `K`,
period three, and all-reverse membership, but it is not the public target and
cannot be proved from the linear quotient alone.  Its diagnostic quotient
includes the 31 labelled
same-radius assignments, T0/T1 overlaps zero through two with fresh
completions, unequal-radius row profiles, 3/4/5-strict physical exact-five
profiles, legal F0/exact-five/blocker identities, fresh reverse targets and
sharing partitions, blocker/source cross-identifications, and both boundary
orientations.  The current fixed-cell Kalmanson solver is SAT-first diagnostic
only.  The return occurrence is not to be mined from that weak surface; it is
the aggregate full-parent producer to prove with the omitted global geometry
or provenance.

The common-order producer is drafted source-faithfully as
`nonempty_reverseCycleSharedCapOrder`.  From `K + allReverse` it chooses one
physical-cap strict block, globally indexes the actual sources and blockers,
and proves every straddle without identifying a blocker with a nonincident
source or choosing one orientation.  The draft has no `sorry` but remains
unelaborated under the active no-build gate.

The bounded fixed-cell run has completed: 1,728 forward boundary orders times
six representatives, or 10,368 exact rational checks, are all UNSAT with no
`unknown`.  The common-core census proves that this is an encoding artifact,
not convergence evidence: every fixed representative makes a T0/T1 target
pair also lie on the independent physical exact-five row, and the two apex
centers do not alternate with that pair.  One Kalmanson inequality plus those
two row equalities closes; no reverse row or period-three fact is used.  The
repaired test must start with disjoint and partially identified cross-surface
cells in the complete role/identity quotient.

The first such repaired test is now exact-QF_LRA SAT.  Its 25 named roles make
T0/T1, the physical exact-five row, blockers, and all six reverse outside
targets mutually disjoint, while retaining every strict triangle and
Kalmanson inequality, all row equalities, the common order, and two-owner
alternation.  This is a compatibility witness for the linear distance
relaxation, not a planar/MEC realization.  It blocks the shared-order linear
consumer as a direct period-three closer and makes a source-backed nonlinear
Euclidean/MEC, full-fiber, or global-minimality field the next required input.

The first fixed-cell rank-two escalation is `UNKNOWN`.  Its audited Cartesian
formula has 46 free coordinates, 19 shell equalities, 79 named exact-support
exclusions, and 575 fixed-word convexity inequalities.  Z3 and cvc5 both timed
out at 120 seconds.  Removing the exclusions and redundant distinctness, or
running short row-family prefixes, still produced only timeouts; even the
T0/T1 prefix did not decide.  This is an instrument bottleneck, not a
mathematical obstruction.  The fixed cell is also only a projection: a live
carrier needs at least one unnamed surplus-cap point and the omitted global
K4/MEC/CSS/minimality/`noM44` fields.  The next bounded method is a
convexity-preserving numerical/homotopy seed with full substitution checking,
not a larger quotient or longer blind SMT budget.

The 24-core numerical checkpoint covers two independently source-legal,
exact-QF_LRA-SAT words.  One 192-start run used affine-regular and exact
nine-point cycle-core seeds.  A second word was audited to contain the banked
exact rational complete-`b0`-row model as a cyclic subsequence, then tested in
another 192 starts using that full exact seed.  All 384 starts failed the full
substitution gate.  This is bounded `NO-HIT`, not `UNSAT`, and closes no
source theorem.  More SLSQP starts are retired; only a different
interior-point/support-line or homotopy instrument is a meaningful local
continuation.

The 2026-07-18 prose backup further shows, at the mathematical role level,
that each reverse critical support omits `S.oppApex2` and its outside pair is
contained in the union of the two complementary open blocks.  This is real
quotient pruning but not closure: the stored `rho = radius`, `IO` witness
already obeys it.  The backup supplies no T0/T1-to-reverse shared target, no
applicable through-opposite reverse row, and no full exact-transverse block
population.

The period-four/five extension factors the shared-order geometry into one
uniform paper lemma: a strict local-extremum source has disjoint incident
reverse outside pairs.  Exhaustive finite-order enumeration leaves 2 of 6
normalized period-four source orders and 6 of 24 period-five source orders;
with all source-faithful blocker placements the residual counts are 34 and
228.  These are exact order-model reductions, not kernel-checked theorems or
contradictions.  They show that the period-three union consequence cannot be
silently reused for the longer cycles.

Closing this theorem does **not** finish `L`.  The mutual-omission exact-five
arm, the unequal two-radius core, the localized robust-center nonreturn, and
the independent retained-parent branches remain.  It is nevertheless the
smallest current theorem with a direct, nonrecursive reduction of the public
goal.

## 4. Effect of the user-owned `ShellCurvature` chart completion

The current production file proves analytic equal-radius implications over a
`CounterclockwiseAngleChart`, but the live periodic adapter cannot construct
that chart as typed.  The unrestricted `orderedTriple_sign_neg` field applies
to `p < q < cycleEnd` while the wrapper also requires
`point cycleEnd = point p`; that repeated-endpoint triple has sign zero.  The
interface needs an open-fundamental-window or distinct-endpoint repair in
addition to constructing compatible unwrapped real edge/chord lifts.

Completing that chart does **not change the theorem choice above**.  It changes
the available proof method:

- the all-reverse leaf already has the best current source-facing match:
  every reverse row has two same-cap targets straddling its blocker and two
  outside-cap targets;
- after the sign-window repair, a compatible cyclic lift can place those four targets into the
  shell-curvature interface and discharge the per-row analytic inequality;
- but one per-row inequality is not contradictory, and the chart does not
  force two outside pairs to coincide or complete a named U5 packet.

The remaining theorem after chart repair and construction is still an aggregate
multirow packing/overlap result which combines the period-three-to-five rows
on one common polygon chart.  Only if the user-owned lane also supplies that
cross-block/packing consumer, rather than the chart constructor alone, does
it close or materially replace the proposed branch theorem.

The unequal two-radius core can also feed a completed chart—each fresh shell
already has four equal-radius targets—but its exact convex regression shows
that two local shells and their separate curvature bounds are compatible.
It likewise needs an aggregate incompatible-budget theorem.  Therefore the
active production lane is not a reason to duplicate chart construction and
is not a blocker to stating or auditing the branch closer.

## Epistemic boundary

- Robust second-apex classification, robust large-radius reduction, exact-five
  low-hit continuation, physical omission cycle, and exact-five parent
  residual: **present in current source and recorded kernel-checked by their
  owning lanes**.
- Strict-pair two-radius reduction, unequal-core consequences, mutual
  crisscross/global-cap split, reverse-membership capacity/straddle, and
  first-apex blocker-fiber collision: **recorded kernel-checked scratch**.
- The displayed all-reverse `False` theorem and every aggregate multirow
  curvature/cap-packing collision: **CONJECTURAL / OPEN**.
- Production or project build in this audit: **NOT RUN** under the active
  no-build/ownership gate.
- Production `sorry` closed by this audit: **NONE**.
