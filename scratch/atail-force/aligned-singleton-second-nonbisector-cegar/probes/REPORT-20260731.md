# Bounded aligned-singleton/nonbisector probe (2026-07-31)

## Commands

The fresh QF Boolean CEGAR probe was run with a hard bounded budget:

```text
UV_CACHE_DIR=scratch/.uv-cache uv run python \
  scratch/atail-force/aligned-singleton-second-nonbisector-cegar/qf_bool_cegar.py \
  --output scratch/atail-force/aligned-singleton-second-nonbisector-cegar/probes/qf-bool-probe-20260731.json \
  --checkpoint scratch/atail-force/aligned-singleton-second-nonbisector-cegar/probes/qf-bool-probe-20260731.checkpoint.json \
  --wall-seconds 45 --outer-timeout-ms 30000 --metric-timeout-ms 5000 \
  --max-refinements 1 --random-seed 17
```

Result: `UNKNOWN`, wall-clock budget exhausted after 60.435779666062444 s,
zero refinements, and no outer model reached the metric gate.  The output's
scope is exact bounded `n = 17` outer constraints with no `z`/escape
constraints.

For a concrete existing bounded survivor, the source verifier and strict metric
gate were rerun:

```text
UV_CACHE_DIR=scratch/.uv-cache uv run python \
  scratch/atail-force/aligned-singleton-second-nonbisector-cegar/verify.py \
  scratch/atail-force/aligned-singleton-second-nonbisector-cegar/nonbisector-v3.json \
  --output scratch/atail-force/aligned-singleton-second-nonbisector-cegar/probes/nonbisector-v3-verify-probe-20260731.json

UV_CACHE_DIR=scratch/.uv-cache uv run python \
  scratch/atail-force/aligned-singleton-second-nonbisector-cegar/metric_check.py \
  scratch/atail-force/aligned-singleton-second-nonbisector-cegar/nonbisector-v3.json \
  --output scratch/atail-force/aligned-singleton-second-nonbisector-cegar/probes/nonbisector-v3-metric-probe-20260731.json \
  --timeout-ms 30000
```

The outer payload verifies `verified: true` (`n = 17`, 17 blockers, 14
non-robust centers), while the rational strict-metric/Kalmanson gate returns
`UNSAT` in 0.30726508400402963 s.  Its unsat core uses 8 equality links and the
gate asserted 146 equality, 1894 disequality, 2040 strict-triangle, and 4760
strict-Kalmanson constraints.

## Classification and omissions

This is **not** a full abstract countermodel to the Lean theorem: the fresh
CEGAR run produced no model, and the saved combinatorial survivor is rejected
by the additional rational metric gate.  It is also not a Lean proof or a
refutation.  The probe identifies the missing necessary constraint for the
outer Boolean abstraction: strict Euclidean/metric realizability (the current
gate uses strict triangle plus Kalmanson inequalities).  The outer model still
omits Euclidean coordinates/MEC realizability, full `noM44` geometry,
theorem-bank cuts not encoded here, and the monolithic all-class two-circle
intersection overlay.  Therefore the live theorem remains **UNKNOWN**;
neither SAT nor UNSAT in this bounded abstraction promotes to kernel closure.
