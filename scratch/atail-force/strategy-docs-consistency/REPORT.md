# Strategy-doc consistency audit

Date: 2026-07-17

Scope: read-only audit of the five shared strategy documents requested by the
lane owner. No shared document, production Lean file, build artifact, or git
state was changed.

## Verdict

The newly added top status blocks agree on the important finite and source
facts, but the five documents are **not yet dispatch-consistent**. The main
problem is not the current 218-bank verdict. It is that older sections still
use unqualified `current`, `active`, `primary`, and `next theorem` language for
routes that the new top status has superseded.

The authoritative current hierarchy should be stated once and used everywhere:

1. final parent assembler:
   `false_of_twoLargeCaps_commonCriticalMap` before LIVE construction;
2. robust branch boundary:
   `false_of_frontierBiApexRobustResidual` over the whole retained parent
   residual;
3. primary missing producer inside that branch:
   `nonempty_criticalFiberClosingCore R` (or the explicitly secondary broader
   existential `∃ rows, ShellMetricCoreAlternative rows H`);
4. already-proved immediate consumer:
   `false_of_criticalFiberClosingCore`;
5. protected original/swapped `FA-UNIQ4/5` consumers as separate dependencies.

With that hierarchy, `false_of_frontierBiApexRobustResidual` is the branch
theorem to be assembled, while `nonempty_criticalFiberClosingCore R` is the
primary mathematical producer currently being sought. They should not both be
called the sole primary target without explaining this parent/producer
relationship.

## Current facts that are synchronized

These statements are consistent across the current top blocks and with the
authoritative `/opt/nfs` files:

- The corrected 217-schema bank had a card-11 structural survivor; adding the
  front-before-isosceles schema produced the current 218-schema bank.
- Card 11 is exactly decided within the stated outer abstraction: the 210
  row-zero-support cubes are an exhaustive/disjoint partition and have checked
  UNSAT proofs. This is bounded coverage and closes no source `sorry`.
- The current 218-bank is not uniform. Independently checked outer carriers at
  cards 12 and 13 avoid all 218 schemas.
- Those two carriers are not geometric survivors: distinct missed transports
  of production `false_of_two_cycle` kill them. These are per-carrier
  eliminations and regression gates, not uniform occurrence/coverage.
- More card-11 cubing and broad literal 555/654 mining are not load-bearing.
- The eleven LIVE-Q/C signatures do not directly supply the new generic
  consumer antecedents and lose the shared-critical-system provenance needed
  by the parent route. The active route calls the parent contradiction before
  LIVE classification; it does not dispatch the 31 helper holes individually.
- `/opt/nfs/verified-results.md:12211-12213` explicitly leaves O17, O18, and
  O19 open. Lemma 200 reduces each of the two residual six-center `000111`
  records to sixteen A/B words, but does not eliminate them; arbitrary
  mixed/dense sinks also remain (`:12232-12235`).

The clearest synchronized statements are:

- `docs/closure-plan-full-spec-2026-07-09.md:250-306`;
- `docs/closure-matrix-2026-07-09.md:99-100`;
- `docs/atail-force-producer-plan-2026-07-13.md:150-187`;
- `docs/audits/2026-07-17-critical-fiber-strategy-alignment.md:120-159`; and
- `docs/97-rvol-full-prose-proof-2026-07-13.md:760-798`.

## Contradictions and stale dispatch language

### 1. Closure matrix: current executable rows disagree with K-A-PAIR

Severity: **critical**, because the matrix declares itself the executable task
register.

`K-A-PAIR` gives the corrected target at line 99: prove
`nonempty_criticalFiberClosingCore R` (or the broader parent-scoped metric
existential), use cards 12/13 only as regression gates, and do not demand
coverage for every arbitrary selected-row choice.

Several later live rows still dispatch the superseded whole-carrier coverage
route:

- `ATAIL-FORCE` (`:282`) says to close the robust residual or extract a faithful
  whole-carrier decision object.
- `ATAIL-PARENT-CONTENT` (`:300`) says to preserve the robust residual through
  equality-quotiented current-surface coverage.
- `ATAIL-PARENT-WIRE` (`:466`) says that a faithful whole-carrier theorem is
  still open and must be closed.
- `ATAIL-FRONTIER-CLOSING-CORE` (`:295`) says
  `nonempty_criticalFiberClosingCore R` is merely sufficient and not a primary
  target, using the retained-radius selector rationale that the top strategy
  now classifies as fallback.

`ATAIL-ROBUST-COUPLING` (`:441`) is closest to the new target, but it should
name `nonempty_criticalFiberClosingCore R` explicitly so it cannot be read as
another arbitrary whole-carrier coverage assignment.

Required reconciliation: update the executable rows to the hierarchy above;
move the whole-carrier decision work to a DONE-NEGATIVE / regression-only row.

### 2. Producer plan: the second “Current execution plan” is stale

Severity: **critical**.

The authoritative top section correctly says:

- card-11 218-schema coverage is complete (`:153-156`);
- cards 12/13 refute uniform coverage by the current bank (`:158-169`); and
- the primary/preferred direct target is
  `nonempty_criticalFiberClosingCore R` (`:166-180`).

But the later section titled `## Current execution plan` (`:1111`) still makes
the current step 4 a whole-carrier equality-quotiented/full-Kalmanson decision
gate (`:1129-1137`). That gate has already run through the relevant negative
results and cannot be the live dispatch after the cards 12/13 verdict.

The same file later has an unqualified `### Final theorem boundary and active
producer interfaces` whose active target is direct `False` from
`FrontierCommonDeletionRefinedNormalForm` (`:2731-2744`), and an unqualified
`### Content-bearing branch theorem` based on
`ChosenLateCriticalSystemParentContent` (`:3679-3732`). Both predate and compete
with the top parent-scoped critical-fiber producer.

Required reconciliation: rename these sections as dated/superseded records, or
replace their current-action paragraphs with the top hierarchy. There should
be only one `Current execution plan` heading.

### 3. Critical-fiber alignment audit: two different “primary” theorems

Severity: **high**.

The audit's status and Decision section call
`false_of_frontierBiApexRobustResidual` the active theorem (`:5-7`, `:27-38`).
Later, after the higher-cardinality bank audit, it calls
`nonempty_criticalFiberClosingCore R` the preferred remaining theorem
(`:150-159`). Then `## Active next theorems` again says the **only** new primary
theorem is `false_of_frontierBiApexRobustResidual` and still names the
whole-carrier Kalmanson model as its current decision gate (`:259-268`). Finally,
inside a historical fallback it says `nonempty_criticalFiberClosingCore R` is
only optional and not the active coverage conjecture (`:461-463`).

The fallback label makes the last statement historical, but the `Active next
theorems` block is not historical and contradicts the preferred-producer
statement unless the theorem/producer hierarchy is spelled out.

There is also chronological wording at `:64-70` saying the remaining gap is
uniform coverage/certification. The later text correctly supersedes that
claim; it should be introduced as an intermediate checkpoint rather than a
present-tense remaining gap.

Required reconciliation: make the audit say explicitly that the robust
`False` theorem is the parent branch goal and the critical-fiber existence
theorem is the primary missing producer used to prove it. Remove the stale
whole-carrier decision-gate sentence from the active-next section.

### 4. Full prose proof: top Part V is current, completion matrix is stale

Severity: **high**.

The refreshed Part V narrative correctly records card-11 exact coverage,
cards 12/13 nonuniformity, the parent-scoped critical-fiber target, and open
`/opt/nfs` O17--O19 (`:774-798`).

The document's own unqualified completion matrix later says active work is the
seven strict-interior/refined-frontier leaves (`:1293`) and that final assembly
is blocked only on that O18 plus protected unique arms (`:1295-1299`). That is
the older decomposition, not the current physical split plus critical-fiber
producer.

The Part-V compatibility discussion also says the route derives `False`
directly from `FrontierCommonDeletionRefinedNormalForm` rather than the current
robust/critical-fiber route. Because it appears under the current Part V rather
than a historical heading, it can override the refreshed paragraph for a
reader following the detailed statement.

Required reconciliation: update the completion matrix to the current
hierarchy or explicitly label it a superseded decomposition. The prose should
not claim both the refined seven-leaf surface and the critical-fiber producer
as the active O18.

### 5. Full prose proof reuses O17--O19 for a different registry

Severity: **medium**, but likely to cause status-reporting errors.

At `:797-798`, `O17--O19` means the authoritative `/opt/nfs` obligations. At
`:1292-1295`, the document's local completion matrix uses O17, O18, and O19 for
the exact-five reduction, strict-interior leaves, and protected unique arms.
These are unrelated identifiers.

Required reconciliation: prefix the external ledger identifiers as
`NFS-O17`/`NFS-O18`/`NFS-O19` (or always write `/opt/nfs O17--O19`) and avoid
bare O17--O19 in this document.

### 6. Closure plan: top is correct, appended checkpoints are not globally archived

Severity: **high**.

The top current block is the best single statement of the new route
(`:250-306`). It identifies the primary critical-fiber producer, rejects
uniformity of the 218 bank, records the negative direct-helper audit, and
accurately scopes `/opt/nfs`.

However, only the first old subsection at `:321` explicitly says its
`active/current/next` words are historical. Following same-level sections are
not covered by that warning. Examples of unqualified competing dispatches are:

- `:679-690`: “first legitimate next theorem” is the frontier-coupled
  exact-five cycle consumer;
- `:4237-4267`: `## 2026-07-14 Front-A producer decision` presents itself as
  authoritative;
- `:4337-4341`: “the next theorem” is geometric nonreturn for the refined
  common-deletion packets; and
- `:4470-4487`: close seven strict-interior leaves, with the critical-fiber
  producer only a sufficient optional output.

These passages are useful history, but the current hierarchy supersedes them.

Required reconciliation: add a document-level “historical checkpoint archive”
boundary before the appended dated sections, or add explicit superseded labels
to each later strategy checkpoint. Current action should be defined only in
the top block and matrix.

## Direct LIVE-helper route

No substantive contradiction was found in the newest wording for this topic.
The five documents consistently treat LIVE-Q/C as literal source inventory,
not independent mathematical work, and direct-helper elaboration as negative.
The only issue is that older helper and refined-frontier discussions remain in
unmarked historical sections. The current rule should remain:

> Do not dispatch LIVE-Q/C individually. Prove the parent contradiction before
> LIVE construction, then reference-mine and delete or source-clean-wrap the
> obsolete helper tree.

## Recommended minimal document repair order

1. Fix the executable matrix rows `ATAIL-FORCE`, `ATAIL-PARENT-CONTENT`,
   `ATAIL-ROBUST-COUPLING`, and `ATAIL-PARENT-WIRE` to name the same primary
   producer as `K-A-PAIR`.
2. Replace or demote the producer plan's second `Current execution plan`.
3. Resolve the parent-theorem versus producer terminology in the critical-fiber
   alignment audit.
4. Refresh the full prose completion matrix and disambiguate `/opt/nfs`
   obligation names.
5. Put the closure plan's appended dated checkpoints behind one explicit
   historical/superseded boundary.

After those repairs, the current status can be summarized without ambiguity:
card-11 bounded coverage is complete; the current 218 bank is nonuniform at
cards 12/13; both saved higher-card carriers are Euclidean-dead but do not give
uniform coverage; `/opt/nfs` O17--O19 remain open; direct LIVE closure is not a
route; and the missing load-bearing producer is
`nonempty_criticalFiberClosingCore R` under the full parent geometry.

## Re-audit after the strategy-doc repairs

Re-audit date: 2026-07-17

Verdict: **the requested authoritative blocks are now synchronized.** The
earlier critical dispatch contradictions have been repaired. No shared file,
production file, build artifact, or git state was changed during this
re-audit.

### Repaired points that now pass

1. **Executable matrix hierarchy — PASS.** The four requested rows now agree:

   - `ATAIL-FORCE` (`docs/closure-matrix-2026-07-09.md:282`) makes the
     parent-scoped critical-fiber existence theorem the open producer and
     classifies finite/whole-carrier work as regression only.
   - `ATAIL-FRONTIER-CLOSING-CORE` (`:295`) names
     `nonempty_criticalFiberClosingCore R` as the primary producer, records
     `false_of_criticalFiberClosingCore` as done, and isolates the earliest
     endpoint/cross-hit branch's sole missing cyclic placement.
   - `ATAIL-PARENT-CONTENT` (`:300`) distinguishes the robust branch theorem
     from its missing producer and makes whole-carrier coverage regression-only.
   - `ATAIL-PARENT-WIRE` (`:466`) depends on that producer plus the protected
     unique arms and no longer depends on a whole-carrier bank theorem.

   `ATAIL-ROBUST-COUPLING` (`:441`) is also compatible: its deliverable is the
   full-parent existential producer, while cards 11--13 remain regression
   evidence.

2. **Authoritative current execution plan — PASS.** The producer plan now has
   one explicitly authoritative `Current execution plan`
   (`docs/atail-force-producer-plan-2026-07-13.md:1111-1153`). Step 4 is
   `nonempty_criticalFiberClosingCore R`; it identifies the exact earliest
   missing field `EndpointOrderedBoundaryPlacement fresh`. Step 6 correctly
   demotes whole-carrier banks and old retained/collision/matching trees to
   regression or in-proof reuse.

3. **Active theorem hierarchy — PASS.** The critical-fiber alignment audit now
   consistently distinguishes:

   - branch goal `false_of_frontierBiApexRobustResidual`;
   - primary producer `nonempty_criticalFiberClosingCore R`; and
   - completed consumer `false_of_criticalFiberClosingCore`.

   This is explicit both in the status/Decision block
   (`docs/audits/2026-07-17-critical-fiber-strategy-alignment.md:5-42`) and the
   `Active next theorem hierarchy` (`:263-275`). The old “optional” wording is
   explicitly revoked as historical at `:468-471`.

4. **Full-prose completion matrix and identifiers — PASS.** The local active
   obligations are now `P97-O17` through `P97-O20`
   (`docs/97-rvol-full-prose-proof-2026-07-13.md:1292-1299`), so they no longer
   collide with the explicitly qualified authoritative `/opt/nfs` O17--O19 at
   `:797-798`. `P97-O18` states the parent-scoped critical-fiber producer;
   `P97-O17` demotes the strict-interior/exact-five work to a source library.

5. **Closure-plan archive authority — PASS.** The new rule at
   `docs/closure-plan-full-spec-2026-07-09.md:308-317` says the opening status
   block and executable matrix define current action, and all later dated
   checkpoints are historical unless they explicitly supersede the 2026-07-17
   hierarchy. This resolves the old unqualified `current`/`next theorem`
   language in the appended archive. The old 2026-07-14 producer section is
   additionally labeled historical at `:4248`.

6. **LIVE helper policy — PASS.** The repaired authoritative sections continue
   to agree that LIVE-Q/C is source inventory pending the parent bypass, not an
   independently dispatched route.

### Remaining current-vs-historical issues

No remaining contradiction was found in the five specifically repaired
authority blocks. Three lower-severity cleanup issues remain:

1. **Alignment-audit chronological sentence (medium).** At
   `docs/audits/2026-07-17-critical-fiber-strategy-alignment.md:60-74`, the
   prose still says in present tense that the next decision object is the
   whole-carrier Kalmanson surface and that the remaining gap is uniform
   coverage/certification. The later current verdict (`:140-163`, `:263-275`)
   says that current-bank uniformity is false and the primary missing producer
   is parent-scoped critical-fiber existence. This early paragraph should say
   “at that checkpoint” or be marked superseded.

2. **Matrix retained-selector subrows (medium dispatch ambiguity).** The
   umbrella rows correctly say retained-radius/collision/matching modules are
   source libraries used inside the producer, not separate final dispatch
   trees. But `ATAIL-RETAINED-BLOCKER-SELECTOR` and neighboring rows
   (`docs/closure-matrix-2026-07-09.md:296-299`) still use statuses and
   imperatives such as “FINAL ARM CONSUMERS OPEN” and “prove/terminate.” Because
   they sit under `Primary ATAIL theorem decomposition`, a dispatcher could
   still read them as parallel final obligations. Mark them
   `INPUT-LIBRARY / NO INDEPENDENT DISPATCH` or explicitly make their only
   consumer `ATAIL-FRONTIER-CLOSING-CORE`.

3. **Producer-plan appended checkpoints (low archival ambiguity).** The
   authoritative current plan at `:1111` resolves precedence, and the former
   theorem boundary is now labeled historical at `:2734`. Other later dated
   sections, especially the 2026-07-15 continuation checkpoints, still contain
   unqualified “next theorem” wording. They no longer override the explicitly
   authoritative plan, so this is not an active strategy contradiction, but a
   closure-plan-style archive authority sentence would remove the remaining
   ambiguity.

Net current status after repair: the docs now consistently dispatch
`nonempty_criticalFiberClosingCore R` under full parent geometry, followed by
the proved consumer and robust branch assembly; protected unique arms remain
parallel dependencies; finite banks and `/opt/nfs` remain regression/theorem-
discovery inputs; and no source `sorry` is claimed closed.

## Final owner re-audit

The three lower-severity items above were subsequently repaired:

- the chronological whole-carrier paragraph is explicitly a superseded
  regression checkpoint;
- the retained collision/matching rows are fallback source libraries with no
  independent final dispatch; and
- the producer plan now has an authority rule covering every later checkpoint.

The final two-cycle audit is also synchronized across all five documents. They
now state that 218/262 were mined inventories rather than cyclic-cut-closed
theorem banks, that the scoped 292 checkpoint adds 74 records and hits the
saved card-12/card-13 carriers 4/14 times, and that path-length dependence
prevents interpreting this repair as finite all-cardinality coverage.

Final verdict: **PASS** for the current authoritative strategy surface. The
primary missing producer remains `nonempty_criticalFiberClosingCore R`; the
saved carriers and `/opt/nfs` geometry are regression/theorem-discovery input;
no source `sorry` is claimed closed.

## Post-audit bi-apex signature correction

The five authoritative documents were re-synchronized after the endpoint
order audit exposed one remaining overstrong statement. The primary producer
is now

```text
nonempty_criticalFiberClosingCore_of_biApexRobust
  (B : FrontierBiApexRobustResidual R)
```

rather than the R-only `nonempty_criticalFiberClosingCore R`. This change is
load-bearing: the advertised proof uses both physical-apex blocker-image
omissions, while the old signature could not consume
`B.secondApex_robust`. The output remains the existing production
`CriticalFiberClosingCore R`, so the completed consumer is unchanged.

The documents also agree that the endpoint/PG136 branch is fallback rather
than the active primary route. Production does not supply its coarse six-role
order, exact rational strictly convex models realize both surviving nested
tails, and the pair-preserving row refinement reaches no current terminal.
The B-indexed two-fiber/triple-fiber geometry is the current source surface.

Post-correction verdict: **PASS**. No source `sorry` is claimed closed.

## Final source-verified cap and quantifier correction

The preceding post-audit bare-`B` signature is preserved as history and is
superseded. Source inspection of the physical-ingress and frontier boundaries,
together with `biapex-parent-surface-audit/QUANTIFIER_AUDIT.md`, fixes the
public robust endpoint as

```lean
L : FrontierLargeOppositeCapsBiApexRobustResidual B
```

with both live branch facts

```lean
6 <= S.oppCap1.card
6 <= S.oppCap2.card
```

and fixes the primary missing theorem as the direct contradiction

```lean
false_of_largeOppositeCapsBiApexRobust
    (L : FrontierLargeOppositeCapsBiApexRobustResidual B) : False
```

Generic bare `B` admits cap-four and cap-five configurations already routed
by the physical cap exits. A fixed-initial-`H` theorem from `L` to
`CriticalFiberClosingCore R` is propositionally sufficient but operationally
overconstrained and must not guide mining. `CriticalFiberClosingCore` remains
an optional internal route only. If used, the honest helper chooses a
favorable critical system after the full cap/frontier data is known:

```lean
exists_lateCriticalFiberClosingCore_of_largeOppositeCapsBiApexRobust L :
  ∃ Hfav : CriticalShellSystem D.A,
    Nonempty (CriticalFiberClosingCore
      (rebaseFrontierCommonDeletionParentResidual R Hfav))
```

Production already provides the frontier rebase, and scratch provides a
common-deletion rebase. The `R`/`B`/`L` rebase ladder is still missing and is
required only if this internal core route is selected.

The synchronized combined dispatcher is therefore five-arm and cap-first:

1. close `OriginalFirstApexSmallCapResidual`;
2. close `PhysicalSecondCapSmallResidual`;
3. close `PhysicalSecondApexCriticalResidual`;
4. on the remaining large-cap branch, extract the frontier and close the
   protected original unique arm; or
5. on common deletion construct `R`, then `B`, then `L`, and apply direct
   `false_of_largeOppositeCapsBiApexRobust L`.

The five authoritative documents now use this public boundary. Historical
bare-`B` and fixed-`H` core signatures are explicitly non-authoritative. No
production source was edited, no build was run, and no source `sorry` is
claimed closed.
