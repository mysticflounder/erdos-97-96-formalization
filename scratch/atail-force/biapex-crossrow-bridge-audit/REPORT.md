# Large-cap bi-apex cross-row bridge audit

Date: 2026-07-17

Status: **NO EXISTING CROSS-ROW TERMINAL FIRES. THE HONEST PRODUCER MUST
ACCEPT BOTH LARGE-CAP BOUNDS IN ADDITION TO THE BI-APEX ROBUST PACKET. THE
RETAINED PHYSICAL ROWS DO NOT ALIGN THE FRONTIER PAIR OR AN ANONYMOUS BLOCKER
FIBER. SHELL CURVATURE HAS NO CURRENT TERMINAL CONSUMER. NO PRODUCTION
`sorry` IS CLOSED.**

## Scope and corrected live surface

The initial input considered by this audit was

```lean
B : FrontierBiApexRobustResidual R
```

with `R : FrontierCommonDeletionParentResidual F`. The honest post-small-cap
surface is stronger:

```lean
hcap1 : 6 ≤ S.oppCap1.card
hcap2 : 6 ≤ S.oppCap2.card
B : FrontierBiApexRobustResidual R
```

The two cardinality hypotheses are not fields of `R` or `B`; they must be
threaded from the physical-ingress classification. A theorem stated from `B`
alone is therefore unnecessarily strong.

Only this directory was modified. No production Lean, plan document,
generated file, protected unique-row lane, `ShellCurvature`, `surplusM44`, or
card-five consumer was changed. No Lean or Lake command was run.

## Required bank preflight

Before deriving or proposing any local incidence statement, this lane checked:

- `docs/general-n-certificate-bank-mining-2026-07-09.md`, including the
  sibling `p97-rvol` section;
- `certificates/p97_rvol_general_n_mining.{md,json}`;
- `certificates/erdos97_legacy_general_n_mining.{md,json}`; and
- `certificates/erdos_general_theorem_p97_mining.{md,json}`.

Focused `nthdegree docs search --lean` queries covered:

- a frontier pair or retained physical row aligned with an actual blocker
  fiber;
- two large opposite caps and a blocker collision;
- shared late pairs, different-pair critical-row hits, and same-cap outside
  pairs; and
- shell-curvature conclusions combined with large-cap or critical-shell data.

The searches recover the current production Kalmanson and same-cap sinks,
`RetainedRadiusCollision`, the exact-five cross-incidence terminal, and the
current bi-apex multiplicity theorem. They do not recover a theorem deriving
the required alignment or cross membership from the large-cap bi-apex
surface. Neither legacy registry contains a closer with the current
`CriticalShellSystem` provenance.

## What the large-cap hypotheses add exactly

The production theorem

```lean
largePhysicalRadius_or_exactTwoFourRadii_of_robust
```

can be applied immediately to `hcap1`, `hcap2`, and
`B.secondApex_robust`. It gives the exhaustive physical-second-apex split

```text
LargePhysicalSecondApexRadiusIngress D S
or
PhysicalSecondApexExactTwoFourRadiusResidual D S.
```

This is real additional information relative to `B` alone, but neither arm is
an immediate contradiction.

### At least one five-point physical class

The large-radius arm supplies a positive second-apex radius class of
cardinality at least five. Combining its fields with `R.minimal` and
`R.noM44` constructs `RobustLargeRadiusParentSurface`. Production

```lean
nonempty_reducedOutcome_of_largeSecondApexRadius
```

then reaches either another source-faithful common-deletion packet or the
exact-unique-five continuation. The exact-five terminal still requires two
named strict-cap members of the physical class in one actual critical row:

```lean
LargeCapUniqueFiveTwoHitCriticalRow
```

Neither cap cardinality nor bi-apex blocker multiplicity produces those two
cross incidences.

### Two exact four-point physical classes

The other arm supplies two support-disjoint exact four-classes at the physical
second apex. Coupling it back to `R` gives the existing
`FrontierCoupledPhysicalSecondApexExactTwoFourRadiusResidual`. The checked
scratch consumer routes that packet only to the protected original-unique arm
or another common-deletion packet. It does not produce `False` or a cross-row
pair.

Thus the large-cap split improves the source surface but presently loops back
to already named continuations. It does not instantiate
`CriticalFiberClosingCore`.

## Why the retained physical rows do not align the frontier pair

The common-deletion packet stored in `R` was constructed after deleting the
frontier source `F.pair.q`. Consequently the two exact stored rows satisfy

```text
F.pair.q ∉ R.common.packet.B₁
F.pair.q ∉ R.common.packet.B₂.
```

This is the `q_not_mem` field of each `U5QDeletedK4Class`. Therefore neither
stored physical row can contain the complete frontier pair
`F.pair.q,F.pair.w`.

The physical centers themselves do not repair this:

- `S.oppApex1` is equidistant from the frontier pair, but the stored first row
  omits `q`;
- `SurvivorPairRelocationPacket.oppApex2_dist_ne` proves that
  `S.oppApex2` is **not** equidistant from the frontier pair; and
- `frontierCommonDeletionResidual_secondRow_inter_marginal_card_le_one`
  independently shows that the second stored row contains at most one member
  of the full off-surplus first-apex marginal.

Even if the two frontier sources happen to have equal actual blockers, their
common blocker row is the only actual critical row supplied through that
equality. The first apex provides a second metric center but not a second
actual row through the pair; the second apex provides neither. No existing
shared-late-pair, third-actual-center, or same-cap terminal fires from these
facts alone.

## Why the physical rows do not align an anonymous collision fiber

Production bi-apex multiplicity returns either two blocker fibers or one
fiber with at least three sources. It does not identify any of those sources
with `F.pair.q`, `F.pair.w`, `B₁`, `B₂`, or a member of one physical-apex
radius class.

For a chosen collision fiber `P`, the existing immediate terminals have the
following first missing antecedents.

| Terminal | First missing source-valid fact |
| --- | --- |
| `false_of_two_selected_rows_shared_late_pair` | A second distinct selected row containing the same two fiber sources, plus the nonalternating four-role cyclic order. |
| `OrderedCrossRowCore.false` | First-apex alignment of the chosen fiber, a fresh first-apex row equality, one actual different-pair cross hit, and a compatible six-role order. |
| `SameCapCollisionPairCore.false` | A source-faithful second center for the same fiber pair and one cap containing both centers while excluding both sources. |
| `false_of_distinct_actualRow_contains_collisionSources` | First-apex radius alignment of the chosen fiber and one genuinely distinct actual row containing both sources. |

The large-cap inequalities constrain the number of carrier points in two
caps. They do not supply any of these named source identities, memberships,
or orders.

## Shell-curvature audit

At the current working-tree state, production
`ConvexCyclicOrder/ShellCurvature.lean` proves the equal-radius curvature
outputs

```lean
two_sided_quarter_turn_separation_of_equal_radius
closed_middle_arc_add_center_turn_lt_pi_of_equal_radius.
```

Source search finds no use of either theorem outside that module. In
particular, no theorem consumes one shell's quarter-turn bounds together with
`6 ≤ S.oppCap1.card` and `6 ≤ S.oppCap2.card` to obtain `False`.

The large physical radius class and every actual critical row do provide
local equal-radius inputs. What remains absent is a source-valid packing
consumer which:

1. places the ordered shell targets in the required cap blocks;
2. couples at least two shells or a shell and a physical class in overlapping
   curvature budgets; and
3. uses the resulting strict inequalities to exceed the one-cycle curvature
   budget.

Cap cardinality alone does not place the four named support points, and the
bi-apex multiplicity theorem does not provide a second shell at a controlled
center or with controlled overlap. Shell curvature is therefore a promising
geometric ingredient, not a current closer.

## Exact finite regression

`check_large_cap_incidence_model.py` is an exact finite assertion check for a
fourteen-point incidence fixture. It retains:

- a genuine three-cap set partition with profile `(5,6,6)` and
  `|C1|+|C2|+|C3|=|A|+3`;
- two five-point physical-apex classes, hence deletion robustness at both
  physical apices;
- a frontier pair in one off-surplus first-apex marginal and unequal
  second-apex classes;
- the two stored q-deleted four-rows, their overlap bound, and the physical
  second-row/marginal one-hit bound;
- a fixed-point-free blocker endomap omitting both physical apices, with two
  nontrivial fibers;
- source membership, exact-card-four support locking, and the local cap
  two-hit bound for every displayed row.

In the fixture:

- the frontier pair is not a blocker collision;
- neither repeated blocker fiber lies in either physical-apex class or stored
  row; and
- no actual critical row at a distinct blocker contains a repeated-fiber
  pair.

This is **EXACT WITHIN THE FINITE INCIDENCE ABSTRACTION**, not a Euclidean or
minimal counterexample. It proves only that large-cap cardinalities, retained
row incidence, support locking, and the two omission values do not logically
name the missing cross pair. A successful Lean proof may still use convex
metric, MEC, full-filter, shell-curvature packing, or global minimality facts
absent from the fixture.

Replay:

```bash
UV_CACHE_DIR=/tmp/p97-uv-cache uv run \
  scratch/atail-force/biapex-crossrow-bridge-audit/\
  check_large_cap_incidence_model.py
```

## Corrected missing theorem

The honest load-bearing producer should be weakened from the current B-only
plan to the live large-cap surface:

```lean
theorem nonempty_criticalFiberClosingCore_of_largeCaps_biApexRobust
    {D : CounterexampleData} {S : SurplusCapPacket D.A} {radius : ℝ}
    {H : CriticalShellSystem D.A}
    {F : CriticalPairFrontier D S radius H}
    {R : FrontierCommonDeletionParentResidual F}
    (hcap1 : 6 ≤ S.oppCap1.card)
    (hcap2 : 6 ≤ S.oppCap2.card)
    (B : FrontierBiApexRobustResidual R) :
    Nonempty (CriticalFiberClosingCore R)
```

Status: **OPEN MATHEMATICS**.

Its proof must choose the collision fiber after inspecting the complete
large-cap, MEC, common-deletion, and two-apex geometry. The first genuinely
missing output is still one of:

1. a physical-apex-aligned collision plus a distinct actual row through that
   pair;
2. a same-cap source-faithful second center for a chosen collision pair;
3. an ordered different-pair cross hit for a retained-radius collision; or
4. a new shell-curvature packing contradiction which consumes the large-cap
   placement directly.

Once this theorem exists, production
`false_of_criticalFiberClosingCore` closes the robust arm routinely. No
source `sorry` or axiom dependency changes at this checkpoint.
