# Parent geometry route after the aggregate counterfamily

Date: 2026-07-19

Status: **SOURCE-CURRENT ROUTE AUDIT. THE EVEN-CARD CIRCULAR-SPLIT
COUNTERFAMILY REFUTES THE ABSTRACT AGGREGATE CONTOUR ROUTE, BUT IT OMITS THE
ONE SOURCE-FAITHFUL LAYER MOST DIRECTLY CONNECTED TO THE CURRENT TERMINAL: THE
ACTUAL DELETION-CRITICAL `H.selectedAt` ROW AT A RETAINED FRONTIER SOURCE,
WITH THE SAME TOTAL `CriticalShellSystem H` COUPLING THE FRONTIER TO THE
EXACT-FIVE TRANSITION. AN EXACT REPLAY SHOWS THAT THIS SINGLE PRODUCTION
INSTANCE ALREADY EXCLUDES THE 26-POINT T0/T1 LOCAL CELL. THE PUBLIC TARGET
REMAINS DIRECT FULL-PARENT `False`. NO CLOSING THEOREM BELOW IS CLAIMED PROVED
AND NO PRODUCTION `sorry` IS CLOSED.**

This audit owns only this file. It changes no production Lean, shared plan,
proof-blueprint data, exact-seven work, `ShellCurvature`, or `SurplusM44`.

## 1. What production has already finished

The remaining all-reverse gap is not transition bookkeeping.
`LargeCapUniqueFivePhysicalOmissionTransitionGlobal.lean` already provides:

- `nonempty_mutualOmissionTransitionEdge_or_all_reverseMembership`;
- `exists_transitionReverseOutsidePair_eq_pair`;
- `nonempty_firstContinuation_zeroEntryCycleWithReverseMembership`;
- `physicalVertices_card_eq_three_of_all_omissions_reverseMembership`; and
- `false_of_transitionReverseOutsidePair_coRadial_firstApex`.

`ParentExactFiveAssembler.lean` retains these facts in
`FullParentExactFiveAllReverseData` and already packages the exact terminal as

```lean
FirstApexCoRadialTransitionReversePairOccurrence A
```

with checked consumer

```lean
false_of_fullParentExactFiveAllReverseData_of_firstApexOccurrence
```

`ParentExactFiveExactSix.lean` also proves

```lean
false_of_fullParentExactFiveAllReverseData_of_secondCap_card_eq_six
```

so the occurrence is not needed on the closed exact-six all-reverse leaf.
None of the global split, exact-two normalization, source anchoring, or
terminal adapter should be listed as missing work.

## 2. What the circular-split counterfamily actually refutes

`TIGHT_GRAPH_UNIFORM_ATTACK.md` constructs, for every even `n >= 12`, a
strict circular-split metric whose complete equality graph has four nonloop
targets at every source, four uses of every target, strong connectivity,
pair alternation, and strict Kalmanson inequalities. Therefore none of the
following can be the parent closer:

- a generic `4*n-1` edge bound;
- a forced low-degree row;
- an abstract product-box comparability lemma; or
- a one-signed circular-interval flux circulation derived only from the
  complete four-choice row table.

The family is not a live parent model. In particular it does not supply a
single concrete

```lean
H : CriticalShellSystem D.A
```

whose `centerAt`, exact `selectedAt`, and `no_qfree_at` data are shared by the
frontier `F`, the common-deletion parent `R`, the exact-five continuation,
and the physical omission transition. It also is not a planar Euclidean
`CounterexampleData` with the MEC-derived `SurplusCapPacket S` and
`D.Minimal`.

There is a second regression boundary. The exact rational 26-point model in
`scratch/atail-force/full-parent-t0t1-exact-model/REPORT.md` already includes
strict convex/MEC cap geometry, the physical exact-five shell, all three
complete reverse rows, source-faithful T0/T1, the retained frontier metric
split, the unused critical row, and both continuation sources. All three
reverse outside pairs remain non-co-radial from the first apex. Consequently
none of that local surface can prove the occurrence.

That fixture is still not an `F/R/B/L` model. An exact `Fraction` replay of
its stored coordinates gives:

```text
centers with a K4 radius class       = o, left, c0, c1, c2, right
centers with no K4 radius class      = 20
sources with no possible critical
  blocker anywhere in the carrier   = 13
```

The thirteen sources include both stored frontier points `t0i0` and `t0i1`.
For each possible carrier center, the replay enumerated every exact squared
distance class and tested both requirements of the production source row:

1. an exact four-point radius class containing the source; and
2. after deleting the source, no radius class of cardinality at least four at
   that center.

No center works for either frontier source. Thus even the single production
instance

```lean
H.selectedAt F₀.pair.q F₀.pair.q_mem_A
H.no_qfree_at F₀.pair.q F₀.pair.q_mem_A
```

already excludes this exact cell. This is stronger route evidence than merely
noting that the fixture did not encode a total `H`: the omitted frontier-row
instance is exactly false on the stored rational carrier.

## 3. Comparison of the four omitted sources of force

### 3.1 Planar Euclidean rank two

This is genuinely absent from a generic circular-split metric. Production
already exploits it in
`RetainedCollisionCapLocalization.actualRow_center_eq_commonBlocker_of_contains_collisionSources`:
once an actual selected row contains a prescribed distinct pair already
equidistant from two known centers, `Dumitrescu.perpBisector_apex_bound
D.convex` identifies the new center. Its direct wrapper
`false_of_distinct_actualRow_contains_collisionSources` is checked.

However, rank two does not itself produce the positive pair-in-row incidence.
On the current all-reverse surface the terminal already has one same-cap row
center and needs only the first-apex equality. Requiring a third row through
the pair would be a broader producer than the existing co-radial occurrence.
Thus rank two is a valid downstream consumer, not the smallest missing
producer hypothesis.

### 3.2 MEC and cap geometry

This is also absent from the counterfamily and is retained in `S` through its
nonobtuse MEC support triangle and cap partition. Production already uses it
to prove:

- exact-two outside residuals for transition rows;
- shared physical-cap order and strict straddling;
- the exact-six all-reverse contradiction; and
- `false_of_transitionReverseOutsidePair_coRadial_firstApex` via
  `CapSelectedRowCounting.outsidePair_unique_capCenter`.

For a second physical cap of cardinality at least seven, the existing cap
order leaves enough positions for the period-three source/blocker straddles.
It does not force either member of a reverse outside pair into a particular
first-apex radius class. Cap cardinality and order alone therefore do not
produce the open occurrence.

### 3.3 Frontier-source critical provenance: the selected missing layer

The smallest **verified cell-excluding** parent feature positioned upstream
of checked consumers is the actual deletion-critical row at one frontier
source, as supplied by the total `CriticalShellSystem H`. The local 26-point
cell already fails this one instance; no appeal to all unrepresented rows is
needed to reject that particular fixture.

The full parent does not contain unrelated four-point rows. The same `H`
indexes all of the following:

- `F : CriticalPairFrontier D S firstRadius H`;
- `R : FrontierCommonDeletionParentResidual F`;
- `A.transition : PhysicalActualCriticalOmissionTransition H profile`;
- the origin-tagged continuation inside `A`; and
- every exact selected support and deletion-critical blocker used by the
  all-reverse relation.

In particular, `F.pair.q` and `F.pair.w` are distinct members of one exact
first-apex radius marginal by
`F.pair.q_mem_marginal`, `F.pair.w_mem_marginal`, and
`F.pair.q_ne_w`. Hence production already knows

```lean
dist S.oppApex1 F.pair.q = dist S.oppApex1 F.pair.w.
```

The missing content is not a new equality theorem. It is a positive
source-indexed return from one transition reverse outside pair to a common
first-apex radius class -- most concretely, to this already co-radial
frontier pair, or to another complete `SelectedClass` at the first apex.

A sharply testable internal producer would be:

```lean
-- PROPOSED; NOT PROVED.
theorem exists_transitionReverseOutsidePair_contains_frontierPair
    {D : CounterexampleData} {S : SurplusCapPacket D.A}
    {firstRadius : ℝ} {H : CriticalShellSystem D.A}
    {F₀ : CriticalPairFrontier D S firstRadius H}
    {R : FrontierCommonDeletionParentResidual F₀}
    {B : FrontierBiApexRobustResidual R}
    {L : FrontierLargeOppositeCapsBiApexRobustResidual B}
    {profile : LargeCapUniqueFiveSecondApexRadius D S}
    {continuation :
      LargeCapUniqueFiveTwoCommonDeletionSources D S H profile}
    (A : FullParentExactFiveAllReverseData L profile continuation) :
    ∃ q : PhysicalVertex profile,
      F₀.pair.q ∈ transitionReverseOutsidePair A.transition q ∧
      F₀.pair.w ∈ transitionReverseOutsidePair A.transition q
```

The two memberships, `F₀.pair.q_ne_w`, and the two marginal equalities
immediately construct
`FirstApexCoRadialTransitionReversePairOccurrence A`; its existing consumer
then gives `False`. This proposed producer is intentionally concrete enough
to falsify against future models. It may be too strong; if so, the correct
weakening is exactly the current occurrence (some reverse pair in some
first-apex radius class), not another anonymous row packet.

The theorem must visibly use the common `H` provenance. A proof that inspects
only the abstract transition row table and Kalmanson order is refuted by the
counterfamily and the existing all-reverse survivor banks.

There is an exact checked first split at this frontier row:

```lean
ATailCriticalPairFrontier.cross_deletion_survives_or_cross_membership F₀.pair
```

It returns either survival after deleting `F₀.pair.w` at the actual
blocker of `F₀.pair.q`, or the positive incidence

```lean
F₀.pair.w ∈
  (H.selectedAt F₀.pair.q F₀.pair.q_mem_A)
    .toCriticalFourShell.support.
```

The positive arm feeds production consumers that the 26-point cell cannot
instantiate:

- `signedArea_product_neg_of_cross_membership` gives the exact opposite-side
  placement of the frontier pair around the first apex and actual blocker;
- `ATailThirdCenterCommonPair.false_of_thirdActualCenter_selectedRow_contains_frontierPair`
  gives direct `False` as soon as a third distinct actual center's row
  contains the pair; and
- `ATailThirdCenterCommonPair.cardFour_or_frontierDeletionSurvival_or_actualBlocker_eq_knownCenter`
  classifies every source of the total `H` by the two frontier deletions or
  equality with one of the two known common-pair centers.

Therefore a proof attempt should not jump directly from the local T0/T1
surface to the reverse-pair occurrence. It should first split the actual
frontier-source critical row, then use the total map/global K4/minimality to
close the cross-survival arm or produce the third-center antecedent in the
cross-membership arm. This visibly consumes the first hypothesis known to
exclude the local exact cell and lands on an existing checked terminal.

### 3.4 Global minimality

`R.minimal : D.Minimal` is absent from the counterfamily. Production
`GlobalMinimalDeletion.lean` yields
`exists_global_cardMinimal_blocking_subdeletion` and
`exists_fresh_sharedRadiusPair_or_minimalDeletionCore`.

Those are not current terminal surfaces. No checked theorem converts the
fresh-center or `MinimalDeletionCore` arm into a transition reverse-pair
co-radiality, a same-cap shared pair, or direct `False`. Minimality remains a
possible source of the common-`H` return, but it is not the smallest immediate
producer interface and should not be split off as another nonterminal
adapter.

### 3.5 Why `D.K4` is not selected as the interface

The same exact replay shows that `D.K4` itself excludes the 26-point cell:
twenty carrier points have no four-point radius class. It is logically the
smallest global live fact falsified by that fixture.

It is not the best theorem-development interface. `D.K4` at an anonymous
unrepresented center produces another witness circle but no prescribed
connection to the frontier pair or a transition reverse pair. In contrast,
the `H.selectedAt F₀.pair.q` / `H.no_qfree_at F₀.pair.q` instance is also
false in the fixture and immediately feeds the checked
cross-survival/cross-membership split above. Accordingly:

- use `D.K4` to obtain any necessary global witness rows;
- keep those rows source-indexed; and
- route them through the frontier-source `H` split or directly into an
  existing terminal, rather than presenting bare global K4 as another
  nonterminal packet.

## 4. Concrete public theorem signature

The public theorem should retain the full parent and conclude `False`
directly:

```lean
-- PROPOSED; NOT PROVED.
theorem false_of_largeOppositeCapsBiApexRobust
    {D : CounterexampleData} {S : SurplusCapPacket D.A} {radius : ℝ}
    {H : CriticalShellSystem D.A}
    {F : CriticalPairFrontier D S radius H}
    {R : FrontierCommonDeletionParentResidual F}
    {B : FrontierBiApexRobustResidual R}
    (L : FrontierLargeOppositeCapsBiApexRobustResidual B) :
    False
```

This signature contains no new hypothesis. It preserves exactly the current
production fields:

- `F.pair`, `F.firstApexSplit`, `F.secondApexDouble`, and
  `F.secondApexSplit`;
- `R.minimal`, `R.noM44`, `R.carrier_card_gt_nine`,
  `R.frontierRadius_class_card_ge_four`, and `R.common.firstApexDouble`;
- first-apex robustness from
  `R.firstApexFullyDeletionRobust` and second-apex robustness from
  `B.secondApex_robust`; and
- both cap-six bounds from `L.firstOppCap_card_ge_six` and
  `L.secondOppCap_card_ge_six`.

The intended implementation can use the existing parent classifier. On an
exact-five all-reverse child with cap card at least seven, its first internal
step is `cross_deletion_survives_or_cross_membership F.pair`. The proof must
then consume global data absent from the regressions to derive either the
common-`H` occurrence above, the third-center antecedent, or direct `False`.
On exact six it calls the checked direct contradiction. Other classifier
children retain their own existing consumers.

## 5. The mutual sibling remains independent

Yes. `FullParentExactFiveMutualData L profile` contains the origin-tagged
continuation and an arbitrary
`PhysicalActualCriticalMutualOmissionPair H profile`; it does not contain an
`A.transition`, the all-reverse hypothesis, or a reverse outside pair.

Production makes the separation explicit in
`false_of_fullParentExactFive_of_relationConsumers`, whose inputs are
independently:

```lean
FullParentExactFiveMutualData L profile → False
```

and an occurrence producer for every `FullParentExactFiveAllReverseData`.
Likewise, the exact-six theorem closes only the all-reverse child and keeps a
`mutualFalse` input. Therefore proving the all-reverse occurrence does not
close the mutual sibling. Only the public direct theorem
`false_of_largeOppositeCapsBiApexRobust L`, proved without routing through
that relation split, could bypass both simultaneously.

The best current mutual normal form is the checked scratch blocker dichotomy:
distinct blockers give two source-faithful common-deletion packets for one
deleted source; equal blockers give a production
`FrontierCommonDeletionCriticalFiber`. Neither arm currently supplies an
existing terminal. It should remain a separate parent closer, not be silently
folded into the all-reverse occurrence task.

## 6. Decision

1. Keep `false_of_largeOppositeCapsBiApexRobust L` as the sole public target.
2. Do not redo transition-global, exact-two, source-anchor, exact-six, or
   co-radial-terminal work.
3. For the high-card all-reverse child, begin with the actual
   `cross_deletion_survives_or_cross_membership F₀.pair` split. In the
   positive arm, use the total `H` classifier and the existing third-center
   terminal; in the survival arm, use global K4/minimality to obtain a
   source-indexed return. The explicit frontier-pair containment producer is
   the first concrete occurrence test, not an assumed consequence of T0/T1.
4. Reject any proposed proof whose live hypotheses can be projected to only
   the circular-split row table plus strict Kalmanson order.
5. Keep `FullParentExactFiveMutualData → False` as an independent closer
   unless a genuinely direct full-parent proof bypasses the split.

## Epistemic ledger

- Even-card circular-split counterfamily and refutation of the generic
  contour theorem: **EXACT / PROVED IN THE SCRATCH REPORT**.
- Transition-global split, exact-two pair API, source anchoring, co-radial
  terminal, and exact-six all-reverse contradiction: **PRODUCTION / CHECKED**.
- Common `H` is absent from the counterfamily and retained through the full
  parent exact-five assembly: **SOURCE-CURRENT**.
- Twenty centers in the exact 26-point cell fail K4, and thirteen sources --
  including both frontier points -- admit no deletion-critical exact-four
  blocker in that carrier: **EXACT RATIONAL REPLAY**.
- One actual frontier-source `H.selectedAt` / `H.no_qfree_at` instance is the
  smallest audited production feature that both excludes that cell and feeds
  checked frontier-pair consumers: **EXACT REPLAY PLUS SOURCE-CURRENT
  CONSUMER MATCH**.
- Common `H` plus the full parent forces the proposed frontier-pair
  containment: **OPEN; NOT CLAIMED**.
- `false_of_largeOppositeCapsBiApexRobust L`: **OPEN; PROPOSED PUBLIC
  SIGNATURE**.
- Arbitrary mutual-omission sibling: **OPEN AND INDEPENDENT OF THE
  ALL-REVERSE OCCURRENCE**.
- Production `sorry` closed by this audit: **NONE**.
