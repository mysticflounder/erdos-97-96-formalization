# P97 counterexample-search lane — session handoff (2026-07-28)

Author: orchestrator session ending 2026-07-28. Successor: read this
top to bottom before touching the lane. Governing docs:
`docs/p97-counterexample-search-design-2026-07-28.md` (design),
`census/p97_search/PHASE2-SPEC.md` (census spec, incl. §4.4
cut-matrix amendment), `fr-pattern-lemma1.md` (patterns + ledger).

## Successor compute-lane update (2026-07-28)

This section supersedes the P3/P4 items in §§2 and 4 below; the older text is
retained as the state at the original handoff.

- Full source P3 is PROVEN, independently CERTIFIED, and production ADMITTED.
  The formerly open R2 row-case is closed in both distinct- and merged-inner
  cases by exact angle contradictions. See `p3-r2-compute/RESULTS.md`,
  `p3-r2-compute/SKEPTIC-2026-07-28.md`, and
  `fr-pattern-p3-proof-draft.md`.
- Source P4 remains ADMITTED as R-P4. The separately certified auxiliary
  `B_col` family is now ADMITTED as R-P4-B-COL, but every occurrence contains
  R-P2-A, so it adds provenance and diagnostics but zero incremental pruning.
- Fishburn--Reeds Lemma 2 / Theorem 3 has a direct certificate-backed
  replacement for the paper's Table-2 argument: all 56
  transpose-normalized cases with `alpha+beta <= 19` and minimum row/column
  degree three are UNSAT, with every DRAT accepted by `drat-trim` and the
  result independently audited. It is not Lean-kernel checked. See
  `theorem3-table2/AUDIT.md` and
  `theorem3-table2/LEMMA2-SKEPTIC-2026-07-28.md`.
- Do not import the printed Table-2 values as a verified bank. The independent
  source-pattern encoder certifies `g_candidate(6,8)=19`; the paper prints 18.
  This discrepancy does not affect the direct Lemma-2 sweep.
- Both census control suites pass after the full P3 and B_col admissions.
  Theorem 3 is exposed as the validated diagnostic
  `fr_theorem3_dense_small`, not a separate production rule: logically it is
  subsumed by R-P1--R-P4 and therefore adds no new pruning. It remains outside
  `ALL_RULES`, the persistent bank hash, and `prune_cut_matrix`; a future
  early-exit API is appropriate only for a consumer that does not need full
  fired-rule attribution.

## 0. Operating constraints (non-negotiable, learned/confirmed this session)

- **Model tier on every dispatch.** Subagents inherit the session
  model if none is set. ALWAYS pass an explicit lower tier (sonnet,
  or haiku for mechanical work) for provers, skeptics, implementers.
  A week of Fable quota was consumed by omitting this. Top-tier
  effort is for the orchestrator's own soundness design/audit only.
- Every dispatch carries a HARD CONSTRAINTS header: no subagents,
  `uv run python` for all python, INCREMENTAL file writes (an agent
  lost its entire output to the 64k output cap composing one giant
  response), short text responses, session-scratchpad for scripts.
- Orchestrator designs all soundness-critical encodings and audits
  every rule admission personally; validate every agent deliverable
  before committing. Skeptic audit required before any proof is
  marked AUDITED or any pattern admitted as a rule.
- Commits: message ≤ 16 lines, written to a file, `git commit -F`
  (no heredocs). Commit + push freely. No deep-thinker queries.
- FR paper PDF stays OUT of the public repo.
- Kernel `proof-blueprint axioms` is the gate for any Lean theorem a
  PUBLISHED claim cites (source scans don't count).

## 1. State: landed and validated (all pushed, branch main)

Session commits: f1e7b673 (FR-CERT exact certification + C1b
hull-order certificate), ce743cf3 (Phase-2 census core validated),
bb2a9d17 (R-CAPGE4 kernel gate SATISFIED), 0b9fe328 (doc sync),
8c34c7e5 (P2 draft + C2 pin), 6e793844 (P2 audited + R-P1/R-P2
admitted + FR-matrix symmetrization correction), 59bd0d13
(blocker-annotation existence note).

Established facts a successor must NOT re-derive differently:

- **C2 convention pin (load-bearing).** FR cut matrices are read in
  the OPPOSED convention: rows ascending meet columns in decreasing
  index order. Under aligned C1, P2 variant A is REALIZABLE (exact
  witness; `p2_exact_witness_certificate.py`). Evidence hierarchy:
  C1b hull-order certificate (`fr-certify/`, hull order B1..B10,
  A10..A1 = C2), witness, matrix scans. All of P3/P4 work is C2-only.
- **The FR 20-gon cut matrix is SYMMETRIC: 30 ones.** Unordered pair
  {i,j} gives BOTH A_iB_j and A_jB_i. An earlier committed scan used
  a 15-one half-matrix — corrected in 6e793844. Corrected scans:
  native C2 has ZERO P1/P2/P3/P4 occurrences; the C1 misreading has
  16 P2-B and 210-per-orientation P3 occurrences, zero P4 (k=3..5) in
  both readings (so P4's convention evidence rests on P2/P3 + C1b).
- **P1, P2: PROVEN + AUDITED** (ledger in `fr-pattern-lemma1.md`).
  P2's audit gap (patched): Lemma R's restriction-order bracket needs
  L1 for adjacent-in-P consecutive-in-subset pairs, L2(iii) for
  non-adjacent — check any new proof reusing Lemma R for this.
- **R-P1/R-P2 ADMITTED** as cut-matrix-domain census rules
  (PHASE2-SPEC §4.4, `rules.py`, `prune_cut_matrix`). Semantics
  contract: matrices are same-distance, C2-read, convex, contiguous
  cut; CONSUMER discharges those hypotheses; NO shell-semantics
  (per-point radius) generalization is admitted — a pattern among
  mixed-radius cells is NOT killed by the audited theorems. Rule-bank
  hash changed (no production banks existed). Both control suites
  green: `uv run python census/p97_search/controls.py` and
  `controls2.py` (14 gates incl. G-CUTPAT).
- **R-CAPGE4 kernel gate SATISFIED**: `nonempty_surplusCapPacket_of_K4`,
  `capTriple_caps_card_ge_four`, `Problem97.SurplusCapPacket.capSum`
  (NOT `Problem97.MEC.SurplusCapPacket.capSum`) each report exactly
  {propext, Classical.choice, Quot.sound}.
- **No Lean sorrys were closed this session**; the lane is
  infrastructure, not spine work.

## 2. In flight at handoff

1. **P3 prover (SONNET)** → `fr-pattern-p3-proof-draft.md`. Dispatch
   included: C2-only, patched-Lemma-R warning, scan evidence (native
   0 / C1 210 per orientation), wedge+isosceles toolkit pointer,
   probes-first with full supporting-line predicate (NOT turn signs —
   star-polygon pitfall, P2 draft §7).
2. **P4 prover (SONNET)** → `fr-pattern-p4-proof-draft.md`. Dispatch
   included: C2 restricted order (ρ₁..ρ_k, c_k..c_1), all k ≥ 3, both
   variants, two-perfect-matchings/crossing-chords route suggestion,
   k = 3 first, FR-scan note (P4 non-discriminating).
   (Two earlier Fable runs of these were stopped for quota reasons;
   no partial drafts existed.)
3. **Kernel re-check DONE (2026-07-28, this session)**:
   `proof-blueprint axioms Problem97.counterexample_card_ge_nine`
   reports exactly {propext, Classical.choice, Quot.sound} — cite
   this fresh record (not the stale 07-13 one) when applying F6 to
   `blocker-annotation-existence.md` §5.2/§6.

**On each prover notification:** read the draft; verify convention is
C2 and Lemma R reuse is the patched form; spot-check the numerics
(rerun scripts); then dispatch math-skeptic ON SONNET with an attack
surface; apply its patches verbatim after verifying each; update the
`fr-pattern-lemma1.md` ledger; only then admit as R-P3/R-P4 following
the §4.4 pattern exactly (spec amendment first → rules.py → G-CUTPAT
extension or sibling gate → both suites green → RESULTS.md + design
doc sync → commit ≤16 lines).

## 3. Blocker-annotation existence note — audit returned, patches PENDING

`blocker-annotation-existence.md` (59bd0d13) — math-skeptic verdict
NEEDS WORK, core math sound (E1, Lemma D, D9 verified against
source). Apply these before the note is citable; verify each against
the code first (paths given):

- **F4 (blocking, Theorem C proof line "pick any witness system S")**:
  exact cells admit only |S[p]| == k nodes (`cells.py`), so an
  arbitrary ≥4-class system may not be covered. Patch: pick S with
  |S(y)| = 4 for every y (any 4-element subset of the witness class is
  still one circle centered y); Lemma D applies to any witness system,
  so minimality still forces min-indeg ≥ 1 under THIS system, whose
  node lands in the exact cell. Rewrite scope note 2: "the exact-4
  choice is covered; exact cells suffice" (drop "any choice is
  covered").
- **F7 (blocking, k-generality bullet)**: `card_ge_five_of_K4` is
  HARD-CODED k=4 (`Counting.lean:27`), not k-general; the k-general
  counting floor is k+1 (= 4 at k = 3, not 5). Patch: replace "5 in
  place of 9" with the k+1 floor and state a k=3 floor must be proven
  (trivial) before the k=3 composition is used.
- **F6 (blocking, kernel citation)**: the design doc's kernel list
  covers the three FRAME theorems only, not the floor. Cite the
  actual floor record (`docs/97-rvol-full-prose-proof-2026-07-13.md:219`,
  stale) AND the fresh re-run (background task above). Fix "two
  banked Lean theorems" → three frame declarations + floor.
- **F1 (blocking, semantics pin)**: the note discharges BARE-B1
  existence only. Add standing rule: no future annotated-node rule
  may assume more than B1 without a new existence proof for the
  richer semantics ("intended geometric semantics" in the R-FIBER4
  caveat is broader than what was proved). Soften §5.1 "superseded"
  accordingly.
- **F5**: §5.3 sentence — profiled cells currently admit cap-annotated
  objects only (`iterate.py` raises on cap+blocker); if Phase-3
  profiled encodings use blocker structure, a combined mode must be
  added first.
- **F8**: §1 "CONJECTURED at fixed n" → "at fixed n > 9" (D9 proves
  n = 9).
- **F9**: "census cells enumerate all canonical B1-annotatable nodes"
  is a REQUIREMENT on coverage, not a current property (Phase-2
  generators are partial; completeness is Phase 3). Rephrase.
- **F2 nit**: Lemma D's "A′ ≠ ∅ since |A| ≥ 9" → use |A| ≥ 5 from one
  point + its ≥4 shell (decouples Lemma D from k=4).

After patches: re-audit is NOT required (skeptic supplied exact
patches and verified the math); mark the note AUDITED-WITH-PATCHES
with the audit date, update design doc §8 open-items (existence lemma
→ resolved-by-composition, pending only the F6 kernel re-run), and
the R-FIBER4 citation may reference the note.

## 4. Queue after the above (in order)

1. R-P3/R-P4 admission (blocked on prover drafts + audits).
2. A iter-3 inT(oth)=T — computational-closure-plan lane
   (`docs/computational-closure-plan-2026-07-28.md`); REREAD the plan
   doc first, this handoff carries no state for it.
3. Session 3 of that plan: D-R, D-E, B.
4. Lean follow-up (well-scoped, dispatchable to sonnet):
   `K4_delete_of_indeg_zero` + Corollary D9 formalization per
   existence note §5.3.
5. Phase 3 design (orchestrator work): per-cell enumeration with SAT
   encodings + realization arm; cut-matrix rule consumers must
   discharge the §4.4 semantics contract; profiled+blocker combined
   mode decision (F5).

## 5. File map (lane-relevant, this session)

- `scratch/p97-search-lane/fr-pattern-lemma1.md` — patterns,
  transcriptions, C2 pin, P1 proof, STATUS LEDGER (source of truth).
- `scratch/p97-search-lane/fr-pattern-p2-proof-draft.md` — audited P2
  proof + toolkit L1–L5 (reused by P3/P4).
- `scratch/p97-search-lane/p2_convention_checks.py` — corrected
  symmetric-matrix scans (P1/P2/P4 both readings).
- `scratch/p97-search-lane/p2_exact_witness_certificate.py` — exact
  rational-interval certificate for the C1 witness (audit-supplied).
- `scratch/p97-search-lane/p2-probes/` — prover probe scripts (F5 of
  the P2 audit).
- `scratch/p97-search-lane/blocker-annotation-existence.md` — Lemma
  D + Theorem C; patches pending (§3 above).
- `scratch/p97-search-lane/fr-certify/` — exact FR certification
  (RESULTS.md has the audit trail; C1b is the hull-order pin).
- `census/p97_search/` — Phase-1/2 core + §4.4 amendment; run both
  control suites after ANY edit there.
- `docs/p97-counterexample-search-design-2026-07-28.md` — design +
  status (§8 current as of 6e793844 except existence-note patches).
