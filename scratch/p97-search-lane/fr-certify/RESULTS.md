# FR 20-gon exact certification -- run results

Per `SPEC.md` v1.0 Sec 5. Run order: `smoke.py` then `certify.py`, both
via `uv run python scratch/p97-search-lane/fr-certify/<script>.py` from
the repo root. Both exit 0 on success (verified 2026-07-28).

## Gate table

| Gate | Result | Evidence |
|---|---|---|
| G-DYADIC | PASS | 300 random-rational-triple trials at P=128 (add/sub/mul/neg, both association orders, plus round_down/round_up outward-direction check): 0 failures. |
| G-POS | PASS | Two-circle system (x^2+y^2=1, (x-1)^2+y^2=1), seeded near (1/2, sqrt(3)/2); Krawczyk certifies a box at P=128. Exact rational bracketing: x-interval contains 1/2 exactly; y-interval satisfies y_lo>0, y_lo^2 < 3/4 < y_hi^2 (both exact Fraction comparisons) -- box provably contains the true algebraic root. |
| G-NEG | PASS | Same seed/Y, F1 shifted to x^2+y^2=11 (no real intersection with the second circle near the seed): Krawczyk correctly FAILS to certify (`krawczyk_certified: False`). |
| G-SING | PASS | System x^2=0, y-x=0; Jacobian at the root (0,0) is [[0,0],[-1,1]], det=0 exactly -- confirmed no true rational inverse exists. Fed an arbitrary substitute Y (identity); Krawczyk correctly FAILS to certify (`krawczyk_certified_with_arbitrary_Y: False`) -- no false certificate at a singular root. |
| **FR-CERT** | **PASS** | P=128 (no escalation to P=256 needed). See below for full numeric evidence. |

## FR-CERT numeric evidence

- **Precision**: P = 128 bits (first attempt; no retry needed).
- **Producer**: mpmath, 80 decimal digits, Newton from the Table-1 seed
  (y-shifted so seed y1 = 0), 25 max iterations. Residual after
  convergence: `1.4930226845378142131572763842062584367438431395959890562273632064224712022078859e-49`
  (well under the spec's 1e-40 target; converged well before the
  iteration cap).
- **Box radius**: r = 1/2^100 = 7.888609052210118e-31 (matches spec's
  suggested ~1e-30).
- **Krawczyk contraction margin** (min over all 15 free coordinates,
  both endpoints): lo-margin = hi-margin =
  `268435441/340282366920938463463374607431768211456` ≈
  **7.8886086113997365e-31** -- i.e. K(X) contracts to within
  ~1 part in 10^-9 of r itself relative to X's boundary; the Krawczyk
  image K(X) is essentially a single point at the certified root,
  confirming a strong (not marginal) contraction on this box.
- **C1 (local convexity, all 20 cross products)**: PASS. Orientation
  sign hint = +1 (all 20 consecutive-triple cross products verified
  strictly positive over the whole box). Min margin (worst-case
  cross-product lower bound) ≈ **3.9110146602159386e-08**.
- **C1b (hull order, 360 halfplane checks)**: PASS (added in the
  orchestrator validation audit — see below). Every vertex strictly on
  the positive side of every directed edge of the 20-cycle; min margin
  ≈ **3.911014660e-08** (binding case: the near-coincident vertex
  triples, same as C1).
- **C2 (pairwise distinctness, 190 pairs)**: PASS, 0 failures.
- **C3 (x_i > 0 for i=1..10)**: PASS. Min margin (smallest x_i lower
  bound, at x10) ≈ **0.39044092599284125**.
- **C4 (degree-3 combinatorics)**: PASS. Every index 1..10 has exactly
  3 incident pairs in the checker's own (independently transcribed)
  15-pair edge list; 15 edges total.
- **K3-witness property**: not an interval check (SPEC.md Sec 3.3) --
  recorded as an argument string in `certificate.json`
  (`k3_witness_argument`): the certified root v* satisfies all 15
  equations E_ij(v*) = 0 EXACTLY, so each of the 20 vertices has AT
  LEAST 3 exact unit-distance partners by construction (accidental
  extra unit distances not excluded; harmless for the >= 3 witness
  property).
- Full box, K(X) image, Y hint, and margins are in
  `certificate.json` (generated by `certify.py`, not hand-written).

## Independence cross-check

`system.py` (producer-only) and `krawczyk.py` (checker, independent
transcription) were typed as two separate hardcoded copies of the
Table-1 rationals and the 15-pair edge list, per SPEC.md Sec 3.4. Both
copies were checked programmatically post-hoc and agree exactly
(same gamma_3..gamma_6, same edge list) -- this is expected (both cite
the same published source) and confirms neither transcription has a
typo relative to the other; independence here is about code-path
separation (the checker never imports `system.py` or `producer.py`),
not intentional numeric divergence.

## STOP-reported ambiguities

None. The chart, fixed rationals, edge list, and side conditions in
`SPEC.md` v1.0 were unambiguous as written; no soundness-relevant
family required a STOP.

## Orchestrator validation audit (2026-07-28)

Independent validation pass over the implementer's work:

- **Code audit (trusted core)**: `dyadic.py` outward rounding uses
  exact `floor`/`ceil` on Fractions (no float path); every constructor
  re-rounds outward; sub/mul endpoint logic correct; Jacobian
  accumulation handles the shared-variable rows (y4..y7 on y3)
  soundly. Independence rule verified from imports: checker imports
  only `dyadic`; `producer`/`system` are quarantined; `certify.py`
  takes only (m, r, Y, orientation_sign, residual-as-metadata) from
  the producer.
- **Transcription**: `krawczyk.py`'s Table-1 strings and 15-pair edge
  list checked against `fishburn-reeds-notes.md` — exact match.
- **Re-runs**: `smoke.py` (all 4 controls) and `certify.py` re-run
  from a clean `__pycache__`; both exit 0, FR-CERT PASS at P=128.
- **Negative probes** (all behaved correctly): zeroed Y row → FAIL;
  midpoint shifted 1e-6 → FAIL; checker edge list corrupted
  ((1,10)→(1,8)) → FAIL; chart constant gamma_3 perturbed 1e-9 →
  FAIL; clean control re-certifies. A fifth probe (single Y entry
  perturbed 1e-2) still certified — this is theorem-conformant, not a
  bug: Krawczyk holds for ANY preconditioner Y; Y quality affects
  success, never soundness.
- **Finding + fix (C1 gap)**: the original C1 (consecutive-triple
  same-sign) certifies only local convexity — a star traversal also
  passes it, so it did not prove convex position / hull order. Added
  **C1b** (360 strict halfplane checks) to `krawczyk.py`/`certify.py`;
  star-ordered pentagon control confirms C1-logic passes / C1b-logic
  fails on the star. Full pipeline re-run with C1b: PASS.
- **Wording fix**: "exactly 3 partners" → "at least 3" in the
  k3-witness argument (extra accidental unit distances are not
  excluded by the certificate).

Verdict: FR-CERT **CONFIRMED** (with the C1b strengthening) — the
Fishburn–Reeds 20-gon exists exactly as a strictly convex 20-gon in
hull order B1..B10, A10..A1 with (at least) the 15 stated unit
distances, each vertex having >= 3 exact unit partners across the cut.
