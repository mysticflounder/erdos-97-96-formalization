# Non-promotable draft artifact

This canary was produced before the structural CEGAR journal bound its detector
implementation and validator dependencies through an authenticated detector
contract manifest.  Its records therefore do not satisfy the current replay
contract, even though the draft used the same top-level schema name.

Do not use this directory for coverage, promotion, or Lean ingress.  The first
current-contract canary is the sibling directory
`exact12-v14-structural-v1-canary-0000-2`.
