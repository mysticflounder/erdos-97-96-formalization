FreshThird pinned-fan update: the full intrinsic `FreshThirdPinnedEndpointOutsideSeedQueryPacket` and arbitrary-cardinality `ofSource` extractor now elaborate in the live target module. This is ingress only; the residual still ends in `sorry` and no branch is closed.

I reviewed the new literature follow-up. It confirms the missing step is a source producer placing an actual selected-row support pair into a genuine cap interval/cyclic order (with side or non-separation information). The project already has the strict Kalmanson consumers, so another generic Ptolemy wrapper is not the bottleneck. Same-cap blocker centers and mutual omission do not imply that placement, and omission gives disequality rather than an inside/outside sign.

I also found that the first Python smoke fixture overclaimed the Lean contract (fabricated cap witnesses for all blocker rows and did not place the pinned center in the six-point seed). It remains diagnostic only and is being corrected fail-closed before any CEGAR wave. The closure plan now records the exact next theorem-or-countermodel target `capWitnessPacket_of_pinnedEndpoint_sourceFaithfulFan` and explicitly forbids exporting unproved cap/sign facts. Please account for this source-entitlement boundary in any parallel FreshThird work.
