# Exact17 source-to-DIMACS semantic audit (2026-08-12)

Scope: read-only trace of `SourceRealization -> SourceModel -> base/C--G CNF -> DIMACS` and the live cap-nine exact-cover consumer.  No Lean build was run.  Indexed Lean search was used before source search.

## Verdict

The earlier objection to `pairIntersectionClauses` was false.  Its one six-literal clause for each point triple is exactly the correct encoding of “the two rows do not share all three points”; three binary clauses would instead forbid every shared point.  The exporter is a direct serialization of the Lean clause list, and no source/DIMACS semantic mismatch was found across the base and C--G/child refinements.  The live exact-cover arm genuinely constructs a `SourceRealization` through the shared geometric landing theorem.  One real production blocker remains: the final exact-cover contradiction is still open (`sorry`).

## Bugs / blockers

1. **No source/DIMACS bug found in pair intersection (corrected finding).**  In `lean/Erdos9796Proof/P97/ATail/BlockerVExactSeventeenSourceCnf.lean:132-137`, for a fixed triple `p1,p2,p3`, the clause is `¬h(c1,p1) ∨ ¬h(c2,p1) ∨ ¬h(c1,p2) ∨ ¬h(c2,p2) ∨ ¬h(c1,p3) ∨ ¬h(c2,p3)`.  Its truth table is false exactly when all six hits are true, i.e. exactly when all three selected points lie in both rows; a direct enumeration of all 64 assignments had the sole false mask `63` (all six bits set).  Since every 3-subset is enumerated, conjunction of these clauses is equivalent to pairwise intersection cardinality at most 2.  The proof at `:327-354` correctly derives one missing hit and then a satisfying negative literal.  Three binary clauses `¬h(c1,pi) ∨ ¬h(c2,pi)` would be too strong: each would forbid even one common point.  The observed 92,480 six-literal DIMACS clauses are therefore expected (`C(17,2)·C(17,3)`), not evidence of a mismatch.

2. **Production closure is still open.**  The live target dispatches the exact-cover branch to `sorry` at `lean/Erdos9796Proof/P97/ATail/FrontierLiveClosure/Rigid221SourceHeavy.lean:11858-11874`; the preceding helper only produces `Nonempty (SourceRealization D.A)`, not `False`.

## Verified invariants

- **Source shape and tables.**  `Label = Fin 17`, `NamedOrder = Fin 2`, region tables and old centers/fixed hits are explicit at `BlockerVExactSeventeenSourceNormalForm.lean:22-59`.  Both position tables and inverse tables are explicit at `:67-127`, with finite inverse regressions.  The orientation embedding uses forward order and reverse `Fin.rev` at `:131-183`.

- **Realization construction is genuine.**  `SourceRealization` carries convex carrier, injective/image/CCW boundary, normalized label index, embedding, and row pullback at `BlockerVExactSeventeenSourceNormalForm.lean:243-258`.  The generic constructor derives injectivity/surjectivity, row cardinality, center exclusion, cover, pair-intersection, old-cap, multiplicity, next-center, and physical constraints before assembling the structure at `:432-609`; the geometric exact-cover adapter propagates exact-cover `Nodup`/coverage into this constructor at `:710-828`.

- **Live exact-cover hypotheses are wired to the constructor.**  The live theorem requires `|A|=17`, cap cardinality 9, next-row physical bound, and an exact outside incidence cover at `Rigid221SourceHeavy.lean:11078-11126`.  It defines the four row supports and proves cap intersections/fixed pairs at `:11382-11432`, then calls `exists_sourceRealization_of_geometricExactCover` with the boundary, label embedding, row pullbacks, cap pullback, exact-cover pair, next center, and physical pullback at `:11608-11629`.  Forward and reverse constructions use the two explicit position/orientation branches at `:11630-11712` and `:11713-11808`, and the four named-order cases are exhausted at `:11809-11825`.

- **Base clauses have the expected polarity/cardinality.**  Row at-most/at-least are 5-negative and 14-positive sublist clauses (`SourceCnf.lean:112-122`), giving exactly four with each row; center exclusion and point cover are at `:124-130`; pair-intersection is the six-negative triple clause at `:132-137`; old-cap exact unary membership and outside at-least/at-most are at `:139-158`; exactly-one next center, not-old, source inclusion, and physical at-most-one are at `:160-180`; named order is exactly-one at `:182-185`.  The cumulative append order and per-family dispatch are explicit at `:187-201` and `:562-591`.

- **Variable map and exporter parity.**  The one-based map is `hitVar = 1 + 17*center + point`, next-center 290--306, named-order 307--308 at `SourceCnf.lean:93-110`; `litToDimacs` preserves sign at `:203-206`, and the finite range/injectivity regression is at `:607-611`.  `baseDimacs` and `dimacsString` are direct maps/rendering at `:208-218`.  The extended exporter performs the same direct map from `extendedCnf`, with header from `baseNumVars` and clause length, at `BlockerVExactSeventeenSourceCnfCdefgExport.lean:20-27`.

- **C--G schema guards and orientation enumeration.**  Both directions, both named orders, all 17 cuts, and increasing five-/six-offset choices are enumerated at `SourceCnfCdefg.lean:31-46`; source-position, reverse orientation, consumer-cut formulas, and the exhaustive finite regression are at `:59-108`.  Each schema defines a guarded negative-hit clause (`:172-198`), proves the corresponding source-clean consumer impossible (D `:269-400`; E `:403-529`; C `:532-653`; F `:663-796`; G `:799-990`), and the cumulative C--G append/dispatch is at `:992-1011`.  The guard polarity is correct: if the named order differs, `¬namedOrder` satisfies; if it matches, one listed hit must be false.

## Materialized-DIMACS checks

Read-only checks on `scratch/exact17-lean-to-sat/base.cnf` and `extended-cdefg.cnf` found headers `p cnf 308 209692` and `p cnf 308 2189852`, respectively; SHA-256 values match the route spec (`d5ea7921...d425` and `763bb477...7527`).  Both have 308 distinct variables, all in 1..308, all clauses terminate in `0`, and no empty clause was observed.  The extended file's post-header body has the byte-identical base prefix.  The independent validation record confirms the prefix and all ten schema/order family counts (`scratch/exact17-lean-to-sat/extended-cdefg-validation.json:1-77`).  It is an artifact check rather than a proof of the geometric hypotheses, but it shows no serialization mismatch; the remaining blocker is the live production `sorry`, not DIMACS semantics.
