Assume a nine-point convex-independent set with the four-equidistant property. The shell child constructs the finite endpoint shell. The four first-vertex form children and the cyclic-forms child provide all required escaped-form exclusions. Cap containment then supplies the N4e hypothesis, and the circle-placement child is represented in the imported endpoint graph for the final exhaustion node. Since the first cap has cardinality at least two, choose a point in its interior and apply single-apex exhaustion, obtaining the contradiction.

The solution is an explicit reduction through the nine accepted child theorem interfaces. It does not claim that the statement-only offline mirrors prove those children; their accepted proof submissions are the dependency frontier recorded in `provenance.md`.
