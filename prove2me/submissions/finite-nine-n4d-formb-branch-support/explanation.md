The proof packages the middle part of the existing N4e development for a fixed endpoint shell and
zero-defect cap layout. It imports the accepted `n4e_core` theorem and projects the earlier
selector, normalized-height, cap-side, and coordinate facts from that public support record.

The retained proof slice then establishes the strict long-side inequality, rules out the two
third-cap selector placements needed by the Form `b` split, and proves the two reflection and two
lower-arc contradictions used by the remaining branches. These eight conclusions are returned as
fields of a public record so the final Form `b` submission can use them across the solution-module
boundary.

This is a branch-support theorem. Its conclusion supplies the middle geometric contradictions; it
does not by itself prove that Form `b` is impossible.
