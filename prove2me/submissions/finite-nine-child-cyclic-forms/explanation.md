The proof constructs the authenticated one-step and three-step cyclic relabellings of a finite
endpoint shell. The retained support includes the cyclic Moser triangles, cap triples, packets,
both shift definitions, witness-class and cap-index transport, and all six Form transport
lemmas.

Each transported exclusion calls the corresponding accepted public Form A v1, Form B v1, or
Form C v1 theorem. The second-vertex Form A branch also uses the public Form B v1 theorem on the
twice-shifted shell. Form B v2 is imported as an accepted dependency module because it is part of
the established predecessor frontier. The final `solution` theorem packages the five resulting
exclusions in the target conjunction.
