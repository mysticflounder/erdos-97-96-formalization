The proof extracts the first reusable portion of the existing finite-nine N4e development. It
first constructs the two interior points in each of the three caps and proves that each relevant
witness class has exactly four points. The selector lemmas then describe the possible cap
intersections for witnesses based at the three distinguished vertices.

The remaining fields collect the geometric facts used later in the Form `b` analysis: the strict
opposition of the two cap-order differences, coordinate and trigonometric identities, the
normalized-height argument excluding an extra third-cap point, the two impossible selector
positions, the common-side facts for the second cap, and the similarity transport that normalizes
the base chord. Returning these facts in one public record lets later submissions use the checked
proofs through record projections.

This node is a support result. It does not by itself exclude Form `b`; it supplies the common
selector and geometric chain used by the smaller branch-specific proof nodes.
