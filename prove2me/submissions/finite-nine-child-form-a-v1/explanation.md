The proof rules out Form `a` at the first distinguished vertex of a finite nine-point endpoint
shell. It chooses the accepted N4e core-support record and reconstructs the older unsplit
third-vertex selector shape from the record's public three-case split.

The retained authenticated source handles the endpoint-pair geometry and the final Form `a`
case analysis. Calls to the old shell fields for the second interior-cap cardinality, the first
selector, the second selector split, the second-cap side condition, and the zero-defect layout are
replaced by fields of the accepted support record.

The final source theorem takes a Form-B-at-v2 exclusion. The submitted wrapper supplies that
hypothesis by applying the accepted theorem `Erdos9796FiniteNine.form_b_v2`; its proof is imported
rather than repeated in this source.
