The proof rules out Form `b` at the first distinguished vertex of a finite nine-point endpoint
shell. It chooses the accepted N4e core support record, uses its zero-defect cap layout, and chooses
the accepted N4d Form-B branch record for that layout.

Those records supply the earlier selector decomposition, normalized-height facts, radial-order
facts, and the middle branch contradictions. The retained final source builds the faithful
coordinate frame, closes the lower-selector and equilateral-apex rows, covers the remaining upper
arc cases, proves the final pin clash, and combines the cases in
`N4dExcludesFormB_v1_proof`.

This proof resolves the existing `form_b_v1` theorem node. It adds no new mission milestone or
public definition.
