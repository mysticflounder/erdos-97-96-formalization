This is a direct transfer of the repository's finite-nine Form `c` exclusion at the first
distinguished vertex. It does not introduce a new mathematical argument.

For a finite endpoint shell, Form `c` places the non-vertex representative in the third cap
interior. The proof splits that representative between the two ordered side-chain points. It then
normalizes the relevant third-vertex and second-vertex selector configurations. The first branch is
closed by the upper-arc obstruction; the remaining branches reduce to the coordinate and
pin-clash contradictions already established by the finite-nine geometry layer. Swapping the two
third-cap side-chain points transfers the same exclusions to the second representative.

The shell's three-cap data comes from the accepted counting layer, whose construction formalizes
the cap decomposition attributed in this mission to Adrian Dumitrescu. This Form `c` proof uses
that shell and does not claim separate attribution for its later branch analysis.
