# Finite-nine single-apex exhaustion

The authenticated fragment develops the N8 exclusion through the selected distance
class, endpoint-pair, adjacent-arc, normalized-frame, and same-cap branches. Its final
theorem constructs a selected apex from `hK4` and the cap-interior membership, then
applies `N8k_single_apex_false`.

The source proof used a private `n7_circle_placement` theorem at eight reachable call
sites. The generator replaces each call with
`Erdos9796FiniteNine.circle_placement S hN4e`, the accepted public predecessor. The
earlier seven-call inventory was an undercount; the authenticated fragment digest and
the generator's exact occurrence assertion make the corrected count reproducible.

The generator retains all authenticated support units and preserves the
`set_option maxHeartbeats 1600000 in` scope on `N8k_single_apex_false`. The final
`solution` wrapper applies the retained
`N8k_capInterior_false_of_hasNEquidistantProperty` theorem to the public target's
arguments. No solution module is imported.
