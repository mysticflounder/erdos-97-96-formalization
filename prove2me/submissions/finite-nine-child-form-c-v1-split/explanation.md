The proof follows the checked finite-nine Form C case analysis at the first distinguished vertex.
It obtains the zero-defect cap layout, selector splits, cardinalities, and reusable coordinate facts
from the accepted `Erdos9796FiniteNine.n4e_core` support record.

The retained Form C residue separates the two possible points in the third cap and supplies the two
strict cap-order chains used later. The six authenticated Form C units then close the lower-selector,
upper-arc, reflected-row, and final pin-clash branches. Their final coordinator is the existing
`N4dExcludesFormC_v1_proof`, which supplies the requested theorem.

All uses of facts published by the N4e support theorem go through record projections. The packet
does not import the accepted Form B branch record because no field from that record occurs in this
proof.
