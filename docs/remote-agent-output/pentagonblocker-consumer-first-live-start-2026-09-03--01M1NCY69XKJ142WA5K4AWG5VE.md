# PentagonBlocker consumer-first live start — 2026-09-03

## Target
`Problem97.ATailFrontierLiveClosure.false_of_exactFourRigid221_sourceHeavy_secondOppositeLarge_pentagonBlockerDeleted`

## Corrected architectural boundary
Do **not** pursue a new local midpoint/common-circle contradiction. Historical exact rational realizations show the local pentagon trace + apex-circle/bisector packet is realizable. Current source audit also shows only two known on-physical-circle midpoint/equidistance relations in the Deleted leaf: `beta(u)=xv` gives the `(u,xu)` relation and `beta(xv)=deleted` gives the `(xv,u)` relation. The remaining row blockers are not known to lie on the physical five-circle.

The refactor must therefore be consumer-first:
1. pick an existing source-clean cardinality-generic contradiction consumer;
2. transcribe its exact positive support/order/equality ingress;
3. prove the smallest source adapter from the live PentagonBlockerDeleted hypotheses;
4. only promote the adapter if it is immediately reusable by at least one other live root, preferably `Rigid221Placement` or TwoSource.

## Consumer bank under audit
- `KalmansonThreeEqualitySchemas`
- `KalmansonFourEqualitySchemas`
- `CapCrossingKalmanson`
- `TwoCenterBisectorParity`
- `CocircularEquidistantWitness`
- `GenericRowNogoodCertificate`

## Historical source-clean Pentagon ingredients worth reusing
- exact physical five-cycle row traces;
- `beta(u)=xv`, `beta(xv)=deleted` in the relevant child;
- blocker injectivity results where still available;
- forced second-cap order producers;
- reselected-row physical-triple intersection `<=1` results;
- common-deletion and crossed-row exporters;
- strict second-cap localization and row/class overlap bounds.

These are producers, not terminals. The refactor should compose them into an existing metric consumer rather than introduce another residual packet.

## Hard gate
No helper theorem is accepted unless either:
- it directly closes the publish-reachable PentagonBlockerDeleted root, or
- the exact same neutral occurrence packet is needed by at least two identified live roots.

No new sorry. No local midpoint-cycle theorem. No wrapper-only refactor.

## Active audit request
Luna `radius-scribe` was asked to read the current live theorem, map every available hypothesis to the existing consumer bank, identify the first missing positive support/order/equality premise, and name a second live sorry that can reuse the same neutral occurrence packet. Source edits are forbidden until a consumer-complete route is found.
