# RVOL formalization review — decision summary

The full ranked review has been delivered separately as `rvol-formalization-priority-review-2026-09-02.md`.

## Recommended order

1. **C3 two-radius `cardGeThirteen`** — first prove and wire the exact source `cap_betweenness` ingress, finish `firstOpposite` and larger-card coverage, ingest the accepted proof certificates, and regenerate the obligation registry. This is the strongest candidate for the next actual registry decrement.
2. **Exact-five distinct as a two-leaf closure program** — close `false_of_exactFiveDistinct_threeCenter_distinctFresh_physical`, then close `...fiveIncidence`. The physical-or-hard-source-swap theorem is useful, but a dispatcher alone does not change the registry.
3. **TriApex D1 source-to-planar-core adapter** — reuse the proved safe-pair machinery and extract a concrete equilateral/bisector core or uniform common-radius angle normal form from `J/G/Q`. The complete 111-pattern residue is SAT with all Kalmanson and triangle inequalities, so another order/Farkas-only layer is not recommended.
4. **B1 terminal producer** — falsify, then prove an endpoint-residual theorem yielding either a winning same-arc packet or a third joint deletion. Further pair-counting and blocker-localization wrappers are unlikely to close `false_of_b1PhysicalClassFiveSixNormalForm`.
5. **TwoSource occurrence theorem** — start with `false_of_crossBlockerCoincidence` and prove that its live rows actually realize a six-role/Kalmanson or related geometric core before adding any new generic consumer.

## Parallel infrastructure

Prove the tight equality normal form for

```text
Problem97.CapSelectedRowCounting.orderedCap_card_add_four_le_choose_outside
```

including exact endpoint/interior outside-hit counts, pair-family exhaustion, and the outside-pair owner relation. This is the most important bridge from the RVOL computations to source-derived mathematics, but it has no identified immediate registered consumer yet.

Exporting the positive signed equilateral-angle lemma, the three-monotone-transition contradiction, sharp-turn budget, radial-arc budget, rhombus identity, and three-K2/five-row cycle is worthwhile library work only after a live branch produces the required occurrence.

## Registry guardrail

The frozen authority used in the review records 27 publish-reachable obligations: 16 Rigid221, 1 TriApex, 7 TwoSource, and 3 TwoDeletion. The workspace has advanced beyond that registry snapshot, so any closure claim must regenerate the authority and distinguish an internal-helper reduction from removal of a registered parent.
