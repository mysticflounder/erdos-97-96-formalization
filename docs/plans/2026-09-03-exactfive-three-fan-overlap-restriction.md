# Exact-five three-fan overlap restriction

Date: 2026-09-03

## Objective

Turn the conditional three-fan adapter into a source-level restriction for the
final four-incidence continuation. Given its three retained positive
incidences, prove that either the second-apex center is absent from the first
row, or every point shared by the first and blocker rows belongs to the pair
formed by the retained point and second-apex center.

If the second-apex center is present and a shared point lies outside that pair,
the existing source adapter obtains the three-fan metric pattern and contradicts
convex independence. The conclusion therefore removes the generated role
names, cyclic order, and chirality from this family of alias profiles.

## Reuse preflight

A bounded indexed Lean search found the existing metric-exclusivity theorem,
the four-incidence producer, an intersection theorem in a different fresh-third
packet, and the new three-fan source adapter. None states this first-row/first-
blocker overlap restriction. The immediate consumer is the final constructor
inside `false_of_exactFiveDistinct_threeCenter_distinctFresh_fiveIncidence`.
The first unresolved successor after this theorem is the complementary source
case: second-apex omission from the first row, or intersection contained in the
two named points. The search and plan use lane base `8ea1642c2`.

## Claim boundary

The theorem is a necessary condition on the final continuation branch. It does
not close the three earlier missing-incidence constructors, prove the remaining
condition contradictory, or by itself remove the broad admitted endpoint.
