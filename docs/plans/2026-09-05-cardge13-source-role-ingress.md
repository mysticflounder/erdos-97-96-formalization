# CardGe exact-thirteen source-role ingress

Lane `cardge13-source-role-ingress-20260905` owns the new source-role adapter
and this note.  The adapter will package the actual blocker labels from
`CardGeThirteenExact13DispatchRawPacket.fivePositionalRows`, then expose raw
labels for the two good outside sources and the deleted first-apex point.

Scope is source-role binding only: no solver or certificate production, no
fixed cell claim, and no aggregate ingress.  The immediate consumer is the
conditional direct-z08-k09-10 Boolean root assignment.  The first missing
adapter before this lane is the source-model role record itself.  The blocker
labels are selected from the raw witness and transported to the positional
packet by injectivity, because the two existential constructions are not
definitionally the same choice.
