# CardGeThirteen exact-13 source promotion

Date: 2026-09-05

Target:
`Problem97.ATailFrontierLiveClosure.false_of_exactFourPostCardElevenTwoRadiusBranch_cardGeThirteen`.

## Scope

This checkpoint promotes the source-faithful exact-13 coarse-cell ingress and
its 100-cell catalog together with the finite Boolean/provider-path producer
chain consumed by the authenticated PIQD lanes.  Earlier solver commits pinned
these source bytes by SHA-256 while the files remained untracked; this commit
makes the dependency chain reachable from a clean checkout.

The Lean ingress packages the actual direct or reflected cyclic orientation,
the uncovered label in the second-opposite interval, the exact two-or-three
slice, its complement, and the four source-row intersection bounds.  The
catalog proves ordinary finite membership in exactly 100 cells.  Neither file
asserts that a cell is contradictory.

## Validation

The governed build of
`CardGeThirteenExact13CoarseCellIngress` and
`CardGeThirteenExact13CoarseCellCatalog` completed successfully.  The printed
axiom closures for the ingress existence theorem and catalog cardinality
theorem contain only `propext`, `Classical.choice`, and `Quot.sound`.

The six focused producer test modules report 30 passing tests, and Ruff passes
over all promoted Python source and test files.

The build wrapper's best-effort proof-blueprint resync subsequently failed on
an unrelated stale private declaration in `B1CardSixRoleProjectionIngress`.
Thus the focused kernel build is valid, while a fresh live-spine report remains
pending until that external index mismatch is repaired.

## Claim boundary

This is source and build-custody promotion only.  It does not promote any SAT
assignment to a geometric model, any generated cut bank to a Lean theorem, or
any coarse cell to a contradiction.
