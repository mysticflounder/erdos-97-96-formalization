# Exact collision witness for the adaptive equality core — 2026-09-03

## Result

**PROVEN:** the 13-polynomial equality core is proper and has a real solution
under each of the two authenticated affine gauges. This is a result only about
the equality core. It is not a source realization.

The exact checker imports the completed characteristic-zero producer, reads its
immutable terminal and target artifacts, and reconstructs the two authenticated
target polynomial lists. It never calls the producer's transport-capable run
entry point. A missing or incomplete completed tree is an error. Each existing
target artifact is replayed locally against the rebuilt query. Their ordered
polynomial hashes are

- gauge `z3`: `ee542a9066f8367de4aa877d3464925afdbe988ebee00ee0b7e41a502ec1c7bf`;
- gauge `cvc5`: `1f6d2bead88912f000514f1100988e0e9cca38613ff4e56c8da348ffc944b4e1`.

The checker evaluates all 26 actual generator strings with a closed AST
whitelist and exact arithmetic in

```text
K = Q[z]/(4 z^2 - 3).
```

No call to `eval`, `exec`, a numeric solver, or floating arithmetic is present.
The pair representation `a + b z` reduces each multiplication with
`z^2 = 3/4`. The declared modulus has degree two and reduces to zero in `K`.

## Assignment

For the witness display, write

```text
B:=c000, O:=c001, Q:=c002, A:=c003, D:=c004, S:=c005,
E:=c006, U:=c007, V:=c008, X:=c009, Y:=c010.
```

These uppercase witness labels name quotient classes; they are separate from
the producer's lower-case source-role fields. The common assignment is

```text
B = Q             = (0, 0)
O = U = V = Y     = (1, 0)
A = D = S = E = X = (1/2, z).
```

In class IDs, this is

```text
c000 = c002                         = (0, 0)
c001 = c007 = c008 = c010          = (1, 0)
c003 = c004 = c005 = c006 = c009   = (1/2, z).
```

The first target pins `c000=(0,0)` and `c010=(1,0)`. The second pins
`c000=(0,0)` and `c008=(1,0)`. Thus the same assignment satisfies both anchor
pairs. Every one of the 13 reconstructed generators in each target has exact
remainder `(0,0)` in the pair representation.

The polynomial `4 z^2 - 3` has the two real roots `z=√3/2` and `z=-√3/2`:
squaring either root gives `3/4`, so substitution gives `4(3/4)-3=0`.
Applying either root to the quotient assignment gives a real common zero of
each equality target. A common zero prevents `1` from belonging to the target
ideal. This proves equality-core real feasibility and ideal properness without
depending on the timed-out Singular target computations.

## Why this does not realize the source packet

The witness succeeds by collision. In source-role names, its additional
coordinate identifications include

- the `U/c1` class location with the `c2/q` class location;
- `O` with `u`, `v`, and `y`; and
- `a` with `d`, the `p/s` class, the `t/e` class, and `x`.

Consequently eleven quotient classes occupy only three geometric locations.
The omitted source-required distinctness conditions fail, and strict convex
orientation cannot hold for this collapsed configuration. The witness therefore
does not establish source realization, distinctness, convexity, a Lean
statement, a Lean proof, or promotion. All six of those claims remain false.

Future source-facing computation must add the required noncollision conditions
and orientation constraints. For real coordinates, a squared-distance
nonvanishing condition excludes a collision; the exact source ledger must say
which pairs are required. Signed-area conditions must then enforce the intended
strict orientation. A positive equality-only witness cannot bypass either gate.

## Exact collision-stratum reduction

**PROVEN bookkeeping reduction, limited to this collision stratum:** use the
`z3` gauge and substitute

```text
c002=(0,0),
c001=c007=c008=(1,0),
```

while `c000=(0,0)` and `c010=(1,0)` are already anchored. The remaining ring
coordinates are exactly those of `c003,c004,c005,c006,c009`, hence ten
variables. Number the original 13 target generators in their producer order as
`p1,...,p13`. Direct substitution gives

```text
p4 = p2,   p8 = p5,   p9 = 0,   p10 = 0,   p13 = p11 - p7.
```

Therefore the substituted ideal is generated by the eight retained images

```text
p1, p2, p3, p5, p6, p7, p11, p12.
```

This reduction is safe for replaying or studying the displayed collision
stratum: every removed image is zero or an explicit combination of retained
images. It is not a reduction of the distinct source problem because its
substitutions impose the collisions that source-facing work must exclude.

## Next-work boundary

**CONJECTURED next work:** a `B–O` source-facing gauge, using classes `c000` and
`c001` in the display above, may be a better normalization because this relaxed
witness already places them at distinct locations. Before using that gauge, the
formal source layer must prove that the intended source points satisfy `B ≠ O`
and that the normalization preserves every live source configuration under
review. The proposed gauge is a direction for the next faithful encoding, not a
conclusion of this checker.

## Reproduction

The checker is silent on success:

```bash
PYTHONPATH=. uv run python scripts/check_exactfive_hard_source_swap_adaptive_equality_core_collision_witness.py
```

Pass `--verbose` for one canonical JSON record containing the two anchor checks,
all 26 zero remainders, source hashes, the modulus check, and explicit false
claim flags. Errors are always printed to standard error.
