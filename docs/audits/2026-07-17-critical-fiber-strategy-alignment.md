# A-TAIL critical-fiber strategy alignment audit

Date: 2026-07-17

Status: **CAP-FIRST PHYSICAL-APEX DISPATCH SOURCE-VERIFIED;
`FrontierLargeOppositeCapsBiApexRobustResidual B` IS THE ACTIVE ROBUST
BOUNDARY; `false_of_largeOppositeCapsBiApexRobust L` IS THE PRIMARY DIRECT
MISSING PRODUCER; THE SURVIVAL-COVER/PHYSICAL-ALIGNMENT STAGE ALREADY REDUCES
TO THIS ROBUST ENDPOINT OR PROTECTED SWAPPED FA-UNIQ4; THE FIRST LIVE
EXACT-FIVE SUBPROBLEM IS THE FULL-PARENT ALL-REVERSE CO-RADIAL OCCURRENCE;
NO PRODUCTION
`sorry` CLOSED.**

**2026-07-17 exact-five ownership update.**  The active exact-five subproblem
is the aggregate all-reverse-cycle occurrence theorem at the complete `L`
boundary.  It must produce a canonical reverse outside pair co-radial from
`S.oppApex1` (or the stronger retained-frontier-pair equality) and then call
an existing same-cap/Kalmanson/frontier-return consumer.  The earlier
“occurrence retired” finding remains valid only for the weak selected-row,
finite-marginal, and linear-Kalmanson implications; it does not block a proof
using the full `R/B/L` parent, nonlinear Euclidean/MEC geometry, full-fiber
provenance, or global minimality.  No dedicated bare-`B`, fixed-`H`,
period-three-linear, or conditional-consumer lane remains authorized.

## Decision

Keep `false_of_twoLargeCaps_commonCriticalMap` as the final parent assembler,
called before LIVE-Q/C construction. The source-valid parent reduction runs
the physical cap classifier first. It dispatches the original-first small,
physical-second small, and physical-second critical exits before any frontier
is selected. Only the remaining branch carries

```lean
6 <= S.oppCap1.card
6 <= S.oppCap2.card
FullyDeletionRobustAt D S.oppApex2
```

and proceeds to `exists_criticalPairFrontier_of_K4`. The frontier then splits
through `CriticalPairFrontier.originalUnique_or_commonDeletionParent`: the
unique arm feeds the protected original `FA-UNIQ4/5` consumer, while the
common-deletion arm constructs `R`, then
`B : FrontierBiApexRobustResidual R`. The two cap facts must remain attached
as

```lean
L : FrontierLargeOppositeCapsBiApexRobustResidual B
```

because generic bare `B` admits cap-four and cap-five configurations already
routed by the earlier exits. The primary public producer is

```lean
false_of_largeOppositeCapsBiApexRobust
    (L : FrontierLargeOppositeCapsBiApexRobustResidual B) : False
```

Within its exact-five all-reverse branch, prove one terminal occurrence from
the whole `L` packet.  The preferred weak conclusion is

```text
exists i a b,
  reverseOutsidePair K i = {a,b} and
  dist S.oppApex1 a = dist S.oppApex1 b.
```

The reverse row already gives the second, distinct cap-interior center through
that outside-cap pair, so production
`CapSelectedRowCounting.outsidePair_unique_capCenter` returns `False`.
Production `false_of_two_selected_rows_shared_late_pair` and checked scratch
`false_of_supportCentersBisectFrontierPair` are alternative existing sinks.
Keep the occurrence internal to the parent theorem; a standalone period-three
contradiction or another packet whose producer remains open is not the closure
target.

It must use the two cap bounds and two blocker-map image omissions together
with the original frontier pair, minimality, non-`IsM44`, MEC/cap order, and
full radius filters.

This cap-strengthening is mandatory. The quantifier audit is now resolved:
a fixed-initial-`H` theorem producing `CriticalFiberClosingCore` is
propositionally sufficient but operationally overconstrained and must not
guide mining. If the existing production core consumer supplies the proof,
the honest internal helper chooses a favorable system late:

```lean
exists_lateCriticalFiberClosingCore_of_largeOppositeCapsBiApexRobust L :
  ∃ Hfav : CriticalShellSystem D.A,
    Nonempty (CriticalFiberClosingCore
      (rebaseFrontierCommonDeletionParentResidual R Hfav))
```

Production already rebases the frontier and scratch rebases common deletion;
the `R`/`B`/`L` rebase ladder remains to be promoted or proved if this
optional implementation is used.

The existing 101-survivor run is a fixed fourteen-role
`PROFILE = (8,4,5)` theorem-discovery surface. A replay with both physical
apices forbidden rejects all 101 stored shadows, but no theorem extracts this
profile from an arbitrary live residual. Its physical second opposite cap has
cardinality four, while the live robust endpoint has already routed that case
away and lies in the cap-at-least-six regime. The fresh `UNKNOWN` query and
source-hash drift therefore describe only that proxy, not live coverage.

Production `ATail/BiApexBlockerMultiplicity.lean` now proves the exact finite
consequence: either two distinct blocker values have nontrivial fibers or one
fiber has at least three distinct sources. The initially advertised `Fin 12`
regression is not Kalmanson-clean. In the pinned order `0 < 1 < 2 < 3`, rows 0
and 1 both contain `{2,3}`. After quotienting distance terms by row equalities,
the row-0 strict comparison is a self-loop. An independent full linear audit
extracts the same three-constraint UNSAT core.

Production `false_of_two_selected_rows_shared_late_pair` proves that two-row
four-point cancellation directly. The whole-cyclic-carrier decision described
next was the subsequent regression checkpoint, not the current producer.

That faithful extraction now kernel-checks in
`generic-biapex-kalmanson-search/WholeCarrierBiApexKalmansonSurface.lean` for
arbitrary live cardinality. It supplies the actual boundary, global selected
rows, both blocker omissions, strong connectivity, exact blocker rows,
incidence bounds, and strict Kalmanson inequalities with only the standard
three axioms. Later card-12/card-13 carriers and missed two-cycle transports
show that uniform coverage by the current bank is not the remaining primary
gap. The current producer is the parent-scoped critical-fiber theorem stated
in the active hierarchy below.

`whole-carrier-shared-late-decision/` separately proves that the new two-row
terminal is not an exhaustive producer target: source-valid structural SAT
models exist at card 10 and live card 11 with no cyclic shared-late pair. The
kernel-pinned card-10 model still has a three-row generic ordinal self-loop.
Thus the specialized terminal remains useful.

`whole-carrier-ordinal-coverage/` further provides an independently verified
historical bare-`B` card-11 model with the faithful structural prefix, both blocker omissions,
and all one-row ordinal Kalmanson implications. Exact replay rejects the table
with one Kalmanson inequality and a six-center equality chain. The next
verified card-11 model survives every single Kalmanson inequality modulo the
global equality quotient, but full LRA rejects it by an all-weights-one sum of
four strict inequalities using only three selected rows. Both normalized
consumers are kernel-checked. Thus full multi-inequality Farkas coverage became
the next linear decision target; the corrected audit below now provides its
verified survivor.

The finite decision engine now has a fast, fail-closed direct-CNF/full-LRA
loop, but its first bank used the wrong normalization boundary. Metric cores
retained unused ambient gap vertices, allowing the same active contradiction
to be rediscovered at different spacings. The structural models, LRA
infeasibility results, and cuts remain sound; the raw bank progression `231 ->
262 -> 435` is simply not a count of distinct theorem types. Rank-normalizing
to active roles collapses the 262 checkpoint to 215 transported types; adding
the independently checked interlocking shared-pair terminals gives a corrected
217-type bank. Redundant raw-bank continuations were stopped.

The corrected consumer surface is exact for the audited data. Of 88 genuine
full-LRA schemas, 87 contain a Kalmanson-infeasible simple equality cycle of
length at most seven; the residual is the interlocking order
`A < B < X < C < D < Y`. A complete full-row audit of 203 independently
verified card-11 models finds a still smaller alternate in every case: two
Kalmanson inequalities and three selected-row equalities, with unit
coefficients. This identifies one generic arithmetic consumer. It does not
require a literal 203-theorem bank: exact canonical emission gives 20 six-
membership cuts, 19 new relative to the raw 435 bank, and these subsume
156/435 raw schemas.

The corrected active-role outer problem then refutes the proposed coverage
theorem at card 11. An independently verified model survives all 217 cuts,
contains no K1/K2 collision or coupled interlocking pair, and contains no
Kalmanson-infeasible simple equality cycle of lengths three through seven
after checking 64,753 normalized order types. Its row table also admits an
independently substitution-checked exact rational metric satisfying
positivity, all triangle inequalities, every strict Kalmanson inequality, and
every row equality. Pure Kalmanson/Farkas closure on this surface is therefore
blocked. Here "this surface" is the historical bare-`B`, card-11 whole-
carrier abstraction. It does **not** decide the cap-strengthened live branch:
`L` forces `14 <= D.A.card`, and the first live cap profile is `(5,6,6)`.
For that profile the two physical apices are separated by the four-edge
surplus-cap arc (indices `0` and `4`, up to reversal), so any renewed linear
decision must test that card-14 orbit and state which cap fields it omits.
The first such operational probe is non-decisive: expanding the historical
454-schema bank produced 1,679,938 embedding cuts and ended `UNKNOWN` after
about 303 seconds with zero outer attempts and zero linear checks. This is a
preload-architecture failure, not evidence about the live surface.

The cap-aware successor removes that preload and adds the source-proved
center-cap bounds at the exact normalized `(5,6,6)` profile: seventeen
`|row(center) inter cap| <= 2` inequalities and the six valid endpoint
sharpenings to one. Its independent smoke gates pass. No-bank probes reach 73
new metric-dead order schemas in 30 seconds and 169 in 60 seconds, but both
end `UNKNOWN` under their wall budgets. This is a tooling improvement and a
bounded theorem-discovery checkpoint, not SAT, UNSAT, Euclidean evidence, or
closure. The continuing stream of distinct anonymous cores does not justify
a longer run: the next discovery object must retain the named frontier pair,
the actual physical-apex deletion rows, and common critical-map provenance.
Its public target remains direct
`false_of_largeOppositeCapsBiApexRobust L`; a `CriticalFiberClosingCore` may
be used only internally after choosing a favorable critical system late and
rebasing the complete `R`/`B`/`L` packet.

The subsequent theorem-bank audit isolates the first failed field. The row
cell is already non-realizable in the Euclidean plane: independent canonical
outer replay finds three embeddings
of the five-role front-before-isosceles pattern `WF = WX = WZ`, `PZ = XZ`.
Its generic core and CCW boundary bridge are now production source and are
wired into both `GeneralCarrierBridge` alternatives. The survivor also instantiates
the sibling theorem `u5_qcritical_aux_base_collision_metric_incompatibility`
on rows `0,2,4,8,9`; an exact-name local port independently kernel-checks.
The front-before theorem matches 230/230 stored 555/654 cores, so broad literal
mining is stopped. It supplies a strong bounded consumer and card-11 coverage
schema, but the higher-cardinality replay below shows it is not a uniform live
producer. Broad 555/654 witness mining remains stopped because those exact
profiles have carrier cardinality twelve and are disjoint from `L`. A bounded
card-14 Kalmanson decision is admissible only as a test of the corrected
large-cap surface; it is not a return to the bare-`B` or literal-witness route.

The strongest reusable consumer from that bank is the five-role Lemma 81
shape: `W < F < P < X < Z`, with the `W`-row containing `{F,X,Z}` and the
`Z`-row containing `{P,X}`. It still lacks a live occurrence theorem. An exact
card-14 `(5,6,6)` cap-ordered incidence fixture retains both physical classes,
the q-deleted/common-deletion prefix, blocker omissions/collisions, exact
source-locked four-rows, and the sharp cap-local row bounds, yet has zero
complete Lemma 81 placements. This is not a Euclidean counterexample. It pins
the new producer to positive actual-row overlap (`Z` in the `W`-row and one
common `X`) plus the independently load-bearing five-role cyclic placement.

Adding the compact schema gives a 218-schema card-11 outer CNF. The exhaustive
210-way row-zero-support split is now complete: the partition independently
replays as exhaustive and disjoint, every cube is UNSAT, all raw DRATs check,
and all retained compact proofs independently recheck. This establishes exact
card-11 outer coverage only.

Short higher-cardinality gates decisively reject uniformity of the current
bank. Independently substitution-checked SAT carriers at cards 12 and 13 avoid
all 218 schemas. Both are still non-Euclidean: card 12 has a previously
omitted eight-role instance of production `false_of_two_cycle`, while card 13
has a distinct six-role instance. Each yields a two-inequality strict cycle
with independently replayed Farkas certificate, and neither schema embeds in
the other carrier. The exact follow-up audit shows why: 218 and 262 were
mined-core inventories rather than closure-complete theorem banks, and the
cyclic-cut orbit of one six-role two-cycle had five of six cuts. The scoped
292-schema checkpoint adds 74 missing records and gives 4 hits on the saved
card-12 carrier and 14 on the saved card-13 carrier. Thus the bank was
incomplete, and the saved carriers are repaired. The membership schemas remain
path-length- and carrier-dependent, however; without a generic occurrence
theorem this is not a finite all-cardinality producer.

The exact reflection-canonical live adapter independently kernel-checks in
scratch with the standard three axioms. It consumes any one stored five-role
embedding, but the parent does not need a theorem about every arbitrary row
choice. This paragraph records the earlier bare-`B` checkpoint: its preferred
existential `nonempty_criticalFiberClosingCore_of_biApexRobust B` is now
superseded by direct `false_of_largeOppositeCapsBiApexRobust L`. The broader
honest metric alternative is `∃ rows, ShellMetricCoreAlternative rows H`.
Higher-cardinality carriers are regression gates for surfaces that omit
planar Euclidean, MEC/cap, exact-radius, deletion-critical, or common-map
data. The current `/opt/nfs` O17--O19 ledger remains open for the same reason:
bounded five/six-center
reductions do not eliminate arbitrary mixed/dense sinks.

The checked branchwise audit in
`scratch/atail-force/biapex-strategy-audit/BiApexBranchwiseTargets.lean`
also prevents overreading blocker multiplicity. Both the two-fiber and
triple-fiber arms reduce to the same smallest source-faithful seed: one actual
critical row contains two distinct sources, and a source outside that exact
four-shell has a different actual blocker. The second omission supplies more
partners, but does not by itself supply an ambient-apex equality, a favorable
cyclic placement, or a cross membership into the outside source's row. The
triple-fiber cap audit further support-locks all three same-blocker sources to
one exact four-shell and localizes at least one source away from the blocker's
cap; this is a reduction, not a contradiction. These facts are diagnostics for
a verified corrected survivor, not a replacement for the whole-system
coverage gate.

The retained-radius and strict-interior material below is valid checked
fallback infrastructure, but it is no longer the primary dispatcher. It may
be reused only where it contributes an immediate lemma inside the full
bi-apex robust proof.

## Superseded retained-radius decision record

Production `ATail/FirstApexCriticalFiber.lean` now supplies exactly this
typed predecessor through `nonempty_frontierCommonDeletionCriticalFiber`.
Production `ATail/FirstApexCriticalFiberRow.lean` then supplies the exact
`BothOff`/`RowHit` split as `Outcome P`, indexed by that same predecessor.
Both source-critical facts therefore remain available on both branches. This
fixed-fiber split is diagnostic and nonterminal; it does not justify closure
for every preselected `P`.
Production `ATail/CriticalFiberClosingCore.lean` now defines the exact
existential contract and proves `false_of_criticalFiberClosingCore`; its four
axiom checks contain only `propext`, `Classical.choice`, and `Quot.sound`.
This contract is an immediate terminal library, not an established exhaustive
classifier of the retained collision and matching outcomes.

The same row module now proves the production two-circle gate

```lean
criticalShell_inter_frontierRadiusClass_card_le_two
```

for every actual critical row. It preserves any hypothetical three-point
intersection with the retained first-apex radius class into selected
four-subpackets, where the distinct-center two-circle theorem gives the
contradiction.

Production `ATail/CriticalFiberRetainedRadiusSelector.lean` now implements
`nonempty_retainedRadiusBlockerOutcome` and the collision alternation API.
Consequently the active open work starts after the selector: the two geometric
arm consumers, not construction of the dichotomy itself.

## Why the old target was rejected

The checked six-role Kalmanson theorem is mathematically valid, but its proof
uses only three selected-shell equalities.  It does not use that `C,K` are
distinct sources mapped to `A`, or that deletion of each source destroys K4
at `A`.  The arbitrary outside-pair extraction from the common `A`-shell also
works for every selected four-shell.  These are conditional consumers, not
evidence that the forced blocker-fiber collision advances the parent proof.

The source-faithful audits establish the corrected boundary:

- `scratch/atail-force/robust-orow-collision-hit/` proves an exact
  `bothOff ∨ rowHit` split.  A checked `Fin 12` regression realizes `bothOff`
  with the total fixed-point-free critical map, exact support locking,
  first-apex robustness, and row-closure minimality.  The hit arm still lacks
  the cross row and cyclic order.
- `scratch/atail-force/collision-outside-pair-blockers/` proves that the
  collision-faithful cap split yields only common-blocker pinning, an
  outside-cap cross-bisector, or mutual omission.  It does not produce a
  same-cap second center.
- `scratch/atail-force/cross-row-mining-manifest/` rejects raw anonymous
  `O/A/X` mining.  Its corrected coverage object retains `C,K -> A`, both
  `no_qfree` facts, a critical source `J`, `X = centerAt J`, the actual exact
  `J`-shell, the cross incidence, and the cyclic order.  Pinned replay finds a
  source-indexed cross-row embedding in 75 of 101 witnesses and no accepted
  terminal shape in 26; this is not a coverage theorem.
- `scratch/atail-force/critical-fiber-consumer-match/` defines the exact
  existential `CriticalFiberClosingCore R` and kernel-checks the immediate
  contradiction for both allowed constructors.
- `scratch/atail-force/critical-fiber-both-off-cap-localization/` proves the
  full endpoint/interior cap localization of a fixed `BothOff P`; its strong
  shell counts still do not match an existing consumer.
- `scratch/atail-force/critical-fiber-minimal-deletion/` constructs literal
  singleton stutters at the old common blocker, including when the deletion
  universe is the full common shell.
- `scratch/atail-force/critical-fiber-closing-core-ordered/` originally
  isolated `K ∈ selectedAt J` and `O,A,X,J,C,K`; the later retained-collision
  audit proves that order incompatible with mandatory alternation.
- `scratch/atail-force/critical-fiber-closing-core-samecap/` isolates a
  source-faithful second center; the later cap audit proves the first-apex
  version directly inconsistent and the actual-row version center-degenerate.
- `scratch/atail-force/critical-fiber-closing-core-selector/` implements the
  retained collision/injective-matching split. Its repaired fixture is now
  kernel-refuted by the banked duplicate-center core.
- `scratch/atail-force/critical-fiber-alternation-audit/` rejects the first
  Fin12 matching witness: two rows have a forbidden three-point retained-class
  intersection, and the same triple violates shared-pair alternation.

## Active next theorem hierarchy

`false_of_largeOppositeCapsBiApexRobust L` is the robust branch goal and the
primary missing mathematical producer. Production
`false_of_criticalFiberClosingCore` is already complete and may be used only
through the late-choice, fully rebased internal helper described above. The
checked matching-endpoint/cross-hit adapter is fallback only. Production does
not establish the coarse order `O,A,X,perm(K,C,J)`, and exact rational
strictly convex models realize both nested tails left by `/opt/nfs` Lemma 184
/ PG136, even with the
local obtuse-chain inequality. Pair-preserving selection retains more source
data but still lands in known nonterminals or a two-companion double-hit
residual. The primary producer must instead remain indexed by `B` and visibly
use both blocker-image omissions; all unresolved fallback branches remain
explicit rather than assumed constructors.

The source-indexed follow-up fixes the next proof object more precisely. A
repeated blocker fiber already supplies `P,X` in its complete `Z`-row and `Z`
in the row sourced by `Z`. Lemma 81 still needs one parent-return incidence
into that second row and an independent five-role cyclic placement. The exact
card-14 fixture realizes a return hit without the order, so this is not
bookkeeping and neither field follows from `B` or the cap-six bounds alone.

The survival cover itself is not open. The card-at-least-fourteen theorem
already constructs a source-faithful common-deletion packet whose second
center is the physical second apex. The checked uniform consumer sends that
packet to swapped `FA-UNIQ4` or `FullyDeletionRobustAt D S.oppApex2`. The
latter is exactly the field already carried by `B`, so on `L` another pass
through the cover or generic common-deletion recursion makes no progress.

The active residual is the checked robust radius classification at the
physical second apex: either one positive radius class has cardinality at
least five, or two distinct positive K4 radii have support-disjoint selected
four-classes. Cap four contradicts robustness and cap five routes to protected
swapped `FA-UNIQ5`; both normal-form arms remain compatible with the current
one-hit/cap-capacity theorems once `oppCap2.card >= 6`. The next proof must
couple those radius arms to the original frontier, first-apex robustness,
global critical map, minimality, `noM44`, and full cap/MEC geometry. A forced
prescribed-pair row with an immediate Lemma 81 or U5 consumer is acceptable,
but another marginal radius extraction is not.

Current-source reconciliation sharpens the immediate leaf beyond this raw
classifier. The large-radius exact-five arm now reaches a full-parent,
source-exact physical omission cycle. Its all-reverse branch already has
period `3..5`, exact `2 + 2` cap support in every successor row, injective
actual blocker centers, injective outside pairs, and strict cap straddling.
Generic common-deletion/shared-pair/singleton outputs have been audited as
loops. The adopted next theorem is direct `False` on this all-reverse leaf,
retaining the original `L` in addition to
`FrontierCoupledExactFivePhysicalCycleParentResidual H Flarge`. The missing
fact is aggregate multirow geometric force, not another row packet. A
`ShellCurvature` is useful only as prospective per-row infrastructure.  Its
current chart interface is inconsistent with the periodic endpoint equality
used by the live reverse-row wrapper; the sign law needs an open-window or
distinct-endpoint repair.  Even after that repair it still needs a shared
packing/overlap consumer and does not itself close a source `sorry`.

The fixed-anchor subroute has a decisive regression gate. The kernel-checked
exact rational `DirectedCycleCountermodel` realizes period three with all
cycle sources on one physical-apex radius and all three distinct actual-
blocker candidates on the corresponding chord bisectors, in strict-convex
nonobtuse MEC cap order. It lacks the full outside support pairs and the total
critical map. Therefore those global/full-row fields must be used explicitly;
the co-radial source cycle and cap straddling alone are not contradictory.

Two later audits sharpen this boundary. First, an exact rational shared-turn
ledger realizes all three marginal shell-curvature packets with strictly nested
outside intervals, so marginal curvature aggregation is refuted even after the
periodic chart interface is repaired. Second, a deformable period-three search
tested 30,000 exact-equality cycle cores and found no sampled outside-cap target
that stayed inside the Moser disk and became a new hull vertex. Exact rational
replay now shows that this was a sampling artifact: a ten-point strict-convex
nonobtuse-MEC model retains all three source/blocker pairs and one legal target.
Thus both the projected one-row claim and the full cycle-core-plus-one-target
claim are refuted. A further exact rational eleven-point construction adds the
second target and completes one exact `2 + 2` row on that same core. The next
valid local milestone must therefore complete the remaining two rows
simultaneously or consume a global-parent field; another sum of marginal bounds
is still blocked.

The full linear check at that next threshold is also nonterminal: both legal
placements of the two outside targets have exact rational metric ledgers
satisfying all triangle and strict Kalmanson inequalities, row equalities, and
named-class exclusions. These ledgers are abstract distances, not planar/MEC
models. The separate same-side coordinate construction supplies the missing
Euclidean/MEC realization, so one complete row is fully retired. No ordinal
comparison cycle or linear Farkas certificate can restore it; the first live
local threshold is all three complete rows.

For that threshold, outside-pair injectivity must not be strengthened to six
distinct targets. It excludes equality of two unordered pairs but permits a
single target to occur in two rows. A source-faithful audit must cover those
role-identification schemas as well as the six-distinct branch.

The corrected full enumeration contains 2,214 quotient cases with three
through six actual target points. Exact ordinal/Kalmanson closure rejects
1,224; the other 990 admit independently replayed rational LRA ledgers. Hence
the linear distance theory is a valid pruning consumer but not a proof of the
all-three-row contradiction. Any universal closer must use planar Euclidean/
MEC constraints or global critical-map/deletion/parent data on those survivors.

The exact UNSAT cases prove a conditional finite local normal form within the
audited fixed-order abstraction: the union of the three outside pairs has
cardinality at least five, and at cardinality five only the anchored
row-`0`/row-`1` overlap survives. All 1,224 cores reduce to two Kalmanson
overlap exclusions, `Disjoint P0 P2` and `Disjoint P1 P2`, followed by `Finset`
arithmetic. However, `K + period = 3 + all-reverse` currently supplies only
three per-row boundary cuts, not the shared anchored chiral order used by the
census. A shared cap block is source-constructible, but its invariant order
packet is a forward/reflected disjunction; the exceptional overlap pair changes
between the two branches. Since `StrictCapBlockData` also forgets the global
injective/CCW data expected by the current Kalmanson API, a pointwise
`StrictCapOrder` consumer or an explicit CCW-evidence refactor is logically
prior to any Lean promotion or coverage claim.

The smaller pointwise consumer is now proved on paper for both orientations.
It needs only a pointwise API wrapper of the existing extreme-pair hull-line
intersection theorem; no new 1,224-case table or geometric conjecture is
required. Its conclusion is orientation-invariant pruning—one pair is disjoint
from both others—not branch closure.

The subsequent Euclidean parameter search includes every outside-pair overlap
graph of union cardinality three through six conditional on that same shared
order. It imposes the
source/blocker bisectors and target-circle equalities by construction, then
checks strict convexity, cap-side placement, named exclusions, and nonobtuse
Moser-disk containment. All bounded branches returned `UNKNOWN_NO_HIT`. No
candidate reached exact rational reconstruction, and the run proves neither
infeasibility nor coverage; the approximately `-4.09e-5` best broad six-target
margin is only a numerical diagnostic. This is enough to stop undirected
numerical mining, not enough to promote a contradiction. The fixed-order
systems remain positive-dimensional (`11` and `13` generic dimensions), and
representative exact QF_NRA probes are also `UNKNOWN`; equality-only Gröbner
mining is therefore the wrong object. The local route must first prove the
common order and then use interval/CAD or an equivalent exact semialgebraic
decision.

Even the correctly centered first-apex return negation survives the entire
fixed-forward linear bank. With `right = S.oppApex1`, all 990 exact LRA ledgers
make all three reverse pairs non-co-radial, and all eight strict direction
signatures replay exactly. Hence triangle/Kalmanson plus the three complete
reverse rows cannot force the terminal return.

The parent audit finds no existing higher theorem that closes this leaf. Its
narrow nonrecursive terminal is a return of one reverse outside pair to
the retained original frontier pair. That target immediately instantiates
`outsidePair_unique_capCenter` with the reverse blocker and `S.oppApex1`, so it
closes this actual branch if proved. It is OPEN, but it is not a marginal
consequence: an exact 16-label incidence model satisfies the current
critical-map/fiber/bi-apex robustness/frontier/reverse-row marginals with no
first-apex return, and an exact rational convex/MEC model satisfies the cap
bounds, retained pair marginal, and one complete reverse row without return.
The latter deliberately omits first-apex K4/double deletion, the total CSS, and
the other complete rows, while the finite model includes those data only as
non-Euclidean marginals. A viable proof must cross-couple the global critical/
deletion layer to Euclidean/MEC/all-row order or minimality; neither layer alone
forces the return. The conditional orientation/Kalmanson/CAD route remains a
fallback, not live coverage.

The retained parent contributes one precise next role packet rather than fixed
slots: a radius-`r` four-row `T0` containing `q,w`, and a double-deletion row
`T1` at radius `rho` disjoint from `q,w`. Both preserve two strict-`oppCap1`
witnesses. The `rho = r` arm gives six distinct co-radial points; the
`rho ≠ r` arm gives disjoint four-shell supports. The three legal `q,w` cap-role
cases and all unnamed completion roles must remain explicit. This is the
smallest source-faithful cross-system surface not already covered by the two
no-return regressions.

The follow-up split audit prevents overstating that surface.  These are two
selected four-point rows, not two exact CSS shells.  The original unequal-
radius fixture violated the live distinct-circle intersection bound and is
retired.  Its corrected dynamic successor supplies one exact SAT
representative in each of the six coarse `rho`/frontier-role cells, with total
CSS, global K4, source-by-source deletion failure, the exact period-three
reverse rows, pair injectivity and straddling, common cap order, and the
two-circle intersection caps, while all reverse pairs avoid both T0 and T1.
Those six witnesses are not a source-exhaustive census of the 31 labelled
same-radius role assignments or both boundary orientations.  Thus the
occurrence is not a theorem of the represented finite
role/radius/CSS/minimality marginals.  In the
equal-radius arm the new consequence is arbitrary two-point deletion survival
at the first apex; the named six-class plus the checked blocker/deletion
marginals still has an exact finite metric model.  The remaining producer must
therefore use the omitted common Euclidean/MEC/order realization together with
full CSS/minimality provenance.  A reverse pair wholly contained in one named
row is still an immediate `outsidePair_unique_capCenter` terminal.  Its
occurrence is retired only as a conclusion of the represented weak marginals;
it is the active aggregate producer from the complete `L` parent.  The first
omitted force is a source-backed nonlinear Euclidean/MEC, full-fiber, or
global-minimality coupling across T0, T1, and the all-reverse rows.  One pinned map is already
Kalmanson-UNSAT before target negation, but that is not quantified coverage.
The source audit shows that even “all 31 roles plus both orientations” is not
the complete quotient.  Coverage must also enumerate T0/T1 overlap sizes zero
through two with fresh completions, every legal unequal-radius row profile,
all physical exact-five 3/4/5-strict profiles, legal identities between the
independent F0 and exact-five cycle/blocker roles, fresh reverse targets and
their sharing partitions, and blocker/source cross-identifications.  The
common cap order must be constructed from the live cap, not assumed as an
extra public premise.  Thus the current fixed-cell Kalmanson solver is a
SAT-first diagnostic; `COMPLETE_UNSAT` from its existing loops would not be a
coverage result.

The source-side order adapter is no longer conjectural at the API level.
Scratch `nonempty_reverseCycleSharedCapOrder` drafts a period-parametric
construction from `K + allReverse`, using one common physical-cap block and
cap-side distance injectivity for every row.  It adds no public hypothesis and
preserves both orientations and legal blocker/nonincident-source identities.
The draft is `sorry`-free but not yet elaborated under the active no-build
gate, so it remains a validation dependency rather than a promoted theorem.

That diagnostic has now exhausted 1,728 generated forward boundary orders
times six representatives: 10,368 exact rational cells, all UNSAT and none
`unknown`.  The common-core census identifies the result as an encoding
artifact rather than convergence: every cell fixes a T0/T1 pair to be a pair
in the independent physical exact-five row, and those two owner centers do not
alternate with the target pair.  The resulting three-constraint Kalmanson
contradiction uses no reverse row or period-three fact.  The full quotient must
test disjoint and partial cross-surface identity patterns before this route has
positive closure evidence.

The first fully disjoint test has now been run, and it is exact-QF_LRA SAT.
Its 25 named roles keep T0/T1, the physical exact-five row, all blockers, and
all six reverse outside targets mutually disjoint while satisfying the full
strict triangle/Kalmanson ledger, every named row equality, the common order,
and the source-valid two-owner alternation condition.  Thus the corrected
linear shared-order surface is compatible; this is not a Euclidean planar/MEC
realization, but it blocks any claim that the period-three contradiction
follows from the current linear relaxation alone.

The follow-up rank-two query is source-audited as one legal projection but
has no verdict.  Its complete 25-point Cartesian formula has 46 free reals,
19 shell equalities, 79 named exact exclusions, and 575 edge-side convexity
inequalities.  Z3 and cvc5 both returned `UNKNOWN` by timeout at 120 seconds.
The exclusion-free 602-atom formula and every 20-second row prefix also timed
out, including T0/T1 alone, so the probe does not identify a contradictory
row family; it identifies the raw convex-coordinate encoding as operationally
hard.  The fixed names include only four members of the surplus cap, requiring
at least one unnamed live vertex, and the formula omits whole-carrier K4,
MEC/cap, total critical-map, minimality, and `noM44` data.  The next instrument
is witness-directed constrained optimization or homotopy with exact
substitution checks, not more quotient breadth or longer blind SMT timeouts.

That witness-directed rung has also reached a bounded checkpoint.  Two
24-core SLSQP campaigns tested 192 starts each.  One used the original word
and the exact nine-point cycle core; the other used an independently audited
source-legal word whose exact QF_LRA-SAT ledger contains the banked rational
complete-`b0`-row configuration.  Across all 384 starts there were zero full
substitution-verified planar hits.  This remains numerical `NO-HIT`, not
infeasibility.  Because the second word removes the first seed/order mismatch,
the next compute escalation must change algorithm—interior-point/support-line
or homotopy—not merely add SLSQP starts.

The latest prose backup supplies only a domain tightening.  Each reverse
critical support omits the physical opposite apex and its two outside targets
lie in the union of the two complementary open blocks.  The live role match is
exact at the mathematical level, but no Lean declaration is yet imported.
The previously recorded `rho = radius`, `IO` witness already satisfies the
tightening, so it is pruning rather than closure.  The backup's stronger
consumers still lack, respectively, a T0/T1-to-reverse shared target, a
through-opposite reverse row, or an all-exact-transverse block population.
The next audit must therefore test a genuinely Euclidean/MEC, full-fiber, or
global-minimality field against the disjoint SAT ledger rather than enlarging
the same linear quotient.

The period-four/five extension has one reusable geometric normal form but no
automatic global closure.  A local extremum source forces its two incident
reverse outside pairs to be disjoint.  Exact exhaustive order enumeration
removes 4 of 6 normalized period-four source orders and 18 of 24 normalized
period-five source orders from the later search surface, leaving 2 and 6
nonadjacent-extrema order types.  With source-faithful blocker identifications
the residual counts are 34 and 228.  This is an exact finite-order reduction,
not a Lean theorem or a contradiction; the period-three decision remains the
first bounded target.

The four retained-terminal constructors remain fallback evidence. The
`matchingSourceReturn` / `jointDeletion` leaf makes one named source deletable
at four distinct centers but still forces no named cross-support pair. The
endpoint cross-hit leaf remains closest to `CriticalFiberClosingCore`, but its
missing cyclic order is unchanged and `B.secondApex_robust` contributes no
load-bearing field to it. Neither leaf is the primary parent route.

Whole-carrier two-omission models and equality-quotiented/Kalmanson checks are
regression gates only, not the current dispatcher. The fixed-profile mismatch
is pinned by
`scratch/atail-force/biapex-marco-coverage-audit/`: its Lean cardinality gate
shows that fourteen labels cover the whole carrier only at card fourteen, and
that a chosen fourteen-role subpacket already requires card at least fourteen
without supplying blocker/row closure. The
original `FA-UNIQ4/5` and swapped `FA-UNIQ4` consumers are protected parallel
dependencies.

### Historical fallback: strict-interior parent route

Any “active,” “current,” or “next” language in this preserved subsection is
historical. It does not override the bi-apex decision and theorem boundary
above.

Production `ATail/RetainedStrictInteriorPairSelector.lean` supersedes the
whole-radius collision/injective split as the parent dispatcher. It selects
two guaranteed strict-first-cap retained sources before comparing actual
blockers. Equal blockers give a localized collision with the sources and
blocker in that strict cap and exact shell/cap intersection equal to the two
sources. The blocker differs from both shell sources, so these three
strict-interior points force the cap to have cardinality at least five. A third
strict-cap point outside the shell therefore gives a fresh source-valid common
deletion unconditionally; no collision-specific large-cap residual remains.
Production `ATail/LocalizedCollisionMutualOmissionCycle.lean` strengthens
this to a source-exact mutual-omission cycle between one collision source and
the fresh source, with distinct actual blockers and both deletion packets.
The scratch theorem `nonempty_localizedCollisionCapOrderOutcome` further
splits the fresh blocker off the first cap or, on-cap, proves disjoint complete
outside-pair banks with at least two total pair incidences. This is not
promoted because neither exact residual has an immediate consumer.
Distinct blockers feed the existing directed
omission and large-cap consumer.

Production `ATail/RetainedMatchingCommonDeletionCycle.lean` retains two
source-exact common-deletion edges and gives the exact matching endpoint split:
source return, endpoint critical fiber with forced `RowHit`, or a path with
three pairwise-distinct consecutive blockers. Production
`ATail/RetainedMatchingSourceReturnRadiusSplit.lean` refines source return to
joint deletion at the first apex and both blockers or an exact-four partition;
both arms expose concrete U5 row ingress. Production
`ATail/RetainedMatchingEndpointContinuation.lean` refines the endpoint fiber
to a fourth-source cross hit or new common deletion and the three-distinct
path to a closing cross hit or source-exact three-cycle. It also proves that
the original middle source cannot be the desired cross-hit producer because
its row omits the endpoint. Production
`ATail/RetainedStrictInteriorParentNormalForm.lean` assembles these outputs.
Its `nonempty_retainedStrictInteriorTerminalSurface` theorem exposes the four
constructors directly without dropping provenance.
Production `ATail/FrontierCoupledStrictInteriorNormalForm.lean` is now the
authoritative direct-parent interface. Its unconditional
`nonempty_frontierCoupledStrictInteriorNormalForm` packages that terminal
surface together with `FrontierCommonDeletionRefinedNormalForm` from the same
residual. This preserves the original frontier pair, retained second-apex row,
and refined off-radius/on-radius/companion escape classification for every
leaf consumer. Any proof that projects only the strict-interior surface must
justify why the discarded frontier fields are unnecessary; reconstructing
them after the split is not part of the plan.
The live direct-parent boundary still has four constructors, now split into
seven concrete research leaves:

1. terminate the localized collision's source-exact mutual-omission cycle;
2. consume source-return joint three-center deletion;
3. consume source-return exact-four U5 ingress;
4. consume the endpoint-fiber fourth-source cross hit;
5. terminate the endpoint-fiber fresh common deletion;
6. consume the three-distinct closing cross hit; and
7. terminate the source-exact three-cycle.

The source-return U5 bank-match audit is negative and load-bearing. The joint
q-critical intersection has card at most one; exact-four critical/deleted
intersections are singleton / pair with the remaining dangerous point forced
absent. No imported U5 contradiction consumes the present ingress. U5 is a
center/support-confinement fallback only. The active route is to recover force
from cap-order/Kalmanson coverage and the retained second-apex/original-pair
provenance, not to add more generic row packets.

The first coupled audits refine that instruction.  Off-cap collision now
localizes the fresh blocker to strict surplus or strict second-opposite cap
and builds exact U5 ingress. Its fresh-row intersection with `{q} ∪ T` is
exactly empty, a dangerous singleton, or a dangerous pair, with `q` excluded.
Global K4 builds an exact named q-deleted/q-critical row at the other
collision source, and both blockers have dangerous-center/selected-candidate
role splits. The remaining datum is a `p` plus second-dangerous membership in
that row, another named directed row hit, or a direct cap/MEC contradiction
in one of the three intersection branches.
On-cap collision reduces 84 orders to twelve serial orders, all instances of
one order up to reversal and outside-label permutation.  A kernel-checked
exact metric survivor satisfies triangle, strict Kalmanson, cap nonacute, and
general Ptolemy inequalities but violates planar Gram positivity.  Therefore
linear Kalmanson/curvature is exhausted; row-specific Ptolemy equality,
planar rank/two-circle geometry, or a third actual row is required.

Source return now has a kernel-checked physical second-apex bridge. At least
one selected source survives deletion at both physical apices and at the
companion blocker, and the endpoint splits into first critical, second
critical, joint double deletion, or disjoint exact shells. The selected pair
has unequal second-apex radii, so it provably cannot recover K-A-PAIR.
`SourceReturnPhysicalResidual.lean` exhaustively routes the four constructors
to `FullyDeletionRobustAt D S.oppApex2` or protected
`SwappedFirstApexUniqueFourFrontier D S H`: critical leaves give the swapped
frontier, disjoint shells give robustness, and joint double deletion splits
on robustness with a critical/swapped fallback.  These physical leaves are
no longer independent research targets.

The older seven-order different-pair collision audit is now a reusable
endpoint-fiber terminal and regression bank, not the parent dispatcher. All
five of its acyclic order types also have kernel-checked exact rational models
satisfying strict convexity, the three equality rows, and every strict
nonacute cap-chain inequality, so cap-only five-order mining is blocked.

### Historical retained-radius selector audit

First close the retained collision arm. Every collision now production-proves
`RetainedRadiusCollision.nonempty_rowHit`, and its sources alternate across
the first apex and common blocker. The former ordered target
`O,A,X,J,C,K` is kernel-refuted on this arm: it places both sources on the
same boundary arc, contradicting that mandatory alternation before the cross
membership is used. The same-cap target is not a natural alternative either:
putting the first apex and common blocker in one cap while excluding both
sources is already direct `False`, and an actual same-cap critical row through
the pair has the common blocker center. These facts, the strict-cap common
blocker localization, and the exact two-source cap intersection are now
production in `ATail/RetainedCollisionCapLocalization.lean`. Continue through
a new alternation-compatible cross-fiber consumer or direct `False`.

Production `RetainedCollisionCapLocalization` proves that “cross-fiber” cannot
mean another row through the same collision pair: every such actual critical
row has the existing common blocker by the perpendicular-bisector apex bound. A
kernel-checked exact rational convex eight-point model realizes the complete
local two-shell/overlap/alternation marginal. The next consumer therefore
needs a second fiber on a different equality pair to form an ordinal cycle,
or a genuinely global MEC/filter/deletion/no-`IsM44` contradiction.

That route now has a production positive boundary in
`ATail/RetainedCollisionDifferentPairConsumer.lean` and an exact scratch
negative boundary.  The collision
automatically provides a fresh first-apex equality pair `OJ = OC`; if the
actual `J`-critical row also contains the opposite collision source `K`, then
the compatible order `O < J < C < A < X < K` closes by the strict cycle
`AC < AJ < AC`.  The actual `J`-row forces a second alternation of `J,K`
across `O,X`; together with the collision's alternation of `C,K` across
`O,A`, this leaves seven live order types.  Exhaustive ordinal closure makes
two terminal.  Five orders remain; exact rational strictly convex local
models realize every one, and Lean independently kernel-checks the first,
`O < J < X < C < A < K`, with `OJ = OC = OK`, `AC = AK`, and `XJ = XK`.
Therefore the bare different-pair cross hit is a conditional consumer, not
the missing producer.  Closure must force a cyclic order, exclude all five
acyclic orders with full-parent geometry, or add a further source-valid
equality pair.

Then close the injective matching arm. The original finite witness is invalid,
because it violates the production two-circle bound and cyclic separation.
A repaired Fin12 object has a total fixed-point-free source-exact
support-locked map, robust card-five retained class, injective restriction,
row-closure minimality, every pairwise exact-class overlap at most two, and a
cyclic order satisfying every directly generated alternation. It is not a
survivor of the existing theorem bank. The formalized metric-core replay finds
a `DuplicateCenterCore` with common triple `0,1,3` and centers `2,8`; the Lean
fixture now proves the closure paths and `no_planar_realization` using only the
standard core axioms. Therefore this object supplies no evidence that the
matching arm needs new cap/MEC mathematics. The next computation must first
enforce every formalized metric core and ordinal/Kalmanson closure; only a
bank-clean residual can expose a genuinely geometric missing ingredient.

The general live arm has advanced independently of that fixture. Production
`ATail/RetainedMatchingGeometricReduction.lean` proves
`nonempty_retainedMatchingGeometricReduction`: it chooses two distinct
retained sources in the strict first-opposite-cap interior, orients a missing
cross membership forced by distinct blockers, packages the resulting
cross-deletion survival together with first-apex survival as a
`CommonDeletionTwoCenterPacket`, and retains the `noM44` consequence that one
opposite cap has cardinality at least five. The next matching theorem consumes
this packet; bank closure remains mandatory for any finite discovery used to
find that consumer.

Production `ATail/RetainedMatchingLargeCapConsumer.lean` now performs that
first consumption step. It orients the common deletion and splits the reverse
incidence into exactly three outcomes: paired source-faithful common deletion;
a reverse hit with the first cap large, producing a fresh third strict-cap
common deletion; or a reverse hit localized to an exact two-point first-cap
shell while only the other opposite cap is known large.

Production `ATail/RetainedMatchingCommonDeletionNormalization.lean` proves
that the last arm also contains a fresh common deletion.  The reverse shell
has only two points outside the first cap, while the large second cap has at
least three strict-interior points, so one is outside the shell.  Parent
first-apex deletion robustness and exact critical-shell semantics produce the
packet.  Hence matching has one open consumer surface, not three: terminate a
source-valid common deletion while retaining its matching/large-cap origin
and full parent geometry.  The generic packet remains nonterminal by the
checked successor-cycle regression.

This paragraph is historical. Its terminal lemmas are inputs to the current
full-parent producer. Its former bare-`B` primary is superseded. The present
plan makes `false_of_largeOppositeCapsBiApexRobust L` the primary missing
producer; a closing core is optional internal structure chosen only after a
favorable critical system has been selected and the full parent packet
rebased.

Any further computation must encode the complete actual blocker restriction,
the production intersection bound, all generated boundary alternations, the
full formalized equality/metric and ordinal/Kalmanson banks, and an immediate
checked consumer. A new finite survivor or UNSAT claim is not load-bearing
without a source-valid coverage manifest; cap/MEC/full-filter or no-`IsM44`
facts enter only after that bank-clean gate.

The fixed-fiber residual audits are negative but load-bearing for scoping.
`BothOff` cap localization gives strong endpoint/interior shell counts with no
matching consumer. Global minimal deletion can stutter at the old common
blocker with either singleton collision source, even over the whole common
shell. These routes also do not replace the direct physical-apex split or its
one full bi-apex robust consumer.

## Synchronized documents

This decision is reflected in:

- `docs/closure-plan-full-spec-2026-07-09.md`;
- `docs/closure-matrix-2026-07-09.md`;
- `docs/atail-force-producer-plan-2026-07-13.md`; and
- `docs/97-rvol-full-prose-proof-2026-07-13.md`.

Historical audits and external prompts remain historical artifacts and are
not rewritten as current plans.
