# Exact13 occurrence bank ingress audit

Lane: `exact13-occurrence-bank-ingress-20260906`. Request #16526.

The candidate is the exact double-erased L pair furnished by the exclusive-apex support partition. Its immediate consumer is the provider bank coverage interface. Reuse preflight: project-indexed query for exact13 occurrence/otherSupport/double-erased L/source candidate provider; existing SourceOrderBridge provides arbitrary shared blocker candidates, while ExclusiveApex18 provides the exact partition. The missing connection is the occurrence-specific pair and actual blocker identity; no new geometric antecedent is required. Source/import revision: `3f9f5c6401a8d1776f05b1167c730f672d0168f3`.

Scope: standalone adapter only. No concrete bank, finite-family membership, coverage theorem, aggregate import, or unconditional cell closure.

## Validation and trust

`lake env lean -M 16384 Erdos9796Proof/P97/ATail/FrontierLiveClosure/CardGeThirteenExact13OccurrenceBankIngress.lean` exited 0. The public `exists_occurrence_other_shared_pair` axiom probe reports only `propext`, `Classical.choice`, and `Quot.sound`. Source SHA-256: `44f15a38d0e91c52d65f599d17ae042b27de932297b9a7e16a8f26ec941f5e89`. No sorry, admit, custom axiom, or native decision proof is introduced.

The initial check failed because the normal ExclusiveApex18 compiled artifact predated its source constructor. Focused checks refreshed ExclusiveApex18 and WeightedProviderBankBridge in the normal build cache, both exiting 0, before the successful consumer check. No full build or aggregate publication was performed. Execution used lane base `3f9f5c6401a8d1776f05b1167c730f672d0168f3`.

Retained runtime logs under `scratch/runs/exact13-occurrence-bank-ingress-20260906/run-0001/events/`: `constructor-build.log`, `dependency-build-refreshed.log`, and `occurrence-build-refreshed.log`. The initial failing log is retained separately. Logs are reproducible runtime evidence; the committed run manifest authenticates the exact checked sources.

## Mathematical audit

Verdict: Lean-proven occurrence adapter under the original SourceOrderBridge.Input, with no additional hypothesis. The constructor gives ell in L erased at z. Cardinality three then gives exactly two distinct labels after erasing ell. The partition equation places both labels in the other blocker. The constructor's chosen_cases identifies c1 or c0 respectively, retaining the provider tag. The source orientation supplies a common valid bit, and exact support membership proves candidatePredicate at both l and that blocker.

This result proves neither finite-family membership nor bank coverage. It is standalone and is not an aggregate-import or solver-artifact promotion claim.
