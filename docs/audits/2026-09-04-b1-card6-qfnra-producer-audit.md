# B1 card-six QF_NRA producer audit

- Lane: `luna-swarm-slice-ingress-b1-card6-qfnra-producer-20260904`
- Assignment: nthdegree message `#10631`, using B1 assignment `#9780`
- Checkpoint: `.codex/worktree-checkpoints/luna-swarm-slice-ingress-b1-card6-qfnra-producer-20260904.json`
- Run manifest: `scratch/runs/luna-swarm-slice-ingress-b1-card6-qfnra-producer-20260904/run-0001/run_manifest.json`
- Input: `scratch/runs/b1-all-interior-card6-qfnra-prelaunch-20260903/run-0001/SOURCE-MAP.json`
- Input SHA-256: `5d5bc0ff8b16218e7cfa51e0438ad6d94a2e3e6eb090e11741028c1bf725c4b2`

## Gate result

The audit stopped before any PIQD session or solver job. The source map is
pinned to repository revision
`29614dfc71e902523fbdbac5191c964a187282b7`, while the shared worktree is at
`4dca919e2fc6513c3eb3497343686065974026fb`. The source-freshness gate therefore
fails closed. The map itself records `PRELAUNCH_BLOCKED`, six unsupported fields,
`launch_allowed=false`, and no session, solver execution, model readback, or
promotion.

The first assertion-level unsupported bridge is `gauge`: the candidate names
carrier roles but has no Lean coordinate chart identifying its numeric pins.
The first B1 semantic unsupported bridge is the all-interior strengthening of
the card-six decomposition. Lean proves two disjoint two-point live slices and
that one pair lies wholly in the strict second cap, but it does not lower all
six roles to the candidate's finite QF_NRA coordinates.

## Audited source bridges

- Cross-role distinctness is only partial: source packets provide within-slice
  and within-row facts, not all candidate cross-role inequalities.
- Circle and endpoint-bisector support predicates are source-named, without a
  checked coordinate realization in the candidate.
- Escape source/blocker provenance, deletion survival, and no-third joint
  deletion are finite-set/source obligations, not finite real constraints.
- Same-boundary-arc and the three global-gap/closed-terminal alternatives have
  Lean consumers, but no producer lowering their antecedents into this QF_NRA
  candidate.

Because these bridges are absent, no controlled solver invocation was made and
there are no receipt, model, unsat core, or promotion artifacts to report.

## Follow-up reassessment of the six-role ingress

The source-clean ingress does discharge the earlier six-role concern at the
physical-role level:

- `physicalRowChoice_value_image_eq_physicalClass` identifies the image of
  `d1,d2,u0,u1,v0,v1` with the selected physical class.
- `physicalRowChoice_support_card_eq_six` gives six physical carrier labels on
  the card-six branch.
- `physicalRowChoice_boundaryIndex_image_card_eq_six` transfers those six
  labels to six distinct indices of the complete source boundary.

The corrected native `PointConfiguration/v1` design therefore keeps all solver
coordinates free and lets the native encoder apply its generic normalization.
The boundary should be the complete `E.boundary` carrier in source-certified
counterclockwise order.  The six physical roles bind to the six points supplied
by the role packet.  The three profile classes
`[Bc,uL,vL]`, `[Bu,c0,vR]`, and `[Bv,c1,uR]` must remain separate role names
unless explicit source theorems prove each displayed alias equality; the
`B1RowCompletionPair` fields only provide within-row pairs and expressly make no
cross-row disjointness claim.

After dropping the stale fixed numeric gauge, the first concrete assertion in
the copied fixed-order journal that is still not source-bound is line 27:
the positive distance assertion for `x_0` and `x_2`.  Under the authenticated
profile order, `x_2` is the quotient class `[Bc,uL,vL]`.  The three ingress
theorems do not provide a source point for that class, an alias-equality
certificate for its three role names, or its distinctness from `x_0`.  This is
the first remaining quotient-class assertion; the subsequent fixed order and
metric-support assertions require the same missing role-to-boundary lowering.

Thus the six-role facts are sufficient for a six-role source ledger, but not for
the ten-class fixed-order candidate.  No PIQD session or solver job was started
in this follow-up.

## Call-site and typed-payload gates

There are two earlier source-ledger gates before the journal assertion above.
`B1FiveSixWaveIngress` retains only `escape` and `cardCase`; its `.cardSix`
constructor does not retain the `hnormal` or `hsix` proofs required by the
three ingress theorems.  A producer wrapper must carry those proofs explicitly
before it can invoke the six-role image, cardinality, and boundary-index facts.

After that wrapper exists, the first native metric fact needing a new public
adapter is the physical-row `CommonRadius` premise.  The underlying source
fact is the private `physical_support_subset_sourceMetricRows` theorem in
`B1LocalRolePacket`; the public `positiveRowsMatch_sourceRowChoices` theorem
is a broader row-choice wrapper and does not itself bind the six named payload
roles to solver point references.  The native payload must therefore add a
source-labeled role/point ledger and a checked physical-row radius adapter.

These call-site and typed-payload gaps precede the fixed journal’s line-27
quotient-class example.  They reinforce the same stop decision: the native
free-coordinate shape is suitable, but no candidate assertion may be sent
until the source proofs and role bindings are explicit.
