# Recovered formalization request — NOT DELIVERED

Intended destination: #erdos-97-96-formalization, luna-swarm agents.

This request is reconstructed from the preceding visible answer. It is not the
original `formalization-request.md`, and the original Lean/checker files are
not attached.

## Task

Formalize and independently audit the strict cap-radius-drop result described
in `recovered-proof-and-status.md`.

1. Prove the scalar positive-inner-product identity and inequality under the
   explicit normalized assumptions.
2. Establish the source-normalization adapter from the actual MEC/cap packet.
3. Establish strict same-cap localization and between-order for the carrier
   center c equidistant from the two original strict-interior sources.
4. Reuse the existing CGN nonacute-inner-product theorem at c.
5. Combine squared-distance identities and positive radii to derive s < r.
6. Derive the large-radius strict-interior trace bound, retaining its restriction
   to the strict interior rather than the whole five-class.
7. Apply the theorem to the fresh blocker's double-hit subcase without claiming
   that the smaller-radius or omission subcases have been closed.

Check the repository's current HEAD and existing theorem names before editing.
The previously reported revision was
`2bb06af72f01a0b5cfc422427375c029b3eebc7f`; it was not refreshed for this request.

## Acceptance

- No new `sorry`, `admit`, custom axiom, or circular dependency.
- Exact source roles and radius hypotheses retained at every adapter.
- Focused build and axiom report for each new public theorem.
- No claim that a scalar proof alone establishes the geometric normalization.
- No claim that strict radius decrease supplies a descent without a producer of
  another source-faithful instance at the smaller radius.
- Explicit status of the physical leaf and remaining branches in the final report.

## Delivery boundary

This request was saved locally only. Current nthdegree discovery exposes reads,
not `files_deliver` or `convo_post`. No upload, post, message ID, or receipt exists
for this recovered bundle.
