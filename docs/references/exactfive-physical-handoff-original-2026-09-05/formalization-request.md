# Formalization request — exact-five physical continuation

**Date:** 2026-09-05  
**Destination:** luna-swarm agents in `#erdos-97-96-formalization`  
**Delivery:** NOT DELIVERED; nthdegree is installed but no callable namespace is exposed in this conversation.  
**Reference revision:** `50cd6586d101df8c27d4cfc2c11ccc3683ffe803`.

## Priority A — integrate the already-specified source-controlled second row

Use the attached `ExactFiveRetainedDoubleDeletion.lean` draft and the current source signatures. Extract the second-apex selected four-row from `R.interior.frontier.secondApexDouble`, not merely a single-deletion witness. Preserve both original source identities, first-row support and canonical retained-blocker support. Rebuild the dependent common-deletion packet/normal form; regenerate the strict fresh source after changing the row. The tight-cover alternative already has a certificate consumer.

Goal: remove the unnecessary five-incidence dependency from this exact-five production route. Do not claim that the remaining physical admission is closed. The prior draft is unelaborated; correct signatures in the upstream modules rather than importing Rigid221Closure backward.

## Priority B — formalize the new circle-power and MEC-vertex exclusions

From two distinct strict first-cap points `q,w` on the first-apex class, and a second carrier center `e` bisecting them:

1. localize `e` and put it between the pair with the existing cap APIs;
2. retain the global chord-side fact for EVERY carrier point outside the closed cap;
3. derive reflected coordinates `O=(0,0), e=(h,0), q=(u,v), w=(u,-v)`, `0<u<h`;
4. use the supplied `ExactFiveCirclePowerScalar.lean` algebra to prove an exterior hit `z` satisfies `dist O z < r`;
5. combine the earlier strict acute-apex inequality with the scalar outside-disk lemma, and the two cap-endpoint nonacute inequalities, to prove all three MEC support vertices have distance greater than the row radius from `e`.

The complete prose proofs are Sections 3–4 of `proofs-and-formalization.md`. These results genuinely close the double-hit subcases with an outer first-radius source or a supporting-triangle source. They do not close the inner small-radius case.

## Priority C — construct a minimum admissible pair before source commitment

Define admissibility on distinct points of `C∩I` by simultaneous second-apex deletion survival. The finite set is nonempty by the exact-five interior-frontier theorem. Minimize `dist` over that finite set.

Prove that the chosen pair is consecutive in the strict FIRST-CLASS slice: an intermediate class point gives two shorter subchords by cap nonacuteness; a robust second-apex row after deleting that point omits at least one original endpoint because their second-apex distances differ, producing a shorter admissible pair.

Then prove any other carrier bisector center is outside the first class. On the first-closed-cap-cardinality-five child, the entire strict cap has three class points, so there is no other carrier bisector at all. Both actual cross omissions and blocker distinctness follow.

Construct a new `ExactFiveInteriorCriticalPairFrontier.ofInteriorPair`, transport the unchanged unique-radius/minimal/no-M44 fields, reprove the new interior obstruction, and rebuild downstream rows/freshness. Do not claim whole-cap consecutiveness or unchanged blockers. On larger caps, common blockers outside the first class remain possible and must be classified without losing the minimum-pair property.

## Priority D — consume a genuinely global blocker witness

In the full double-hit case, its small row excludes all three MEC support vertices. A carrier-centered selected circle cannot contain the entire noncollinear MEC triangle, so the retained-blocker row omits a triangle vertex `V`. The TOTAL `H` gives the actual blocker `h=beta(V)`. Canonical support equality plus the two robust apices proves `h∉{O,P,b,e}`. Its exact row contains the named MEC vertex, outside the small disk.

Formalize that producer only if useful to an immediate source-complete consumer. The actual open mathematics is a geometric/owner/fiber/renewal argument consuming this row and the global assumptions. Returning a generic fifth-center record or a new sorry is not closure.

## Mandatory negative-control guard

`exact-evidence.md` contains an exact thirty-point strictly convex example with the genuine nonobtuse MEC support triangle, first-apex unique five-class, physical rows, named unique failure rows, and K4 at ALL seven named points `O,L,R,a,d,e,z`. The fresh blocker row still contains both original interior sources with a smaller radius.

It is not global K4: twenty-three other centers fail. Its only unique-four rows cover seven points and omit the MEC vertices, so there is no total critical system. Do NOT promote a local contradiction using only the satisfied hypotheses. The fresh source equals the retained blocker center in this example, an alias permitted by the current exact-row contract; do not count that erased center as a remaining carrier point.

## Audit requirements

Report exact source revision and final FQNs. Run focused `lake env lean`/`lake build` and `#print axioms` for every new producer. Record source-to-caller wiring and all missing premises explicitly. No `sorry`, `admit`, new axiom, native reduction for these small lemmas, unsafe implementation redirection, backwards historical-wrapper import, or unproved order/renewal field.

The existing balanced-555 closure has approved native trust; do not call its aggregate dependency kernel-only. Do not infer its cover hypotheses from cardinality twelve.

Deliver `.lean` and `.md` artifacts with exact proof status. The full exact-five physical contradiction remains open unless the actual terminal is supplied and its dependency audit succeeds.
