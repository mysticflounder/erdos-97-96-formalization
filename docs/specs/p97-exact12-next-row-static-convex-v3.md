# Exact-12 nextRow static convex v3: implementation and promotion contract

Date: 2026-08-10

Status: the deterministic compiler, fresh-schema runner, source inventory,
typed Lean certificate adapter source, and focused differential tests are
implemented. Repeated authenticated cell-0 refinements remain `SAT`; no
terminal proof exists. Three exact-row cuts are in the existing source bank.
The latest survivor also yields a sound five-center positive-membership union
covering all 48 source orders. Its generic and concrete CNF bridges elaborate
in an isolated scratch overlay, and the resulting 117-clause delta is bound by
an authenticated one-entry v2 bank. That bank is not runner-integrated because
the static-convex parent still lacks its Lean auxiliary-variable valuation.
The shared full build is separately blocked by the ongoing Rigid221 refactor,
so no transitive-axiom or live-spine claim is made. Terminal UNSAT,
whole-formula Lean ingress, aggregate coverage, and live `sorry` closure remain
open.

## Closure objective

Close the exact-12 `nextRowOnlyHit` child beneath
`false_of_exactFourRigid221_sourceHeavy_secondOppositeLarge_pentagonOffClassBlocker`
through one source-faithful finite formula, an authenticated proof-producing
terminal solve, and kernel-checked Lean ingress. Version 3 preserves the full
v2 formula and adds the generic convex perpendicular-bisector family exposed
by the first v2 cell-0 canary.

The lane is closed only when the source geometry supplies a Lean-checked
valuation of every auxiliary variable and static clause, every dynamic clause
has its typed consumer, the exact terminal formula has a checked DRAT/RUP
proof, the Lean clause list is proved equal to that DIMACS formula, every
scheduled exact-12 placement cell is covered, the result is wired into the
live child, and refreshed spine plus transitive-axiom audits are clean.

Compilation, SAT-survivor elimination, green tests, one-cell UNSAT without
Lean formula ingress, or all-cell finite coverage without spine wiring are not
closure.

## Source theorem and exact side conditions

The v2 canary satisfied every source, base-CNF, v1, and v2 replay gate but its
first structural certificate had stage
`equality-perpendicular-bisector-convex`. It supplied two distinct foci and
three pairwise distinct non-focus labels whose incident edges are equal in
row-generated equality closure.

The theorem bank already contains the source theorem
`Problem97.Census554.EqualityCore.false_of_convexIndep_of_perpBisectorCore`.
It applies given a squared-distance realization, carrier membership for every
label, convex independence of the carrier, and a checked `PerpBisectorCore`;
the new adapter is responsible for supplying those premises.
The new Boolean certificate `PerpBisectorData.check` verifies exactly the four
distinctness conditions and three equality paths needed to build a
`PerpBisectorCore`. The typed adapter
`SourceOrderPositiveNogood.ofPerpBisectorCertificate` consumes the convexity
hypothesis already carried by `SourceOrderPositiveNogood.refutes`. Both the
generic checker and this adapter are currently source-authored, pending focused
elaboration and a transitive-axiom audit; no source-clean promotion is claimed.

The adapter is intentionally in
`ExactTwelveRigid221StaticConvexSourceOrderConsumer.lean`, outside the
hash-pinned historical terminal-bank consumer. This preserves old source-order
bank hashes and journals while binding the stronger v3 source inventory to the
new declaration.

## Frozen v3 formula

Python schema: `p97_rigid221_exact12_next_row_static_convex.v3`.

For each unordered focus pair `{a,b}` and each unordered triple `{p,q,r}` of
distinct labels outside that pair, append

```text
¬eq(pa,pb) ∨ ¬eq(qa,qb) ∨ ¬eq(ra,rb).
```

There are
`C(12,2) * C(10,3) = 66 * 120 = 7,920` unique clauses. Version 3 adds no
variables. For placement cell 0 the frozen formula has:

- 44,875 variables;
- 634,859 clauses;
- v3 clause-delta SHA-256
  `49e6e5d5b8b4c68f7bccb0356d5bf38ce9ee25b0397fe555a00f9cddb2fbafa2`;
- final DIMACS SHA-256
  `0d4ab5fa17a63ef862533615eb534b0fb4be5892052788e2a47477e7bd250ff9`.

The compiler checks the complete count, literal arity, relation-variable
scope, clause uniqueness, parent-formula hash, and single installation.
Canonical replay checks the v1, v2, and v3 deltas on the minimal equivalence
closure generated by selected rows.

## Runner isolation and trust boundary

The fresh schemas are:

- run: `p97_rigid221_exact12_next_row_static_convex_cegar_run.v3`;
- job: `p97_rigid221_exact12_next_row_static_convex_cegar_job.v3`;
- cut: `p97_rigid221_exact12_next_row_static_convex_source_order_cut.v3`;
- survivor: `p97_rigid221_exact12_next_row_static_convex_survivor.v3`.

The job authenticates the v1-v3 compilers, detector stack, geometric kernel,
generic certificate, separate v3 adapter, and the unchanged live source-order
bank. Only existing generated Lean-backed source-order entries may be appended
dynamically. A duplicate-center, equilateral-bisector, or perpendicular-
bisector detector hit after SAT replay is a
`STATIC_CONVEX_INVARIANT_FAILED`; every other structural stage is preserved as
an unadmitted survivor.

Terminal promotion remains
`AWAITING_LEAN_STATIC_AUXILIARY_VALUATION`. A solver proof cannot be promoted
until Lean proves the canonical squared-distance-equality valuation satisfies
the entire v3 clause family and the replayed Lean terminal formula is the
authenticated terminal DIMACS formula.

The positive-membership sidecar is more precise. Its status is
`LEAN_PATTERN_CNF_BRIDGE_COMPLETE_AWAITING_STATIC_PARENT_VALUATION`. The
authenticated v2 bank has SHA-256
`553c0f6861c6e6186cd5e59f276a68f0031f9f0575fd7baed9d711f7ac9e7d94`.
It extends the frozen cell-0 parent from 44,875 variables and 634,859 clauses to
44,880 variables and 634,976 clauses. The 117-clause delta has SHA-256
`13f57853a644cb6c7bccc115707819dcad907e605643f927af3db82730196194`.
All five terminal, UNSAT, universal-lift, live-closure, and runner-integration
claims remain false in the bank manifest.

## Validation and next gates

The positive-membership compiler and authenticated-bank checks are Ruff-clean,
and their focused pytest invocation passes 12 tests. The concrete bridge was
checked only in the isolated scratch overlay because the shared source tree is
mid-refactor. The broader v3 compiler and runner suite previously supplied
these differential witnesses:

- the v2 canary satisfies all v2 static clauses and is rejected by v3;
- the earlier detector-clean witness still satisfies v3;
- all three static detector stages fail closed in the v3 runner; and
- the historical hash-pinned source-order consumer is restored byte-for-byte,
  so the existing live bank attestation remains valid.

The execution sequence is:

1. mirror the generated relation-variable layouts and canonical edge-closure
   valuation in Lean;
2. prove that valuation satisfies the duplicate-center,
   equilateral-bisector, and perpendicular-bisector static clause families,
   then compose the whole frozen cell-0 parent-formula theorem;
3. import the concrete positive-membership bridge and replay the exact extended
   formula against that parent theorem;
4. only after this terminal ingress is green, launch the bounded twelve-cell
   structural CEGAR wave and repeat theorem-family search on survivors;
5. on terminal UNSAT, rerun the byte-identical CNF with proof tracing and check
   the terminal proof against the authenticated Lean clause list;
6. wire checked all-cell coverage to the live exact-12 child and refresh spine
   and transitive-axiom reports.

The `card ≥ 13` children remain a separate source-theorem problem. Exact-12
terminal coverage cannot by itself close them.
