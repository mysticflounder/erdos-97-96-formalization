<!--
Copyright (c) 2026 Adam McKenna. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Author: Adam McKenna <adam@mysticflounder.ai>
-->

# Full-tree sorry closure ledger (2026-07-06)

Companion to [`closure-plan-2026-07-06.md`](closure-plan-2026-07-06.md) (the
master closure plan). The master plan owns cross-cutting strategy per
obligation; this ledger enumerates **every `sorry` in the source tree** —
on-spine, off-spine, and in-proof — with a closure route and acceptance test
for each. Nothing in the tree is outside this ledger.

## 0. Inventory method and snapshot caveat

Measured 2026-07-06 (post-blueprint-unblock) via three independent probes:

1. `proof-blueprint index --refresh` + `symbols --with-sorry` — decl-level
   scan of the current source tree (imported or not): **7 sorried decls**.
2. `rg '\bsorry\b'` over all of `lean/` — textual scan: 14 files mention
   `sorry`; 11 of them in prose/comments only ("no sorry here" notes);
   **3 files carry code sorries: 85 textual `sorry` terms**.
3. `proof-blueprint spine` — kernel view: 5 on-spine obligations, off-spine
   section as of the **last build** (see lag note below).

The three probes reconcile exactly: 85 textual terms live inside the 7 decls.
No unimported project file carries a code sorry (probe 2 covers the whole
tree; probe 1 confirms at decl level).

**Snapshot caveats.** (a) The tree is under active concurrent edit (liveData
and erased-pin lanes); line numbers are advisory, decl names are the keys.
(b) The mined graph lags the working tree by one build: `spine` still lists
`isM44NonSurplusContainmentErasedPinTripleReductionInputs` as a third
off-spine sorried decl, but current source has already restructured it into
an inline `have hreduce : …ReductionInputsStatement := by sorry` inside the
on-spine obligation (`RemovableVertexAxiom.lean:406-408`); the next
build+mine will show 2 off-spine sorried decls (the U2 pair), not 3.

## 1. The complete inventory

| # | Decl (Problem97.…) | File:line | Spine | Textual | Lane owner |
|---|---|---|---|---|---|
| 1 | `isM44EndpointResidualsExcluded` | RemovableVertexAxiom.lean:391 | ON (slot 2) | 1 (`:= sorry`) | four-point lane |
| 2 | `isM44PinnedSurplusResidualsExcluded` | RemovableVertexAxiom.lean:397 | ON (slot 2) | 1 (`:= sorry`) | four-point lane |
| 3 | `isM44NonSurplusContainmentErasedPinTripleResidualsExcluded` | RemovableVertexAxiom.lean:404 | ON (slot 2) | 1 (inline `have … := by sorry` at :408) | erased-pin session (anchor 019f300c) |
| 4 | `U1LargeCapRouteBTailMetricResidualTarget.DoubleApexOffSurplusSharedRadiusPair` | U1LargeCapRouteBTail.lean:2383 | ON (slot 3) | 1 (at :2428) | census budget-partial + D probe landed 2026-07-07 (§2.4); census resumed + C-split gated go 2026-07-07 — k=4 bank + n=12 gate dispatched |
| 5 | `u1_largeCap_routeB_tail_liveData_false` | U1LargeCapRouteBTail.lean:3251 | ON (slot 3) | **79** (case leaves, :3518–:5613) | liveData session (anchor f1cc942b) |
| 6 | `U2NonSurplusSqueeze.oppCap2_endpointEscape_false` | U2OppCap2Escape.lean:296 | OFF | 1 | unowned — decision needed (§2.6) |
| 7 | `U2NonSurplusSqueeze.surplusEscape_pinnedFamily_sep_false` | U2OppCap2Escape.lean:393 | OFF | 1 | unowned — decision needed (§2.6) |

Kernel collapse: the 79 textual sorries of row 5 are all inside **one** decl's
proof body — the kernel sees one obligation. Rows 1–5 are exactly the five
named obligations of the master plan §1. Rows 6–7 are reachable from no
target and count for nothing until wired (spine off-spine policy).

Prose-only mentions (no action): FormAEndpointPairKill (5 lines),
N8CapCoordNorm (4), U1TwoShortCapReduction (2), P96/UpstreamBridge (2), and
one line each in U5ModeADeletion, OangleBridge, N9Endpoint/N4e,
N4d/FaithfulFrame, MEC/ArcAngle, Dumitrescu/Lc3, Erdos9796Proof.lean.

## 2. Per-sorry routes

### 2.1 `isM44EndpointResidualsExcluded` (row 1)

Route: master plan §2, unchanged — the RowZeros stack
(`Direct`/`Product`/`Bank`) is **built but unimported**; remaining work is
wiring only: import the endpoint soundness stack into the module chain that
proves `IsM44EndpointResidualsExcludedStatement` and replace `:= sorry` with
the assembled proof. LOW uncertainty. Acceptance: the obligation leaves the
`spine` open-obligations list (spine diff, not build log).

### 2.2 `isM44PinnedSurplusResidualsExcluded` (row 2)

Route: master plan §3, now sharpened by the general-m enumeration
(2026-07-06, `certificates/surplus/reports/pinned_surplus_generalm_required_facts.{json,md}`,
generated by `scripts/pinned-generalm-required-facts.py`):

- **Headline: the erased-center residue is EMPTY.** All 3041
  nonzero-coefficient generators across the 135 relaxed-split leaves fall in
  four fact categories: exact v-center (269), exact w-center (338),
  Rabinowitsch separator (133 — exactly one on each leaf that has one, never
  zero-coefficient), erased-center facts whose both witnesses lie in the
  leaf's `common_erased_masks` (2301). Zero residue instances; all 135 files
  pass the validator; output byte-identical across runs.
- Consequence (master plan §3 step 1 resolved): the general-m route needs
  exactly (i) the relaxed DFS completeness theorem (split_path bits are
  case hypotheses), (ii) per-leaf transport of the four fact kinds —
  v/w-center facts from the pin, separator via
  `pinnedSurplusTenLabels_pairwise_of_mem`, erased-center facts via
  ⊆-monotone submask lemmas (`leaf_common_submask` source, 2301 uses).
  No fifth fact kind exists.
- Near-term: land the m = 5 exact-bank path and the pre-named
  general-m residual `isM44PinnedSurplusGeneralMResidualsExcluded`
  (house placeholder pattern) so the obligation shrinks now and the
  general-m content lands against a named surface.

Acceptance: spine diff as in §2.1; the named general-m residual (if used)
appears as the strictly narrower replacement obligation.

### 2.3 Erased-pin inline `hreduce` (row 3)

Current shape (post-restructure): the on-spine theorem's transport is fully
written; the single sorry is the inline
`have hreduce : IsM44NonSurplusContainmentErasedPinTripleReductionInputsStatement := by sorry`
(`RemovableVertexAxiom.lean:406-408`). The four-point plan records the
reduction surface (chain / primitive-row / one-sided-payload splitters) as
already landed above it. Route: the erased-pin session produces the
reduction-inputs witness from that splitter surface — this is that lane's
active work; no second owner. Probe input (2026-07-06, RESOLVED-SAT): the
one-hit bounds `leftAdjCount ≤ 1` / `rightAdjCount ≤ 1` at interior centers
are FALSE (exact witnesses, `scratch/two-hit-probe/report.md`) — the count
facts route via seeded-census extension with the two-hit rows, not via
`AdjacentChainOneHitData` (unproducible at interior centers). Ledger policy: if the witness production
splits again, split into **named** residual theorems (house pattern), not
additional naked inline sorries. Acceptance: row-3 obligation leaves the
spine list; off-spine section stays clean (no re-orphaned ReductionInputs
theorem left behind in source).

### 2.4 `DoubleApexOffSurplusSharedRadiusPair` (row 4)

Route: master plan §5 ((5,5,4) census with the surplus-designation WLOG
caution and smoke gate) + §7 (workstream D for |A| ≥ 12; D1 DONE — 5 leads,
no direct transfer; D2 DONE 2026-07-06 — NO clean combinatorial separation,
so the |A| ≤ 11 slice stays census-shaped and D3 aims at
ideal-theoretic/geometric certificates via the PROVEN forced gauge-pair
collapse; dichotomy DONE 2026-07-06 — all mined dead cores ℂ-dead, so D3
poses ideal-theoretically with ℚ-Nullstellensatz certificates in the
endpoint-bank shape; D3 direction DECIDED 2026-07-06 by Adam — uniform
ideal-theoretic lemma first, certificate census fallback; D3 formulation
pass DONE 2026-07-06 — the decided shape is FALSIFIED at n ≥ 15 by
explicit realizable witnesses (lattice n=15; concentric-ring n=16, 16, 20,
general-position/full-H1/fully-exact; independently verified,
`scratch/d3-formulation/`), which also kill the pattern-level census
fallback for the tail; witnesses are non-convex so the leaf itself stands;
Candidate B row-decorated remnant FALSIFIED 2026-07-07 (ρ embeds in every
ring witness, every enumerated candidate a solution, exact-verified —
`scratch/d3-formulation/candidateB/`), so the ideal-theoretic uniform
shape for the tail is CLOSED; remaining routes: Candidate C head split
n ∈ {12,13,14}, Candidate D convexity-coupled forcing (no mechanism);
split decision to Adam — plan §7 D3; RESOLVED 2026-07-07: Candidate D
probe first — per-type convex-order exclusion over the 19 reflected
primitive N8 tuples + composition test targeting obstruction D1,
dispatched to `scratch/candidate-d-probe/`; C census extension deferred
pending the probe verdict; probe LANDED 2026-07-07, validated — pure
per-type route CLOSED: 13/19 classes REALIZED at the decorated minimal
window with exact independently re-verified witnesses, obstruction D1
CONFIRMED token-level at every n = 12–16; PROVEN extractions m ≤ 2,
vertex-pair ⇒ s = 0 (2 unconditional kills), s = 2 ⇒ straddle-only;
s ≥ 3 kills ride SUB2, CONJECTURED; C-split DECIDED 2026-07-07 (Adam):
gated go — k=4 bank LANDED 2026-07-07 validated (10 dead classes
confirmed, certificates kernel-checked in `K4WindowBank.lean`, unimported;
plan §7 D3), n = 12 window-shape convergence gate in flight
(`scratch/census-12-gate/`), full 12–14 census pending the gate readout —
`scratch/candidate-d-probe/report.md`). Proposed
split residual
`DoubleApexOffSurplusSharedRadiusPairCard12Up` separates the census-closable
slice from the uniform-rigidity frontier. This is the only row whose route
has genuine open-strategy uncertainty (register U4). Census dispatched
2026-07-06; **BUDGET-partial 2026-07-07**: 1596 orbits / 40,524 instances
banked and verified (equality-only throughout — U8 resolved clean), no
ALIVE cube in 376 witnesses, coverage incomplete and resumable
(`scratch/census-554/{STATE.md,report.md}`; plan §5 census status).
Acceptance: either the obligation closes, or it is replaced on the spine by
the strictly narrower Card12Up residual with the |A| ≤ 11 slice
kernel-closed.

### 2.5 `u1_largeCap_routeB_tail_liveData_false` — the 79 case sorries (row 5)

**Status correction to master plan §6:** this lane is not parked. The
working tree carries an active ~2300-line expansion (file grew ~3.3k → 5.6k
lines): a complete branch split of the f2 critical row's label assignments —
exactly option "complete branch split" from the decl's own header comment.

Case-tree anatomy (measured):

- **84 fully-ordered leaves** enumerated by comment markers: 36 base
  `Ordered subcube` leaves (pattern: `q_t20`, `q = l1`, then l2/l3/l4
  matched to a permutation of {t1,t2,t3}) and 48 `Ordered source-slot
  subcube` leaves (pattern: `t*_t20` source-slot identification with
  `by_cases` on row-l1 coincidences).
- **12 leaves already closed** by row-consumer lemmas
  (`hfalse_of_q_slot0_t1Row_l1`,
  `false_of_f2CriticalRow_center_p_t20BaseExclusions` and siblings).
- **72 leaf sorries open** in three 24-sorry bands (:3518–:4365,
  :4525–:5139, :5213–:5585), plus **7 terminal top-level branch sorries**
  (:5602–:5613), two of which sit inside the
  `f2CriticalRow_all_named_or_t2ResidualHRowFailure` split.

Closure route, per the decl's header (the lane's own stated target): each
fully-ordered leaf pins the row's selected labels to named points, so a leaf
dies by a row-vs-fixed-triple incompatibility consumer — the same kill shape
as the 12 already-closed leaves — or, where the depth-5 data is genuinely
insufficient (the COMP-1 obstruction recorded in the header), by feeding the
`MetricResidualTarget` sink or adding the sixth-critical-row consumer.
Options named in the header: named residual fields / complete branch split
into the sink / sixth critical row + consumer / `(Q)_C5` fragment /
equivalent incidence theorem.

Ledger requirements on this lane (coordination, not new work):

1. **End-state shape:** the 79 naked sorries must not persist — reduce to
   the house pattern: ≤ a handful of **named** residual theorems with
   `…Statement` abbrevs, so the spine shows named surfaces, not case debris.
2. **Consumer-first:** before hand-closing leaves one by one, factor the
   band-level consumer (the 24-leaf bands are permutation orbits of the same
   configuration — one lemma per band family should kill the orbit).
3. **Tripwire (master plan §6) stands:** any bounded forced-subsystem
   statement produced here gets tested as a liveData producer.

Acceptance: kernel obligation count for this decl goes 1 → 0, or 1 → K
named residuals (K small, each with a statement abbrev and a route note).
Textual `sorry` count in the file returns to ≈ the number of named open
obligations.

### 2.6 The U2 pair (rows 6–7) — wire-or-delete

Facts: both decls are off-spine (kernel-verified); nothing imports their
results into any target chain. `U1LargeCapRouteBTail.lean` re-derives its own
`oppCap2_escape_gen` in a local namespace — the spine does not depend on this
file. Both lane plans already take positions: the slot-3 plan
("must not be treated as live slot-3 closure work unless … explicitly wired
back into the spine") and the four-point plan (its Endpoint Escape Route
section plans a **re-derivation** in current `SurplusCapPacket` vocabulary as
`EndpointEscapeRightAt`/`EndpointEscapeLeftAt`, not reuse of the rvol-era
statements).

Decision routes:

- **(a) Delete-with-record (recommended).** Move the two statements, their
  doc-comments, and the pointer to
  `docs/u-lane/97-u2b-surplus-escape-branch-analysis-2026-07-01.md` into
  `docs/dead-ends.md` as named open leaves of the superseded rvol-vocabulary
  route; delete the two sorried decls (git history preserves the full text).
  Anything in `U2OppCap2Escape.lean` that still builds sorry-free
  (reflection-kill machinery, `no_oppCap2_escape` minus its sorried dep) is
  kept only if some lane names it as an import target; otherwise it goes
  with them. Precondition: 5-minute sweep confirming no doc names these two
  decls as live obligations (done for the two lane plans + dead-ends.md;
  remaining: none found by decl-name grep).
- **(b) Wire.** Only if the four-point reduction's endpoint-escape work
  decides the rvol-vocabulary statements are the right targets after all —
  then the consumer must land first (spine diff showing them ON spine), and
  they become ledger rows with routes (both are finite-configuration,
  certificate-shaped: the endpoint case cites the validated 117-pattern
  enumeration; the pinned-family case is a 4-point exact-radius kill).

**EXECUTED 2026-07-06 — Adam chose (a), attic variant.** The whole file moved
to `attic/U2OppCap2Escape.lean` (git history + attic preserve full text);
`U1LargeCapRouteBTail`'s import swapped to `U2NonSurplusOneHit` (transitive
surface unchanged; module rebuilds clean); dead-ends.md entry added. Rows 6–7
are closed as tree obligations; the statements stay recorded as superseded
rvol-vocabulary leaves.

Acceptance: `spine` off-spine sorries section reports **none** — confirmed at
next mine (the stale erased-pin `ReductionInputs` entry clears at that lane's
next build).

## 3. Tree-level acceptance — the definition of "all sorries closed"

All four gates, kernel-side:

1. `proof-blueprint verify-publish` passes on **both** targets
   (`Problem97.erdos97_rhs`, `Problem96.erdos96_rhs`) — requires rows 1–5
   closed AND `Lean.trustCompiler` approved in
   `.blueprint.toml [axioms].approved` (**approved by Adam 2026-07-06** —
   register U6 closed).
2. `proof-blueprint symbols --with-sorry` reports **0 symbols**.
3. `proof-blueprint spine`: off-spine sorries **none**; open obligations
   **0** on both targets.
4. `rg '\bsorry\b' lean/ -g '*.lean'` returns prose mentions only (the 11
   comment files; tighten those at leisure, they gate nothing).

Standing policy while closing: new sorries land only as named residual
theorems on the spine (house pattern); a sorry that would land off-spine is
either wired the same session or not landed.

## 4. Sequencing and ownership

| Order | Row(s) | Next action | Blocked on |
|---|---|---|---|
| now | 1 | wire RowZeros stack, delete `:= sorry` | nothing — wiring only |
| now | 3 | produce reduction-inputs witness from splitter surface | erased-pin lane (in flight) |
| now | 5 | band-level consumers; reduce 79 → named residuals | liveData lane (in flight) |
| next | 2 | m = 5 exact path + named general-m residual; then general-m per §2.2 (residue EMPTY — route confirmed) | four-point lane capacity |
| ~~next~~ DONE | 6–7 | ~~execute (a)~~ atticked 2026-07-06 (Adam) | — |
| dispatched | 4 | 2026-07-06 wave all landed: (5,5,4) census BUDGET-partial (1596 orbits verified, no ALIVE, resumable — plan §5) + D2/dichotomy/two-hit done + D probe landed (per-type route closed, §2.4) | census resumed 2026-07-07 (installments); C-split gated go — k=4 bank + n=12 convergence gate in flight |
| last | — | ~~trustCompiler approval~~ approved 2026-07-06; final verify-publish ×2 | rows 1–5 |

Rows 1, 2 (m = 5 slice), 3, and the 12-closed-leaf pattern of row 5 are
mechanical against already-built surfaces. Row 5's COMP-1-blocked leaves and
row 4 are the two places where content, not wiring, is still being produced.

## 5. Master-plan deltas applied with this ledger

- §3 step 1 (general-m enumeration): **resolved — residue EMPTY**; register
  U1 closed accordingly.
- §6 (liveData): status corrected from "parked + tripwire" to **active
  in-tree branch split** (this ledger §2.5); tripwire unchanged.
- Pointer to this ledger added (master plan and README).
